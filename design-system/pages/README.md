# Page Overrides

Create one file per page only when that page genuinely needs a documented override from `../MASTER.md`.

Rules:

- Keep each file to deltas only
- Do not restate the whole master design system
- If a rule becomes global, move it back to `../MASTER.md` or the real SSOT docs
