# 学習直後の成長演出6

2026-09-09。[実画面比較](review.html)。DEV限定 `home-journey-growth-reveal-v6`。公開切替なし。

## 変更

通常学習の区間完了で初めて越えた閾値を親画面に一時保持する。ホームへ戻ると、花壇、玄関ひさし、店、ベンチ、テラス、配達箱の該当する実物だけが1.6秒で現れ、少し弾んで等倍へ収まる。ぽこもこも小さく跳ねる。2.5秒後に演出を消費するため、再読込や再訪で繰り返さない。

45問目は箱が画面外にいる通常巡回から始めず、置く瞬間を先に見せる。reduced motionでは動かさず完成形を表示する。節目名は読み上げ専用で通知し、画面下の既存説明を重複させない。学習入力、次問題、報酬、成長値、保存内容は変更していない。

実画面確認で腕が大きく膨張して見える中間案を拒否し、腕の拡縮を0.8〜1.25倍に制限した。距離は配達者の立ち位置で合わせる。

## 検証

[ブラウザ記録](browser-report.json)：HMR履歴を捨てて起動し直したDEV target。phone 390×844、tablet 768×1024（reduced motion）で空DBから通常学習45問、成長演出、到着、近景／全景、全保存データの不変、Canvas継続、再読込と同じ予約の再開を確認。pageerror 0、実参加者 N=0。

変更対象16テスト、typecheck、build/assets、lint（エラー0、既存warning 2）通過。全回帰は304ファイル中301通過、3,291件中3,286通過。並行作業中の小数入力・分数カーソル・小数ヒントに関する既存範囲で5件失敗し、本変更のhomeJourney対象は全通過。

## 独立判定

- 視覚：HOLD。変化と配達箱は読めるが、家の最終素材品質は基準未達。
- 無言理解・安全：膨張して見える腕を除去。独立した子ども5人の確認は未実施。
- Runtime：DEV接続の実画面は通過。本番保存移行、公開全経路、新景観でのoffline/PWAは対象外。

