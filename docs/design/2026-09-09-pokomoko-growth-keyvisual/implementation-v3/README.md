# 家の近景・島全体の切替

2026-09-09。[実画面比較](review.html)。DEV限定 `home-journey-close-contact-v3`。公開切替なし。

## 実装

初期表示を家中心の近景にし、「家の近く」「島全体」でカメラのみ切り替える。Canvasを再生成せず、進行や学習記録、配達の経過を維持する。主人公の着座位置は胴体下面から算出。運搬から置く間は手先アンカーを荷物の左右へ合わせる（肩の回転と拡縮）。関節を持つ最終キャラクターのIKではない。

共有作業中のビルドで検出したIslandProblemPromptのoptional questionTextも空文字で扱うよう補正した。

## 検証

対象8テスト、lint、typecheckを含むbuild/assets、docs:check通過。前回の全3,254件通過後、今回は変更対象を検査。

[ブラウザ記録](browser-report.json)：phone390×844とtablet768×1024（reduced motion）で空DBから通常学習45問、配達、カメラ切替、保存全体の不変、Canvasの継続、再読込と同じ予約の再開を検査。pageerror0。実参加者N=0。

v7は停止していたdev serverへの接続失敗。v8ではタブレットの屋根上端が切れていたため画角を修正。掲載画像は修正後のv9。source.jsonは共有dirty DEVの選択ファイルhashで、productionの固定build証拠ではない。

## 独立した判定

- 視覚：HOLD。近景で表情を読めるが、キービジュアル相当の家・地形・素材ではない。
- 無言での理解・安全：未判定。手先位置の計算は通過したが、配達者の背中で荷物と手元が隠れる。お茶を飲む演技や最終的な接触品質は未完了。子どもの確認は未実施。
- Runtime：今回のDEV接続は通過。本番の保存移行、全経路、新景観でのPWA/offlineは対象外。activeタスクは継続。
