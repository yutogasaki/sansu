# Nature Town NT-5：将来規模の負荷診断

2026-09-19。**PARTIAL：3条件は全世界一致、96人・24地区は計算が終わらず打切り**。SAFE-07はNOT_RUNを維持。実機iOS・長期運用も未確認。アプリ変更なし、検証scriptと記録は未コミット。

## 対象と診断条件

- 実行URL `http://127.0.0.1:5337/#/nature-town`。build `nt3-index-20260918:49487bd0-d20d-4d36-ae22-8e26ff6605df`。
- ソース `11d0d25954e35239a02bf5762437ded1742fba66` の隔離コピー。Nature Town関連36ファイルの一致を確認。flagは `VITE_ISLAND_ENABLED=true` / `VITE_NATURE_TOWN_ENABLED=true`、候補 `nature-town-living-s1`。
- 390×844・動き抑制、headless Chromium。端末/OS/CPU/版は[詳細結果](runtime-report.json)に記録。
- 24／96人×8／24地区。各地区16×16で4列に配置。診断用平地に食たく・家4軒・畑2枚・花・木を置く。人口を分散し在庫・参照・物量をvalidatorで確認。初期保存を注入した負荷fixtureであり、通常プレイによる獲得ではない。
- 各条件で30tick。実アプリtimerのコールバックを、前の保存と2回のRAFを待ってから診断用に呼ぶ。固定fps・実時間30秒の検査ではない。先に同じ隔離ソースのNode stepWorldで30tickの期待世界を計算する。

## 実測値

| 人口 | 地区 | 描画 | 30tick所要秒 | 観測RAF/秒 | callback平均/最大ms | 終了JS heap MiB | 保存payload KiB |
|---|---|---|---|---|---|---|---|
| 24 | 8 | 通常 | 6.4 | 24.1 | 162 / 2452 | 21.2 | 328 |
| 24 | 8 | 省略 | 6.3 | 24.4 | 159 / 2380 | 35.2 | 328 |
| 96 | 8 | 通常 | 10.3 | 17.4 | 290 / 6225 | 32.6 | 362 |
| 96 | 8 | 省略 | 10.5 | 17.2 | 292 / 6253 | 36.7 | 362 |
| 24 | 24 | 通常 | 15.3 | 14.8 | 230 / 1177 | 82.5 | 961 |
| 24 | 24 | 省略 | 15.8 | 13.9 | 229 / 1182 | 66.9 | 961 |

96人・24地区はNode側の30tick期待値計算が完了せず、直前条件の完了から **153秒以上** 経過して手動停止した。どのtickで長時間になったかは初版で未計測。ブラウザ版の96人・24地区を実測したとは扱わない。

callbackは実アプリの同期区間であり、純粋なstepWorld単独時間ではない。Node単独のtick平均/p95/最大、保存と描画待ち込み時間、long task一覧も詳細結果に保存。JS heapはGPU/ブラウザ総メモリを含まず、payload長は前世代・DB管理領域を含まない。RAFはheadlessの観測値で物理画面のFPSではない。特に数秒の同期処理は操作を止めるため、性能の合格を主張しない。

## 描画省略と世界一致

診断用CSS `.town-cell,.town-resident-marker { content-visibility:auto }` で画面外内容を省略。アプリへ出荷する最適化ではない。完了した3条件では通常/省略の両方が、同じstepWorldを30回適用した全世界と一致。画面外の地区・住人、在庫、乱数ordinal、学習progressも保持。

初版はコンテナ自身のcheckVisibilityを測り、省略件数を0と記録した。コンテナの存在と内容の省略は異なるため、子要素の可視性を測るよう修正。同じbuild/CSSの24人・8地区を1tickだけ再確認し、**224個の子要素の省略**と全世界一致を確認。[追加診断](visibility-report.json)。全負荷測定は繰り返していない。[全測定時のscript原本](harness-measured.mjs)と[修正版](../../../tools/e2e-nature-town-load.mjs)を区別する。

## 次に直すこと

住人×畑×食たくの候補選択で、経路探索とグラフ生成を繰り返している。これはコードから見た調査候補で、CPUプロファイルによる寄与率は未計測。次は長いtickの内訳を取り、同じ経路・同順位の選択・乱数・物量を保ちながら重複計算を減らす。96人・24地区は改善後に再測定する。

- 実装整合: 完了3条件の世界一致PASS、全負荷条件は **PARTIAL**。SAFE-07と受入44/47を維持。
- 性能: **改善が必要**。通常描画で最大約6.2秒の同期区間、最大条件は計算未完了。
- 見た目の魅力: **HOLD**。最終美術・演技は別件で継続。
- 無文字理解・安全: 参加者未評価、0人。
- 実機iOS・固定30/60fps: 未確認。今回の測定で代替しない。

## 重要経路の画像一覧

| 状態 | 通常 | 省略 |
|---|---|---|
| 24人・8地区 | [画像](24-8-full.png) | [画像](24-8-culled.png) |
| 96人・8地区 | [画像](96-8-full.png) | [画像](96-8-culled.png) |
| 24人・24地区 | [画像](24-24-full.png) | [画像](24-24-culled.png) |

全条件は `node tools/e2e-nature-town-load.mjs`。現版の96人・24地区は非常に重いため、改善前に繰り返さない。省略確認だけは `SANSU_TOWN_LOAD_PROBE=1 SANSU_TOWN_LOAD_TICKS=1`。`SANSU_TOWN_URL` / `SANSU_TOWN_SOURCE_ROOT` / 新規 `SANSU_TOWN_OUTPUT` を指定可能。[hash一覧](sha256.json)。
