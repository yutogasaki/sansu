import {chromium} from '/private/tmp/sansu-canopy-material-worktree/node_modules/playwright/index.mjs';
import {inventory,closeMenu} from '/private/tmp/sansu-canopy-material-worktree/tools/island-life-ui-helpers.mjs';
import {readFile,mkdir,writeFile} from 'node:fs/promises';
const source=JSON.parse(await readFile('/tmp/sansu-facility-production-final2/report.json'));const test=source.cases[0],out='/tmp/sansu-facility-walking-diagnostic';await mkdir(out,{recursive:false});const c=await chromium.launchPersistentContext('/tmp/sansu-facility-production-final2/phone-browser',{viewport:test.viewport});const p=c.pages()[0];p.setDefaultTimeout(15000);const caught=[];const d=await c.newCDPSession(p);await d.send('Debugger.enable');await d.send('Debugger.setPauseOnExceptions',{state:'all'});d.on('Debugger.paused',async e=>{caught.push({exception:e.data?.description,frames:e.callFrames.slice(0,5).map(f=>({function:f.functionName,location:f.location}))});await d.send('Debugger.resume').catch(()=>{});});
try{await p.goto(source.target);await p.locator('.life-world[data-rendered="true"]').waitFor();await inventory(p,test.library);await p.getByRole('button',{name:'ぽこもこを よぶ',exact:true}).click();await closeMenu(p);await p.waitForFunction(id=>JSON.parse(document.querySelector('.life-world').dataset.lifePoses).some(p=>p.id==='pokomoko'&&p.itemId===id&&p.facilityUse?.action==='reading'),test.library);await inventory(p,test.hut);await p.getByRole('button',{name:'ぽこもこを よぶ',exact:true}).click();await closeMenu(p);await inventory(p,test.hut);await p.getByRole('button',{name:'みてみる',exact:true}).click();await p.getByRole('combobox',{name:'たてものから みるもの'}).selectOption(test.flower);await p.waitForTimeout(1000);await p.screenshot({path:`out.png`.replace('out',out+'/screen')});await writeFile(`${out}/report.json`,JSON.stringify({scope:'Caught-exception diagnostic; debugger pauses affect timing',caught,text:await p.locator('body').innerText(),view:await p.locator('.life-relation-view').evaluate(n=>JSON.parse(n.dataset.relationView))},null,2));}finally{await c.close();}
