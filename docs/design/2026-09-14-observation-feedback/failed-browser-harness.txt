import assert from 'node:assert/strict';
import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {chromium} from 'playwright';
import {inventory,closeMenu} from './island-life-ui-helpers.mjs';
import {readNative} from './island-e2e-helpers.mjs';
const root=process.env.SANSU_LIFE_FEEDBACK_SOURCE,out=process.env.SANSU_LIFE_FEEDBACK_OUTPUT;
assert(root&&out);const source=JSON.parse(await readFile(`${root}/report.json`));assert(source.pass);assert(['localhost','127.0.0.1'].includes(new URL(source.target).hostname));await mkdir(out,{recursive:false});
const build=JSON.parse(await readFile(process.env.SANSU_LIFE_FEEDBACK_MANIFEST));
async function verify(){for(const f of [...build.files,...build.distFiles])assert.equal(createHash('sha256').update(await readFile(f.path)).digest('hex'),f.sha256);}
await verify();const html=await readFile(build.distFiles.find(f=>f.path.endsWith('/index.html')).path,'utf8'),entry=html.match(/<script\b[^>]*type="module"[^>]*src="([^"]+)"/)?.[1];assert(entry);
async function record(p){return p.evaluate(async()=>{const request=indexedDB.open('SansuIslandLifeV1');const db=await new Promise((ok,no)=>{request.onsuccess=()=>ok(request.result);request.onerror=()=>no(request.error);});try{const q=db.transaction('worlds').objectStore('worlds').getAll();const rows=await new Promise((ok,no)=>{q.onsuccess=()=>ok(q.result);q.onerror=()=>no(q.error);});if(rows.length!==1)throw Error('One earned owner required');return rows[0];}finally{db.close();}});}
const report={sourceHash:build.sourceHash,version:build.version,target:source.target,scope:'Real earned profiles, explicit visits and target choices; no DB injection, clock acceleration or debugger pauses',cases:[],pass:false};
try{for(const test of source.cases){const c=await chromium.launchPersistentContext(`${root}/${test.device}-browser`,{viewport:test.viewport,reducedMotion:test.device==='tablet'?'reduce':'no-preference'});const p=c.pages()[0];p.setDefaultTimeout(45000);try{
 await p.goto(source.target);await p.locator('.life-world[data-rendered="true"]').waitFor();if(await p.locator('script[type="module"][src]').first().getAttribute('src')!==entry){await p.evaluate(async()=>{await(await navigator.serviceWorker.ready).update();}).catch(e=>{if(!String(e).includes('Execution context was destroyed'))throw e;});await p.waitForFunction(expected=>document.querySelector('script[type="module"][src]')?.getAttribute('src')===expected,entry);await p.locator('.life-world[data-rendered="true"]').waitFor();}
 assert.deepEqual(await(await p.request.get(new URL('/version.json',source.target).href)).json(),build.version);const native=await readNative(p),initial=await record(p),wallet=await p.locator('[data-life-drops]').getAttribute('data-life-drops');
 await inventory(p,test.library);await p.getByRole('button',{name:'ぽこもこを よぶ',exact:true}).click();await closeMenu(p);await p.waitForFunction(id=>JSON.parse(document.querySelector('.life-world').dataset.lifePoses).some(p=>p.id==='pokomoko'&&p.itemId===id&&p.facilityUse?.action==='reading'),test.library);
 await inventory(p,test.hut);await p.getByRole('button',{name:'ぽこもこを よぶ',exact:true}).click();await closeMenu(p);const position=await p.locator('.life-world').evaluate(n=>{const {projection,view}=JSON.parse(n.dataset.lifeCamera),r=n.getBoundingClientRect();const mul=(m,v)=>[0,1,2,3].map(row=>m[row]*v[0]+m[4+row]*v[1]+m[8+row]*v[2]+m[12+row]*v[3]);const q=mul(projection,mul(view,[2,.045,-1.5,1]));return{x:r.left+(q[0]/q[3]+1)*r.width/2,y:r.top+(1-q[1]/q[3])*r.height/2};});await p.mouse.click(position.x,position.y);await p.getByRole('button',{name:'みてみる',exact:true}).click();const before=await record(p);await p.getByRole('combobox',{name:'たてものから みるもの'}).selectOption(test.flower);
 const panel=p.locator('[data-life-observation]');await panel.getByText('みちを とおっているよ。ついてから もういちど えらんでね。',{exact:true}).waitFor();assert.equal(await panel.getByText('しまの きろくを たしかめられなかったよ。もういちど ためしてね。',{exact:true}).count(),0);const rejected=await record(p);assert.deepEqual(rejected.actions,before.actions);await p.screenshot({path:`${out}/${test.device}-walking.png`});
 await p.waitForFunction(()=>JSON.parse(document.querySelector('.life-relation-view')?.dataset.relationView??'{}').status==='bench');assert.deepEqual((await record(p)).actions,before.actions,'Arrival must not automatically retry a rejected choice');
 // A deliberate ordinary comparison verifies retry independently of whether
 // another resident has reserved the flower during the walk.
 await p.getByRole('combobox',{name:'たてものから みるもの'}).selectOption(test.library);await panel.locator('[role="alert"]').waitFor({state:'detached'});let after;const until=Date.now()+10000;do{after=await record(p);if(after.actions.length===before.actions.length+1)break;await p.waitForTimeout(100);}while(Date.now()<until);assert.equal(after.actions.length,before.actions.length+1);assert.equal(after.actions.at(-1).command.type,'observe-relation');assert.equal(after.actions.at(-1).command.targetId,test.library);await p.screenshot({path:`${out}/${test.device}-retried.png`});
 assert.equal(await p.locator('[data-life-drops]').getAttribute('data-life-drops'),wallet);assert.deepEqual(await readNative(p),native);assert.deepEqual(after.discoveryJournal.savedIds,initial.discoveryJournal.savedIds);await p.getByRole('button',{name:'みてみるを とじる',exact:true}).click();await c.setOffline(true);await p.reload();await p.locator('.life-world[data-rendered="true"]').waitFor();assert.deepEqual((await record(p)).actions,after.actions);assert.deepEqual(await readNative(p),native);report.cases.push({device:test.device,viewport:test.viewport,wallet,beforeActions:before.actions.length,rejectedActions:rejected.actions.length,afterActions:after.actions.length,pass:true});
 }catch(error){report.failure={device:test.device,error:error.stack,text:await p.locator('body').innerText(),record:await record(p),view:await p.locator('.life-relation-view').evaluate(n=>JSON.parse(n.dataset.relationView)).catch(()=>null)};await p.screenshot({path:`${out}/${test.device}-failure.png`});throw error;}finally{await c.close();}}
 await verify();report.pass=true;}finally{await writeFile(`${out}/report.json`,JSON.stringify(report,null,2));}
