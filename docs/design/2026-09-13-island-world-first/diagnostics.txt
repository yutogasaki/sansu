First DEV focused run: saving retry succeeded, but the driver assumed the color panel would remain selected. It now opens the color tab explicitly before checking the saved selection.
First production combined run: phone including Chromium SW offline passed; the next viewport hit a screenshot protocol error. The original catch masked the triggering exception with a failed screenshot. The catch now records the original error first. The standalone tablet run passed on unchanged app inputs.
WebKit: UI, save-failure/retry, learning return, and online SW-controlled reload reached the offline boundary. Playwright then returned "WebKit encountered an internal error" from page.reload after setOffline(true). The prior island remained visible. Offline document replacement was not proven; this is not a real iOS PWA result.
