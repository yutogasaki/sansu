import { chromium } from '/tmp/sansu-canopy-material-worktree/node_modules/playwright/index.mjs';
import { readFile } from 'node:fs/promises';
const root='/tmp/sansu-world-shadow-ui6';
const panels=[['C3 world reference','/Users/yutogasaki/Projects/sansu/docs/design/references/2026-09-13-canopy-dots/reference-c3.png'],...['phone','tablet'].flatMap(d=>['world-ready','pokomoko-greeting','otter-greeting','saved-memory','stored-bench','learning'].map(s=>[`${d}: ${s}`,`${root}/${d}-${s}.png`]))];
let html='<style>body{margin:0;padding:20px;background:#edf1ed;color:#243a33;font:16px system-ui}main{display:grid;grid-template-columns:repeat(7,240px);gap:14px}figure{margin:0}img{width:240px;height:510px;object-fit:contain;object-position:top;background:white}h1{font-size:22px}p{font-size:13px}figcaption{height:28px;font-size:13px}</style><h1>M3 actual app critical path — C3 visual HOLD / Human N=0</h1><p>5233 DEV Island/Life; bark study OFF | source 468b00716a58 | version dfdd99a1-7551-4490-b52f-b2e1bdda38d1 | candidate canopy-dots-c3-v1 | fixed QA record, not natural acquisition</p><main>';
for(const[label,path]of panels)html+=`<figure><figcaption>${label}</figcaption><img src="data:image/png;base64,${(await readFile(path)).toString('base64')}"></figure>`;
html+='</main>';const browser=await chromium.launch();try{const page=await browser.newPage({viewport:{width:1804,height:1250},deviceScaleFactor:1});await page.setContent(html);await page.locator('img').evaluateAll(images=>Promise.all(images.map(i=>i.decode())));await page.screenshot({path:'/tmp/sansu-world-shadow-contact.png',fullPage:true});}finally{await browser.close();}
