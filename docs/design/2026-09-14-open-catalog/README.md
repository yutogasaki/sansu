# 枠を重ねない商品選択と建物の識別

ユーザーの「ボックスUIださい」「建物が似たような外観」を受けた作業中の候補。完成・本番反映・子どもの理解の検証済みとは扱わない。

## 最終検証の範囲

ローカル本番用ビルド `http://127.0.0.1:5307`、`development-local:b052a594-0f36-4889-a344-ba1bc19ae34b`。Island/Life/Discovery=true、Life preview=false、BuildPlay=false。世界は既存のmoon-garden。C3の本番有効化や外部配信ではない。

390×844と768×1024で、実際の導入3問による獲得、12品表示、苗購入、移設・収納、オフライン学習、Life保存のみの失敗と再試行、再接続までPASS。元の学習記録と所有権を保持し、報酬の二重加算なし。修正後の画面とreportは `runtime/`、固定ソースと配信ファイルのSHAは `build-source.json`。`contact-sheet.png` は同じ最終ビルドの実画面。

初回DEVでは高さ640pxとタブ移動・閉じるも確認したが、最終ビルドの640px検証ではない。全施設の自然利用・二つのビルド間の更新・実機・人の無文字理解を証明しない。Runtimeは記載範囲PASS、視覚はHOLD、独立観察者0人。商品をさらに大きく見せる余地がある。

## 対象と方向

実装対象は Life のメニューと、既存の園芸小屋・図書室の描画。商品ボタン、タブ、メニュー外周の箱を外し、共有する明るい面に実際の立体モデルを直接並べる。購入価格・保存・入口座標・占有範囲・既存住民は維持。

| C3から引き継ぐ特徴 | 引き継がないもの |
| --- | --- |
| 大きな色面、用途の読める暮らし、広い余白 | 生成された住民、固定配置、細部の複製 |
| 黄色・桃色・青の屋根の分節 | 色だけを変えた同型の建物 |

園芸小屋は片流れ屋根と開いた作業棚、図書室は丸い屋根と外から見える本棚。新しい住民の動作・性格は追加しない。既存の入口での行為をそのまま使う。

## 最初の実画面

`phone-first.png` はローカル DEV `http://127.0.0.1:5250`、390×844。Island/Life preview 有効、既存の vault/shelter/buttress/lagoon/turf C3試作上に表示。親リビジョンは `8a9ec86`、作業中ソース。ビルド固定・本番の証拠ではない。

- 視覚: カードと画像背景の箱は除去、建物の外形は分岐。余白と模型の大きさは引き続き調整対象。C3全体の合格を主張しない。
- 無文字理解/安全: 独立観察者0人。モデルを区別できるという作者の判断は理解の合格ではない。
- runtime: 検証進行中。学習入力・保存ロジックは今回の差分に含まない。

## 検証中に見つかった修正

初回ソース `098324c8453445f9f5f8b2a577521d60412f8f2bd8c75b729ee077a932c57d8c` は看板に持ち物と同じ `life-held-book` 名を使ったため、住民の本の個数と表示判定で4テスト失敗。看板を `life-library-sign` に分離し、既存の施設/関係10テストPASS。合わせて説明行の残存枠を除去。初回の画面とFAILログは `before/` に保存。

修正後ソース `f03b6f45255db23a342d71341251614fb476d0f9003c19e7971bd999c879c25f` は core 411ファイル/3958テスト、lint/typecheck/build/assets PASS。初回の画面を修正後のスクリーンショットとして扱わない。
