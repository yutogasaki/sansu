# しずく・ひかり・育ちの表示

2026-09-12、ユーザーの配置・デザイン見直しをローカルに実装。仕様48の表示変更で、公開作業ではない。

通常画面は、見出しの下にしずく・ひかりの残高だけをまとめた生成りの面を置く。既存の玩具アイコンと同じミント・黄、異なる輪郭、名前・整数で見分ける。ボタンは増やさない。育ちと今日のいぶきは「しまの ようす」へ移し、資源の増え方と使い道もここで読める。

一時通知は残高の下に置き、増加数と次の任意操作を分ける。編集・配置・情報パネルの間は通知を重ねない。パネルは残高の下に収め、題名と閉じる操作を固定し、本文だけをスクロールする。世界とカメラの表示サイズは変えない。

## 継承と変更範囲

前の実画面は `../2026-09-12-island-belongings/phone-island.png`。生成り・藍・既存アイコンの色・実3Dモデルを継承。通貨と成長状態を同列にした広いカード列、通知と残高の重なりは継承しない。資源の獲得額・価格・成長速度・一度だけの通知基準・7秒・学習入力・writerは変更しない。

共有作業ディレクトリには先行のもちものUI、商品スワイプ、住人モーション変更がある。対象は `LifeResources.tsx`、`life-resources.css`、`IslandLife.tsx`のHUDと情報パネル部分、仕様48、専用QAと本記録。その他をこの作業の成果としない。

## 検証

- docs:check、lint、typecheck、build: PASS。lintは既存IslandMilestone.tsxの警告1件、最終対象ファイルのlintは警告なし。
- 全unit/integration: 351 files / 3,665 tests PASS。後続のCSS寸法・スクロール構造の修正は型検査・対象lint・再build・専用ブラウザ経路で確認した。
- 共通スモーク: 31 scenarios PASS。DEV共通導線の回帰として固定productionとは区別する。
- asset budget: 98 precache files、10.72 MiB / 12 MiB。新しい画像素材は追加していない。
- 専用QA: `tools/e2e-island-life-resources.mjs`。`SANSU_RESOURCES_URL`と新規`SANSU_RESOURCES_OUTPUT`を指定し、必要なら`SANSU_RESOURCES_WIDTH`で幅を限定する。

320×568、390×844、768×1024 reduced motion、844×390で実初回設定→通常学習3問→+6しずくを確認。0残高、情報パネルのスクロールと44px退出、世界サイズの不変、残高/通知/パネルの分離、制作/もちもの/色/拡張のメニュー、今日のいぶき3/6、再読込の残高保持と通知非重複を検査。閲覧・再読込で7つの学習storeは不変。音に依存する操作は追加しない。

5桁の検査は表示DOMへ99999を入れるタイポグラフィ診断であり、大量creditや学習実績の生成ではない。実残高は再読込で6へ戻り、保存の不変を確認する。

## 修正した初期診断

- 短い画面で残高とパネルの余白不足：パネル上端の予約領域を確保。
- 5桁で数字が内部列からはみ出す：残高面を284pxへ調整し、省略せず検査。
- 横向きで大きいスクロールパネルの背後に矩形が見える：見出しとスクロール本文を分離。Threeの世界描画は変更せず解消。初期の実画面を `initial-landscape-artifact.png` に保持。
- 7秒通知の撮影後に同じ通知を押すと、撮影待ちの間に消える：アプリの表示時間は延ばさず、撮影後のメニュー確認には常設入口を使用。通知の増加数と位置は消去前に検査する。

## 判定と配布状態

実画面は `contact-sheet.html`、結果は `report.json`、固定ビルドとhashは `artifact.json`。

- 視覚：4サイズの実画面で情報量・桁の可読性・パネルの重なりを確認。最終の好みの承認はユーザー確認待ち。
- 無説明理解・安全：形と文字、非懲罰的な育ちの表示を保持。独立した子どもの観察はN=0、HOLD。
- Runtime：記載した対象経路はPASS。実機iOS、インストール済みPWA、故障注入、通常連問の再計測、全アプリの公開判定を兼ねない。
