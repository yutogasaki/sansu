import {build} from '/Users/yutogasaki/Projects/sansu/node_modules/esbuild/lib/main.js';
import {loadFixture} from '/Users/yutogasaki/Projects/sansu/tools/nature-town-load-fixture.mjs';
import assert from 'node:assert/strict';import fs from 'node:fs/promises';
const engines=[];for(const root of ['/tmp/sansu-nt3-index-t1bn4rns','/Users/yutogasaki/Projects/sansu']){const b=await build({stdin:{contents:"export {newWorld,makeChunk,makeProp,makeResident,context} from './src/domain/natureTown/world';export {stepWorld} from './src/domain/natureTown/simulation';export {assertWorld} from './src/domain/natureTown/validation';",resolveDir:root},bundle:true,platform:'node',format:'esm',write:false});engines.push(await import(`data:text/javascript;base64,${Buffer.from(b.outputFiles[0].text).toString('base64')}`));}
const report=[];
for(const [people,chunks] of [[24,8],[96,8],[24,24],[96,24]]){const initial=loadFixture(engines[0],people,chunks,'Load');const row={people,chunks,runs:[]};for(let mode=people===96&&chunks===24?1:0;mode<2;mode++){let w=initial;const times=[];for(let i=0;i<30;i++){const t=performance.now();const result=engines[mode].stepWorld(w,engines[mode].context());times.push(performance.now()-t);w=result.state;}engines[mode].assertWorld(w,'Load');if(mode===0)row.expected=w;else if(row.expected)assert.deepEqual(w,row.expected);row.runs.push({mode,mean:times.reduce((s,n)=>s+n,0)/times.length,max:Math.max(...times),ticks:30});}delete row.expected;report.push(row);console.log(JSON.stringify(row));await fs.writeFile('/tmp/nt5-compare.json',JSON.stringify(report,null,2));}
