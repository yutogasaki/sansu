# Nature Town NT-5：経路探索の重複削減

2026-09-19。**4負荷条件の計測・全世界一致PASS**。前回未完了だった96人・24地区も完走。SAFE-07はPASSへ、受入45/47。性能の実機保証や最終美術の合格ではない。未コミット・未公開。

## 実装と確認

以前は住人×畑×食たくの組合せごとに探索とグラフ生成をやり直していた。96人・8地区の旧版先頭tickをCPU計測し、約5.85秒のうち `routeOn` が約3.46秒、`graph` が約0.66秒、`validBridges` が約0.52秒だった。[サンプリング結果](cpu-profile-summary.txt)。

今回、始点ごとに探索を再利用する `routesFrom` を追加。優先度付きキューで最小コストの候補を選び、同コストなら最初に発見された順を維持する。配送の割当では全員の移動後から割当終了までだけグラフと探索を共有。散策では住人1人の判断中だけ共有する。混雑度が変わる移動や次tickをまたいで使わず、返す経路配列は毎回独立させる。保存schema・学習ルール・乱数・供給条件は変更しない。

[旧版との比較](old-new-world-comparison.json)では24人・8地区／96人・8地区／24人・24地区の30tick後の全WorldStateが一致。96人・8地区のNode単独最重tickは約5,788ms→67ms、平均273ms→36ms。96人・24地区の旧版30tickは前回打切りのため完全な旧新版比較を主張しない。新実装では平均212ms、最大606msで30tickを完走した。[当時の比較コード](comparison-measured.txt)。

追加3テストは同距離/混雑度付き/道路限定/到達不能の全経路比較、経路配列の独立性、次の探索で混雑・地形変更が反映されることを確認する。既存の長時間物量・学習・保存等の全unitも実行。lint（既存警告1件）、typecheck、429ファイル/4,055 unit、flag有効buildとassets検査はPASS。[隔離ソースと配信ファイルのmanifest](build-manifest.json)に記録。

## 実行対象

- URL `http://127.0.0.1:5338/#/nature-town`。390×844、動き抑制、Chromium版・端末情報は[詳細結果](runtime-report.json)。
- 基点 `11d0d25954e35239a02bf5762437ded1742fba66` に `grid.ts` / `transport.ts` / `life.ts` と追加テストだけを重ねた隔離ソース。別作業を含まない。エンジンのSHA、実行script/fixtureのSHAは詳細結果に記録。
- flag `VITE_ISLAND_ENABLED=true` / `VITE_NATURE_TOWN_ENABLED=true`。候補 `nature-town-living-s1`。
- build `nt5-optimized-20260919:0f4a665b-657b-470f-8cc2-68a4d5664efc`。画面でも `nt5-optimized-20260919` を確認。

## 本番形式のブラウザ計測

[前回と同じ診断fixture](../2026-09-19-nature-town-load/README.md)を共通化して使用。24／96人×8／24地区、通常描画と診断用 `content-visibility:auto` を各30tick。前の保存と描画完了を待ってアプリtimerを進める加速診断であり、実時間30秒や物理画面FPSの検査ではない。

| 人口 | 地区 | 描画 | 30tick所要秒 | callback平均/最大ms | 観測RAF/秒 | 終了JS heap MiB | 保存payload KiB |
|---|---|---|---|---|---|---|---|
| 24 | 8 | 通常 | 2.04 | 15.4 / 39.3 | 73.1 | 40.4 | 328 |
| 24 | 8 | 省略 | 2.11 | 15.7 / 39.8 | 71.0 | 30.7 | 328 |
| 96 | 8 | 通常 | 3.07 | 45.6 / 68.9 | 58.1 | 28.7 | 362 |
| 96 | 8 | 省略 | 3.15 | 45.0 / 68.4 | 56.9 | 21.8 | 362 |
| 24 | 24 | 通常 | 10.02 | 40.9 / 64.3 | 23.8 | 42.7 | 961 |
| 24 | 24 | 省略 | 10.01 | 40.8 / 61.9 | 21.9 | 74.4 | 961 |
| 96 | 24 | 通常 | 14.79 | 183.6 / 594.4 | 16.8 | 68.1 | 1000 |
| 96 | 24 | 省略 | 14.56 | 179.4 / 564.8 | 15.3 | 66.9 | 1000 |

全4条件の通常/省略保存世界が同じソースの期待値に完全一致。学習progress保持、pageerrorなし、画面外の子要素が実際に省略されたことも確認。省略CSSは診断のみで、出荷コードへの追加ではない。

callbackは同期区間で保存/描画全体とは異なる。保存と描画込み時間、Node tickのp95/最大、long taskも詳細結果に記録。JS heapはブラウザ総量やGPUを含まず、保存payloadは前世代・DB管理領域を含まない。単一ローカル測定で統計的な端末性能保証ではない。大規模状態には依然100ms超の処理があり、実機の応答性は別途評価する。

## 判定と残件

- 実装整合: **PASS**。探索結果・物量・乱数・保存を保ち、4負荷条件完走。SAFE-07の測定条件を満たす。
- 見た目の魅力: **HOLD**。最終美術・全身受渡し演技は別タスク。
- 無文字理解・安全: 参加者未評価、0人。
- NT-5: 実機iOSは未確認。RNG-02固定fpsは対応環境待ち。SAFE-06、参加者評価も残る。

## 重要経路の画像

| 状態 | 通常 | 省略 |
|---|---|---|
| 24人・8地区 | [画像](24-8-full.png) | [画像](24-8-culled.png) |
| 96人・8地区 | [画像](96-8-full.png) | [画像](96-8-culled.png) |
| 24人・24地区 | [画像](24-24-full.png) | [画像](24-24-culled.png) |
| 96人・24地区 | [画像](96-24-full.png) | [画像](96-24-culled.png) |

再現script: [load](../../../tools/e2e-nature-town-load.mjs)、[fixture](../../../tools/nature-town-load-fixture.mjs)。隔離ソースの `SANSU_TOWN_SOURCE_ROOT`、配信URLの `SANSU_TOWN_URL`、`SANSU_TOWN_EXPECT_REVISION=nt5-optimized-20260919`、新規 `SANSU_TOWN_OUTPUT` を指定する。古い隔離ソースを既定のまま使わない。[hash一覧](sha256.json)。
