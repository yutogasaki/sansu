# 体験改善の統合確認

現行の固定候補は `84d3ddf-experience2-3ddcac1103f0`。共有遊びの頭・胴体を各姿勢で確認する画角修正を625 app入力で固定し、core 145ファイル・1694テスト、docs/lint/typecheck/build/assetsがPASS。原QAは58入力。今回の[公開検証](../2026-09-08-production-release/README.md)で実UI、clean共通回帰、Island PWA/実offlineと正式速度80 runがPASS。clean treeの実数は144ファイル・1676テストで、上記の以前の記録とは区別する。子どもN=0。問題が出た配置での顔の読み取りを、機能テストだけで合格にしない。

3ddの一般共有遊びは両幅・88画面がPASS。基本6場面のgather/share/enjoy18画像で、出所・受渡し・結果を作者確認した。別の `known-fox` は1回の実準備訪問で既知の家具配置・役割・第三住民位置を再現し、1経路/38画面がPASS。問題だったウサギの顔は樹冠から見えるようになり、実gather/share/enjoy画像で確認できた。原QAを保ち、追加QAは625 app入力と同じ配信versionを参照する別manifestで58 QA入力を照合した。

この既知配置の最初のgather描画はCPU465.3ms、重なったlongtaskは501ms。実clickから最初のgather観測までは1553msで、歩行も含む。単発のローカルChromium診断であり、純粋な画角計算時間・実機性能・正式回答速度とは区別する。顔の遮蔽は解消したが、この開始時の長い処理はサクサク感の残課題。

再開時に公開検証のmanifestと3ddの全625入力・production versionの一致を確認した。公開検証のIsland正式80 runは再利用し、実DEV version末尾 `f37cbdcc-b029-410e-aee8-312102bf0043` をproductionのversionと区別する。正答後の入力可能P95はphone193.8ms / tablet194.1ms、誤答後の再入力は194.2 / 193.5ms。全正答の処理量はStudy比2.120 / 2.088、全gateとevidence.eligibleがtrue。自動キーによる固定問題測定であり、人の学習速度ではない。

3dd追加回帰はIsland DEV11、Park DEV6、Parkの更新checkpoint3項目と実SW offline、共通筆算13経路、smoke31、classic更新4項目がPASS。Park/classicは同じ625 app入力から対応flagで別buildを作り、Island配布artifactへ混ぜない。各runnerの実行前後で625 app入力・58 QA入力が一致。production targetは対応するversionを開始/終了に照合した。DEVのversion.jsonは生成されないため、DEVの結果には実際のDOM識別と実行対象を使い、productionの識別を補作しない。classic正式40 runもPASS/evidence.eligible=true。独立したclean検証repoの同一commitと実起動したDEV対象を照合し、元の閾値と操作列を維持した。

この確認後、ユーザーが「通常の3問区切りの操作が学習を妨げる」「最初は報酬を密にし、通常は強弱をつける」と指摘した。[更新後の作業目標](../../../tasks/active/2026-09-07-experience-improvements.md)では通常の区間跨ぎも追加0操作を要求する。旧Island正式80 runの継続操作1回は記録どおり保持し、新しい基準のPASSとしない。別途進行しているホーム/起動導線の変更も、固定3ddの実測へ合算しない。

## 直前の5d7候補

`84d3ddf-experience2-5d7be27ceacb` は、前向きな学習観測、住民ごとの短いしぐさと対象だけの素材反応、初回実描画後の復旧案内修正を625 app入力で固定した比較候補。core145ファイル・1687テストがPASSしたが、下記の顔の遮蔽で視覚HOLDだった。以下の結果は新しい3dd候補へ転記しない。

5d7の追加測定（2026-09-08）:

| 項目 | 実測・確認結果 | 範囲 |
|---|---|---|
| 前向き学習記録 | 数字入力と筆算の2経路・11画面がPASS | 誤答→訂正、ヒント/お手本/支援完了、途中段、再読込、次回答を実DOMと保存eventで照合 |
| 住民の短い反応 | 自由遊び2画面幅が機能PASS。通常キツネ/ウサギ、学習ウサギを撮影 | 最初のrunの不足を既存の後続訪問で補測し、通常全3種とreducedカワウソを確認。学習カワウソ/キツネは未撮影 |
| 対象だけの素材反応 | 6家具を実獲得した2画面幅・60画面がPASS | 通常の花弁/灯り/波紋の変化と復帰、対象外の葉/噴き上げの保持、次の学習での解除 |
| 表示復帰 | 共有遊び2画面幅の全機能検査がPASS | 2回の実context喪失→再試行を含む88画面。独立した8画像レビューで古い字幕の残留なし。着座字幕が出る復旧配置は未観測 |
| 通常学習・支援・初回・中断・復帰・Island PWA | PASS | 学習23/139画面、支援9/45、初回6/49、中断2/20、実獲得2/20、再確認4/25、東土地2/18。対応flag回帰・正式速度は次候補へ延期 |

観測QAの初回はphoneを通過したが、tablet筆算の途中段ボタンを通常回答と同じ名前で探索し全体FAILとなった。アプリは変えず、共通TenKeyの既存submit属性を指定する1行だけのQA修正で再実行し、両経路がPASS。初回FAILは保存した。自由遊びの補測も元の既存訪問とreduced確認の観測位置だけを変える。625 app入力と配信versionは維持し、改訂QA58入力の別manifestで開始/終了を照合する。元の固定archiveや結果は上書きしない。

通常ウサギの小さなしぐさは家具による下半身の重なりと全景サイズの制約が残る。学習ウサギの対象への向き直りと復帰は、両画面の実画像で読めた。単一画像で小さな動作すべてを判定せず、美術/可読性、子どもの無説明理解、安全性、runtimeを別に扱う。

[5d7の画面一覧](candidates/5d7be27ceacb/contact-sheet.html) は全574枚の原本SHAを照合し、現在519・以前の試行42・承認比較4・旧HOLD比較9を分離する。QA追補は明示したmanifestとSHAを固定し、app625の同一性と各runnerのQA58を照合。観測reportの一部識別欄やPWAの時刻欠測は補完せず、個別画像と実行証拠を区別した。

**作者の視覚判定はHOLD**。基本の花/星/水玉×2画面幅は出所→手渡し→受け手を追える一方、`tablet-fox-flower-gather.png` は手前の紫樹冠が運び手ウサギの顔をほぼ隠す。小物と耳・手が見えることを、参加者が読めることへ置き換えない。この画角を次候補で修正するため、5d7の対応flag全回帰と正式速度は未実行のまま保存する。子どもの無説明理解も未測定。

## 比較用のb155固定候補

比較用の固定検証対象は `84d3ddf-experience2-b155db0a54ec`。十進ブロックのヒント領域を修正し、618 app入力を固定した。core 141ファイル・1606テスト、通常学習23経路・139画面、共有遊び両画面・84画面がPASS。段階支援9経路/45画面、初回6経路/49画面、6家具×2画面/60画面、自由遊び2画面/18画面もPASS。正確性の回帰は下記までPASS、Islandの正式速度80 runもPASS。表示復旧後に古い案内文が残るため、画面状態の整合はHOLD。実参加者は **N=0**。

通常学習の開始/終了で618 app入力・固定QA入力・実配信versionが一致した。作者は最新のphone十進ブロックのヒント画面とtablet筆算のヒント画面を確認。旧版ではみ出したお手本ボタンは画面内に収まり、数の意味と全キーが保持されている。この確認は子どもの理解や実機操作の実証ではない。

共有遊びは実獲得からの3組、専用キツネ参加、再演/取消、動作設定切替、表示復旧、次の実回答と全14 storeの不変を確認。今回は両画面とも直接のキツネ配置が成立し、新QAの最大2回の前準備分岐はこのrunでは使っていない。その分岐の根拠は別archiveのNode診断であり、実訪問2回の成功へ置き換えない。

作者の最新画面確認では、花/水玉の受渡し先と結果を追えた。星はphoneで輪郭が細く、第三住民による身体下部/ランプ下部の部分遮蔽が残る。phoneのキツネ向け花の運搬途中は第三ウサギの鼻先へ一瞬重なり、その後の受渡し先は明瞭。完全遮蔽とは分けて記録する。両画面のcontext復旧後には旧「みずたまを ともだちへ」と「もういちど みるでしまをひらこう」が残った。機能PASSでこの不一致を相殺せず、次の修正対象へ加える。

中断検証の初回はphoneを通過し、tabletの入力ready直後に、まだ次の実描画記録がない段階を検査してFAILを保存した。QAだけを別コピーで改訂し、最初の入力readyかつ全身範囲を持つ描画を待つ。移動中である条件や時刻の起点は維持した。完全再実行 `interruption-2` は両画面5条件/20画面がPASS、全618 app入力とQA closureの開始/終了が一致。学習を押してから最初の実入力/全身描画までphone 26.3ms、tablet 22.8ms、どちらも1人が退避中・もう1人が待機中だった。これは単発の復帰診断で、正式throughputではない。

実獲得→遊び→再学習2画面/20画面、訂正/独力再確認4経路/24画面、東土地の実2区間獲得/歩行中/着座後の復帰2画面/18画面、Islandの更新checkpointと実SW offlineもPASS。新しい住民反応・前向き観測・復旧案内の修正は次段階の隔離実装であり、これらのb155結果に含まれない。

Island DEV 11、Park DEV 6、Parkの更新checkpointと実SW offline、共通筆算13、smoke 31、classic更新回帰がPASS。Park/classicは同じ618 app入力から対応flagで別buildを作り、Islandの配布versionと区別した。更新hook検証を実際の二build配布・実機インストールの証拠にはしない。

[b155の画面一覧](candidates/b155db0a54ec/contact-sheet.html) は、個別画像のSHAと実build識別がある画面、失敗試行、比較用の承認画像を分ける。DEVのSHA欠測54画像や、PWAの個別識別のない画像を補作して昇格しない。外側runnerでソースとtargetが一致した実行結果は、それぞれの実行記録として保持する。

正式速度測定の初回は3 runの後、次のStudy開始前に停止した。人工profile投入と未完了の初回プロフィール照会による移動が競合し、学習画面を待つ間に島homeへ移動していた。誤答後や速度閾値の失敗ではないが、全体FAILを保存した。目的URLへgotoする最初のQA修正もfragment内の移動となり、32 run後に同じ開始前停止が再発。文書を捨てる次の修正はIslandのDEV fixture用windowとcreating hookを失い、1 run後に停止した。3試行ともFAILのまま保持する。最小の最終QA案は、人工profile投入より前に空DBのwelcome DOM commitを待つ。これにより初回照会の完了を待ち、同じ文書のfixtureを保持する。固定問題・操作・時刻起点・回数・閾値は維持した。第4試行は全80 runを完走しPASS・evidence.eligible=true。618 app入力と改訂QAを外側runnerで開始/終了照合し、実DEV versionも1つだった。

正式測定は `island-throughput-4.json`（2026-09-08 01:35:43〜01:42:13 JST）。正答後の入力可能P95はphone 195.7ms / tablet 195.1ms、誤答後の再入力P95は195.1 / 195.0ms。全正答の10問処理量はStudy比2.328 / 2.341。通常問題の追加操作0、6問区切りの継続操作1回、入力残留なし、実atomic receiptが一致した。各viewportのIsland正答180・誤答20 sample。Chromium 145・Metal・reduced motion・音off・自動物理キーのDEV固定fixtureであり、実機・子ども・通常plannerの速度として扱わない。通常plannerは別のproduction学習23経路で確認している。classicの正式40 runはb155では未実行で、次の統合候補に対して測定する。

## 直前の5c5候補

以下は `84d3ddf-experience2-5c5b5289a8f8` の保存済み結果。最新候補の結果へ合算しない。通常学習のヒント後にはみ出しを発見したためHOLDだった。

| 対象 | 同じ5c5ビルドで確認したこと | 状態 |
|---|---|---|
| 段階支援 | 9経路・45画面。下書き保持、ヒント→お手本、全筆算、入力停止、再開、報酬とDueの整合 | 操作・保存PASS |
| 初回導線 | 6経路・49画面。空DBから設定、名前任意、プロフィール追加、旧探索優先、保存失敗/更新保護 | 操作・保存PASS |
| 組合せ遊び | phone全体PASS。tabletも花・星・水玉の各5段階、再演、途中の動作設定変更を通過。追加のキツネ用配置をQAが見つけられず停止 | 全体FAILを保持。配置探索を診断中 |
| 通常学習 | 最初の8経路は通過。9番目のphone十の棒の引き算で、お手本ボタン下端が844px viewportを約22px超える | FAIL。図の固定余白と支援領域を次候補へ修正中 |
| 共通検証 | core 141ファイル・1606テスト、lint/typecheck/docs/build/assets | PASS。全回帰・正式速度測定は未完了 |
| 子どもの理解・再遊び・定着 | 1人からの記録道具と短い手順を用意 | 参加者の結果待ち |

支援・初回導線は各harnessのsource closure一致とbrowser終了に加え、外側のrunnerで618 app入力・全QA archive入力の開始/終了SHA、実配信versionを照合した。外側の証拠は個別captureの欠測を補作するものではない。作者は最新版のWelcome、設定、ヒント、割り算全段のお手本、共有遊びの花と星を実見した。旧版で星の手元を隠した木と第三の住民は、今回のtablet受渡し画面では接点を隠していない。画面の魅力、子どもの無説明理解、runtimeを別に判定する。

## 直前の993候補

旧 `84d3ddf-experience2-993febcf1560` はcore 141ファイル・1599テスト、段階支援9経路・45画面、初回6経路・49画面がPASS。共有遊びは花の再演で参加者が交代し通常利用へ戻ったためFAILだった。5c5では同じ完了ペア・同じ保存配置への連続招待に限り同じ二人を優先し、現在の経路と可視条件を再評価する。

[993の画面一覧](candidates/993febcf1560/contact-sheet.html) は成功、失敗、過去のQA失敗、比較用の承認画像を分けて表示する。旧候補の成功を最新候補へ移さない。全ページ画像と画面内画像も区別している。

同じ993入力の [SRS仮想DB測定](../2026-09-07-srs-policy/README.md) は12条件が現行仕様と一致。同日/期限後の正解、04:00境界、同問訂正、二つの主練習化判定を測定した。これは定着の証明ではなく、具体的な新閾値は未採用。

[共有遊び・統合の詳しい記録](../2026-09-07-shared-play/README.md)、[1人の観察手順](../2026-09-07-single-child-study/README.md)。自動操作・作者確認は、子どもの理解や自発的な再訪を代替しない。

現行3ddの実target `http://127.0.0.1:5399/`、version `84d3ddf-experience2-3ddcac1103f0:3ee728b8-f6c5-4668-a583-a1d589ef39ab`、flag `VITE_ISLAND_ENABLED=true`、delivery `mystic-island-v1`、world `mystic-island-procedural-v2`、learning `mystic-island-learning-v2`、art `moon-garden`。画面寸法390×844・768×1024はブラウザのviewportであり、実端末測定ではない。
