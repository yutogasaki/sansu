# Whole-app UX/UI coherence — partial audit

> **Target guard:** The `live-36`–`live-39` flag-off captures below are legacy classic/Explore references only. They do not count as evidence for the current PokoMoko (Mystic Island) app. Current whole-app UI work must use `VITE_ISLAND_ENABLED=true`, open `/#/island`, and verify the Island root's `data-island-feature-enabled`, delivery, visual/learning candidate, and build-revision attributes. `/explore` remains an explicitly selectable secondary mode when Island is enabled, but it is not the canonical Island screen or whole-app audit target. The product title/repository alone does not identify the app mode. Legacy observations are retained as history, not silently relabeled.

- Status: partial; the full cross-surface and responsive audit is still open.
- Date: 2026-09-20 (Asia/Tokyo)
- Historical target for the latest focused captures: `http://127.0.0.1:5194` (flag-off classic; not the current app target)
- Canonical target for new work: `npm run dev` → Island-enabled `http://127.0.0.1:5198/#/island`. The first saved current-Island route survey is `live-45`: 13 screens at each of 390×844, 768×1024, and 1280×720; 14 at each of 844×390 and 1024×390 (including the scrolled play action); runtime identity is checked on every capture. The focused follow-up for the play scroll affordance is `live-49`; it supersedes copy iterations `live-46`–`live-48` only for that affordance, not the broader route survey.
- Source revision: base HEAD `28bd2526` plus the shared dirty working tree; the parent-history label change is described by `live-28`, the empty parent tablet state by `live-29`, the latest F-28 label fix/populated weak-badge fixture by `live-36`, and the F-12 rendered chart confirmation by `live-37`; `live-38` adds populated parent-history tablet evidence and `live-39` adds the 1280px flag-off route survey, both without app-source changes
- Source candidate ID: `sansu-whole-ux-2026-09-20-28bd252-live-37` (`live-38` and `live-39` exercise the same source candidate; they add parent-history tablet and 1280px route evidence without changing app source; earlier route evidence keeps its original candidate labels)
- Focused parent runtime (`live-36`): `buildRevision=development-local`, `VITE_ISLAND_ENABLED=false`, `VITE_NATURE_TOWN_ENABLED=false`, delivery `snap-root-v1`, visual lineage `pokko-field-v1`, no separate visual-candidate ID, and `navigator.serviceWorker.controller=false`. This is a development preview, not a released build.
- Focused stats runtime (`live-37`): same flags and app identity as `live-36`; no separate visual-candidate ID or service-worker controller. The radar and trend inputs were synthetic rows in a disposable QA profile, not a production or natural-learning data sample.
- Focused parent-history runtime (`live-38`): same development preview, flags, delivery `snap-root-v1`, and visual lineage `pokko-field-v1`; no separate visual-candidate ID or service-worker controller. Five recent attempts were synthetic display-only rows in a disposable profile; no learning logs or memory rows were written.
- Focused route runtime (`live-39`): `buildRevision=development-local`, flags `VITE_ISLAND_ENABLED=false` / `VITE_NATURE_TOWN_ENABLED=false`, delivery `snap-root-v1`, visual lineage `pokko-field-v1`, rendered Explore visual candidate `dig-pop-carry-bloom-v3`, and no service-worker controller. The disposable AuditQA profile answered no learning questions; the only gate answer was the parent check.
## Target-version incident and resolution (2026-09-21)

The earlier review opened the flag-off classic Explore surface and treated it as the current PokoMoko app. That was a target-selection error: the old generic `npm run dev` started the flag-off app on port 5173, while Island, classic Explore, and Nature Town shared one repository and product identity. The `live-36`–`live-39` records correctly describe their own flag-off previews, but their placement in a broad whole-app audit made the boundary too easy to miss. The F-30 Explore correction is retained for that optional secondary game; it is not evidence that the Island main screen was audited or fixed.

The stale default was not isolated to `01_app_spec.md`: `06_screen_specs.md` still said `/` redirected to `/explore`, and `15_mvp_rollout_verification_spec.md` still labeled Explore as the current entry and normal startup surface. Those statements conflicted with the current route contract in 12/28/43. The three specs now mark the old redirect, onboarding destination, and classic tab model as legacy Explore history while preserving `/explore` as an explicitly selected secondary mode.

The three relevant targets are now explicitly separated:

| Target | Entry | Meaning |
|---|---|---|
| Current PokoMoko / Mystic Island | `npm run dev` → port 5198 → `/#/island`, Island flag on | Canonical target for this whole-app UX task |
| Classic Explore | `npm run dev:classic` → port 5201 → `/#/explore`, Island flag off; Island-on `/explore` also remains available as an optional game | Preserved legacy/secondary mode; excluded from Island-wide completion evidence |
| Nature Town prototype | `npm run dev:nature-town` → port 5233 → `/#/nature-town`, both Island and Nature Town flags on | Separate owner/preview, neither classic Explore nor Island home |

Prevention: normal dev startup now selects Island and fails on port collision; classic has an explicit command; the core entry guard checks these routes/flags/ports plus the shared app-root Island/Nature Town flag markers and the Island runtime identity marker. Positive and negative guard tests reject the old generic default, a Nature Town-to-Explore route mix-up, missing shared flags, and missing Island identity. The legacy `npm run e2e:smoke` suite is also pinned to a separate raw-Vite test server and flag-off Classic Explore, and is not treated as current-Island evidence; the guard rejects a future switch back to the generic `npm run dev` path. Old screenshots and findings remain under their actual flag/candidate labels, never silently relabeled. The user-provided 5233 page is Nature Town, not classic Explore: a read-only GET of the already-running Vite preview's transformed module confirmed `VITE_ISLAND_ENABLED=true`, `VITE_NATURE_TOWN_ENABLED=true`, and the `/nature-town` route; the preview returned HTTP 200. The task neither started/stopped that process nor changed its working files or profile. The supplied screenshot remains the only visual sample reviewed here; no new screenshot or quality assessment of Nature Town was made. See the active task's [retrospective](../../../tasks/active/2026-09-20-whole-app-ux-coherence.md#2026-09-21-target-mix-up-retrospective) for cause, safeguards, and evidence limits.

The follow-up also found stale app-root guidance in project memory, the ownership map, the Explore implementation plan, and the risk register. Those operational documents now identify Island as the current app and Explore as an optional historical mode. `check:current-ui-entry` validates those four files plus product specs 01/06/15, and its regression tests reject the former launch instructions/headings or missing current-route context.

## Cross-route feature-flag identity (`live-53`, 2026-09-21)

The first-run Island Welcome already exposed its exact flag/delivery/candidate/revision, but the shared `.app-container` did not expose either feature flag. That made generic shared routes harder to classify from the rendered document alone. It now publishes `data-island-feature-enabled` and `data-nature-town-feature-enabled` on every route, alongside its existing build identity; route-specific roots retain their own delivery/candidate metadata. The current-UI guard and positive/negative tests enforce both shared markers. A fresh isolated Playwright session at `http://127.0.0.1:5198/#/onboarding` reported app-root flags `Island=true`, `NatureTown=false`, build revision `development-local`; its `island-welcome` root independently reported delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and Island flag `true`. Before the change the app-root flags were absent while the Island child marker was present. Direct `/parents` navigation in this no-profile session correctly returned to onboarding, so no parent profile or learning data was created and the populated Parent page was not reviewed in this step. This identity check is not a visual-quality, screen-reader, participant, or release-build assessment.

- Island-shell runtime version (`live-23`): `development-local:bd9e244d-6b20-426d-9875-d3034f81865e`
- Island-shell runtime flags (`live-23`): `VITE_ISLAND_ENABLED=true`, `VITE_NATURE_TOWN_ENABLED=true`
- Island identifiers: delivery `mystic-island-v1`, visual `mystic-island-shore-garden-v18`, learning `mystic-island-learning-v2`
- Focused Battle runtime identity (`live-26`): app `buildRevision=development-local`, delivery `snap-root-v1`, visual lineage `pokko-field-v1`; the Battle DOM exposes no visual-candidate or visual-mode ID. Runtime flags remain `VITE_ISLAND_ENABLED=true`, `VITE_NATURE_TOWN_ENABLED=true`.
- Observed viewport: island-shell navigation captures at 390×844, 768×1024, and 1280×720 (`live-23`); focused Battle play captures/checks at 390×844, 390×667, 360×640, and 844×390 (`live-24`); the 320×568 screen-size and 768×1024 tablet-portrait gates were rechecked at `live-25`; Battle setup and play were added at 1280×720 and phone setup at 390×844 (`live-26`). The empty-state parent page and its return path were checked at 1280×720 and 390×844 on `live-27`; populated parent history at 390×844 on `live-28` and 768×1024 on `live-38`; the empty parent state at 768×1024 on `live-29`; populated weak-badge display at 390×844 and 768×1024 on `live-36`; and the latest stats charts at 390×844 and 768×1024 on `live-37`. `live-39` surveyed the flag-off non-island routes at 1280×720. Focused parent/curriculum gate error and focus checks remain at `live-22`. Nature Town and island-enabled routes remain under their owners; this is not a complete audit of every feature-flag configuration. This is a live working-tree preview, not a fixed or released build. Concurrent Nature Town working-tree changes were left untouched.

The first expanded island-shell navigation survey is `live-45`, after the older `live-23` baseline. It records 67 route-state captures (13 each at the first three viewports; 14 each in short landscape, including a scrolled play state) plus [390×844](evidence/screens/live-45-navigation/390-contact-sheet.png), [768×1024](evidence/screens/live-45-navigation/768-contact-sheet.png), [1280×720](evidence/screens/live-45-navigation/1280-contact-sheet.png), [844×390](evidence/screens/live-45-landscape-final/844-contact-sheet.png), and [1024×390](evidence/screens/live-45-landscape-final/1024-contact-sheet.png) contact sheets. Later Island runs `live-50`–`live-54` are recorded below; consult those for newer evidence rather than treating `live-45` as latest. Focused `live-24` Battle evidence is recorded below. Focused `live-22` screenshots show the [parent gate error/focus state](evidence/screens/live-22-parents-gate-error-focus-390x844.png) and [curriculum gate error/focus state](evidence/screens/live-22-curriculum-gate-error-focus-390x844.png) at 390×844. Prior `live-19` captures retain the non-empty [phone](evidence/screens/live-19-stats-chart-390x844.png) and [tablet](evidence/screens/live-19-stats-chart-768x1024.png) stats charts, [paper score dialog](evidence/screens/live-19-paper-score-390x844.png), [print preview](evidence/screens/live-19-print-preview-390x844.png), and [1280×720 parent gate](evidence/screens/live-19-parent-gate-1280x720.png). The `live-16` [short-landscape contact sheet](evidence/short-landscape-844x390-contact-sheet.png), 22 numbered captures, and earlier navigation/landscape/retry reports remain historical evidence. The older `live-06` 1280×720 overview observation is not treated as current-candidate evidence.

## Current Island navigation evidence (`live-45`, 2026-09-21)

This is the first saved route survey for the canonical Island-enabled PokoMoko target after the target-version mix-up. It used `http://127.0.0.1:5198`, `VITE_ISLAND_ENABLED=true`, route `/#/island`, and the runtime `data-island-feature-enabled=true` marker. All captures report delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, and build revision `development-local` / version `development-local:a268ed15-e3af-4d55-9d9f-37ba2d34512c`. This is a local development preview, not a release build. The isolated E2E context used sound off and reduced motion; no service worker controlled it.

| Viewport | Coverage | Result |
|---|---|---|
| 390×844 phone | Home, play, guide, inventory, customization, experience, help, house, learning settings, learning, records, photo detail, photos; 16 route/action checks | Pass; 13 captures; no page errors |
| 768×1024 tablet | Same route survey and return checks | Pass; 13 captures; no page errors |
| 1280×720 desktop | Same route survey and return checks | Pass; 13 captures; no page errors |
| 844×390 short landscape | Same route survey and return checks; wheel-scroll play content to the continuation action | Pass; 14 captures; 17 route/action checks; no page errors |
| 1024×390 short landscape | Same route survey and return checks; wheel-scroll play content to the continuation action | Pass; 14 captures; 17 route/action checks; no page errors |

Each viewport report records the complete URL, Island-enabled marker, delivery/candidate IDs, revision, reduced-motion state, and service-worker state. The browser fixture reserved and answered one learning question, moved starter furniture, and saved a photo in its isolated context; closing that context discarded the test profile. This is test data, not a change to the user's profile. The survey confirms route return, draft preservation, placement cancel/save, camera close, photo detail close, direct-learning reload/close, scroll restoration, and the records refresh after an answer.

The landscape captures show expected in-panel scrolling rather than fixed-navigation occlusion in the records view. In play mode, the low-height panel exposes continuation actions through `.island-play-body` scrolling; a direct 844×390 geometry check measured an 89px viewport for 358px of content. The initial screenshot therefore shows only the top portion until the child scrolls. A real wheel gesture exposed `ひかりを とどける` fully inside the scroll surface at both widths, and its target remains at least 44px high. The initial and scrolled states are saved as [844×390](evidence/screens/live-45-landscape-final/844-play-scrolled.png) and [1024×390](evidence/screens/live-45-landscape-final/1024-play-scrolled.png). `live-49` below adds an explicit visual/text cue. Runtime reachability and cue behavior are tested, but physical-touch discoverability and child comprehension are not established.

One initial landscape run failed because the test selected the first generic utility scroller immediately after the URL changed; it still belonged to Settings while the Curriculum route was committing. The test assigned `scrollTop` to that old surface, then compared it with the newly mounted page. It now waits for the `レベル いちらん` heading, scopes the scroll measurement to that screen, and waits for the restoration frame. Both landscape sizes now pass. No app scroll implementation change was needed; this was an E2E target-selection race, not an Island runtime failure.

Visual review of the five contact sheets found no major overlap or clipped primary navigation in the surveyed states. This visual pass does not prove silent comprehension, safety, enjoyment, or correct real-screen-reader reading order; those remain separate open gates. The [portrait/tablet/desktop report](evidence/screens/live-45-navigation/report.json) and [landscape report](evidence/screens/live-45-landscape-final/report.json) contain capture-level metadata.

## Play scroll continuation cue (`live-49`, 2026-09-21)

This focused follow-up fixes the discoverability gap observed in `live-45` without changing the play actions or scroll container. The current Island play heading now shows a downward arrow and `つづき` only while `.island-play-body` has content below; the hint disappears at the end and returns when scrolling back up. The arrow is decorative, with text providing the cue. Portrait/tablet screens whose content already fits do not show it. The behavior is specified in [`43_island_navigation_spec.md`](../../../product/43_island_navigation_spec.md).

| Viewport | Runtime check | Result |
|---|---|---|
| 844×390 | Hint visible at the top; hidden at maximum internal scroll; continuation CTA reachable and ≥44px; hint restored at top | Pass; 17 route/action checks; 14 captures |
| 1024×390 | Same hint/scroll/CTA sequence | Pass; 17 route/action checks; 14 captures |

The run passed on a freshly started local preview at `http://127.0.0.1:5198`, route `/#/island`, `VITE_ISLAND_ENABLED=true`, with runtime version `development-local:4d899a58-86ab-4483-9123-fdba304cea3d`, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, and revision `development-local`. It ran in an isolated browser context with reduced motion and no service-worker controller; the temporary test profile was discarded at context close. There were no page errors. The [initial 844×390](evidence/screens/live-49-play-scroll-cue/844-play.png), [scrolled 844×390](evidence/screens/live-49-play-scroll-cue/844-play-scrolled.png), [initial 1024×390](evidence/screens/live-49-play-scroll-cue/1024-play.png), and [capture report](evidence/screens/live-49-play-scroll-cue/report.json) retain the exact runtime evidence.

Earlier `live-46`, `live-47`, and `live-48` folders are retained as superseded copy iterations, not final acceptance evidence. Their wording/layout was revised for concise, unwrapped placement; `live-49` is the final visual/runtime record. Automated visibility and reachability checks do not establish that a child on a physical touch device notices or understands the cue; participant and assistive-technology checks remain open.

## Current Island navigation and landscape learning (`live-50`, 2026-09-21)

The updated Island learning layout was checked together with the main Island routes at `http://127.0.0.1:5198/#/island`. Every screenshot reports Island enabled, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, build revision `development-local`, and runtime version `development-local:2e369356-a8be-4057-8218-45f070935a79`. This is a local development preview, not release evidence. All runs used reduced motion and had no service-worker controller.

| Viewport | Coverage | Result |
|---|---|---|
| 390×844 phone | Island home/play/guide/house/settings/learning/records/photos and return paths | Pass; 16 route/action checks; 13 captures |
| 768×1024 tablet | Same navigation, learning draft/resume, records refresh | Pass; 16 route/action checks; 13 captures |
| 1280×720 desktop | Same navigation and return paths | Pass; 16 route/action checks; 13 captures |
| 844×390 short landscape | Same survey plus play-panel scroll to its continuation action | Pass; 17 route/action checks; 14 captures |

The combined run passed all 65 route/action checks with 53 captures and no page errors. Selected screenshots were visually reviewed across home, play, learning, and records. At 844×390 the learning screen keeps the complete four-row keypad, answer field, hint, skip, and close controls visible; the question/support area is the constrained scroll region. The current phone, tablet, and desktop screens showed no major overlap or clipped primary navigation in the reviewed states. This visual check does not establish child comprehension, enjoyment, physical-touch discoverability, or real-screen-reader output.

Each viewport used a disposable browser context. The test created a temporary profile, reserved and answered one UI question, moved starter furniture, and captured a photo to exercise return/update behavior; closing the context discarded those test records. No user profile or data was changed. The [contact sheet](evidence/screens/live-50-current-island/contact-sheet.html) and [capture report](evidence/screens/live-50-current-island/report.json) retain the full route metadata and screenshots, including the [844×390 learning state](evidence/screens/live-50-current-island/844-learning.png) and [scrolled play state](evidence/screens/live-50-current-island/844-play-scrolled.png).

## Current Island navigation retest and stale-entry guard (`live-54`, 2026-09-21)

After reconciling the operational docs, `npm run check:current-ui-entry` passed and `tools/current-ui-entry-guard.test.mjs` passed all 16 tests. The guard now reads the runtime configuration and the current-entry context in product specs 01/06/15, project memory, ownership map, Explore implementation plan, and risk register. Negative cases cover the former `/explore` launch claims, stale current-status heading, and noncanonical Island/classic ports; positive cases require the current Island route to remain explicit.

`node tools/e2e-island-navigation.mjs` ran against `http://127.0.0.1:5198` at 390×844, 768×1024, 1280×720, 844×390, and 1024×390. All five scenarios passed (19 checks at each of the first three sizes; 20 at each short-landscape size), for 97 route/action checks and 72 screenshots with no page errors. Every capture identifies Island enabled, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2` where applicable, and build revision `development-local` / runtime version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`. Runs used reduced motion and no service-worker controller. Temporary profile, answer, furniture, and photo actions were confined to disposable browser contexts and discarded when they closed.

The [contact sheet](evidence/screens/live-54-current-island/contact-sheet.html) and [JSON report](evidence/screens/live-54-current-island/report.json) retain all screen captures and route/runtime metadata. This is a local preview recheck, not a release-build result; it does not claim physical-touch, participant, or real-screen-reader evaluation.

The follow-up `npm run verify:core` passed docs check, current-entry guard, lint, typecheck, all 456 Vitest files / 4,147 tests, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Lint retained the existing `IslandMilestone.tsx` Fast Refresh warning; build retained stale Browserslist data, the empty `pdf` chunk, and the >500 kB chunk advisory. Docs check reported only the unrelated overdue Review By on `docs/tasks/active/2026-09-13-mysterious-island-v3.md`. `agent:index:check` and `git diff --check` also passed. This does not close the task's participant, real-screen-reader, or unreviewed-route gaps.

## First-run Island welcome and short landscape (`live-51`, 2026-09-21)

The runtime was started through the standard `npm run dev` launcher at `http://127.0.0.1:5198`; this sets `VITE_ISLAND_ENABLED=true` and opens the canonical Island route. The first-run captures use `/#/onboarding` before seeding any profile. Their root marker reports Island enabled, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, onboarding candidate `island-touch-first-v1`, learning candidate `not-applicable`, build revision `development-local`, and runtime version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`. Nature Town remains a separate flag/route and was not enabled or opened.

### F-33 — First-run Welcome was trapped in the 430px frame and hid its CTA in short landscape (medium, fixed)

At 1280×720, the canonical Island home used the 1180px app frame but `/onboarding` stayed at the generic 430px maximum, leaving most of the desktop unused. The route renders outside `Layout`, so its first-run sizing had no Island-specific frame rule. The welcome root also lacked `data-island-feature-enabled`, preventing the fail-closed runtime metadata helper from identifying it as current Island evidence. At 844×390, the tall scene and camera row pushed the primary “まなぶ” action below the initial viewport.

The first-run Island flow now opts into the shared 1180px app maximum, with the welcome scene capped at 920px on wide screens. At heights ≤430px, the scene and surrounding controls compact; the welcome scene caps at 680px, and the 44px camera/action targets are preserved while “おはな”, “あかり”, and “まなぶ” stay inside the first viewport. The explicit `/onboarding?mode=add` utility frame remains at its previous 430px maximum. `IslandWelcome` now exposes the live Island flag marker, and the E2E captures it before the disposable profile fixture is created.

| Viewport | Runtime result |
|---|---|
| 390×844 phone | Full-width Island app frame; all three welcome actions visible and ≥44px |
| 768×1024 tablet | Full-width Island app frame; scene and action surfaces centered, all welcome actions visible and ≥44px |
| 1280×720 desktop | App frame 1180px, scene 920px, actions centered; all welcome actions visible and ≥44px |
| 844×390 short landscape | Compact 145px scene viewport and 48px camera-control row; welcome actions visible without scrolling and ≥44px |

The standard-launcher rerun passed all four viewport scenarios, captured 57 screenshots, and reported no page errors. Each run also verified that profile-add keeps its existing frame, then exercised the existing Island navigation/learning/return checks in a disposable browser context with reduced motion and no service-worker controller. Those temporary QA profile writes were discarded when contexts closed. The [contact sheet](evidence/screens/live-51-first-run-layout/contact-sheet.html) and [capture report](evidence/screens/live-51-first-run-layout/report.json) retain the per-screen flags, candidates, revision, and screenshots, including the [1280×720 welcome](evidence/screens/live-51-first-run-layout/1280-welcome.png) and [844×390 welcome](evidence/screens/live-51-first-run-layout/844-welcome.png). This is local preview evidence, not release, physical-device, participant, or real-screen-reader evaluation.

## Shared utility text contrast (`live-52`, 2026-09-21)

### F-34 — Small supporting text was too faint on shared Utility surfaces (medium, fixed and runtime checked)

The inactive subject option in Settings and secondary descriptions in shared panels used pale Tailwind Slate classes. The current Island runtime measured `text-slate-400` at only **2.59:1** against the off-white paper surface, below the normal-text target. The design checklist only asked whether contrast was “acceptable,” and the design-system brief still named the outdated `--color-gray-500` (`#8395a7`) as secondary text; neither specified a numerical minimum or how to account for translucent panels.

Added the shared `text-pokomoko-muted` utility, backed by the existing `--pokomoko-muted` (`#606980`) token, and applied it to supporting text and inactive choices in `SurfacePanel`, `EmptyState`, Settings, Stats, and Parents. Updated the UI guideline, review checklist, and derived Master. The navigation E2E now reads the actual computed foreground and each CSS background layer, composites them, checks the 4.5:1 floor against that surface and opaque paper, and retains `text-slate-400` as an explicit failing sentinel. No Explore or Nature Town visual was changed.

The canonical Island target was `http://127.0.0.1:5198` (`/#/settings?section=learning`), Island flag true, Nature Town flag unset, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, build revision `development-local`, runtime version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, service-worker controller false, reduced motion true. All four viewport runs passed (390×844, 768×1024, 1280×720, 844×390), with 57 captures and no page errors. Computed text `rgb(96, 105, 128)` measured **5.44:1** on the Settings control's composed background, **5.40:1** against opaque paper, and **5.11:1** against the canvas; the old Slate-400 sentinel remained at **2.59:1** on paper.

The [390×844 Settings capture](evidence/screens/live-52-contrast/390-settings-detail.png), [390×844 records capture](evidence/screens/live-52-contrast/390-records.png), [contact sheet](evidence/screens/live-52-contrast/contact-sheet.html), and [full capture report](evidence/screens/live-52-contrast/report.json) preserve the run. Browser contexts were disposable; the E2E's QA answer/photo/furniture writes were discarded at close. This is local preview evidence, not release, physical-touch, participant, or real-screen-reader evaluation.

## Island-shell navigation evidence (`live-23`)

| Viewport | Captured paths and checks | Result |
|---|---|---|
| 390×844 phone | Island home, play/guide, inventory/customization/experience/help, house, settings detail, learning, records, photos/detail; 16 navigation and return checks. Focused parent/curriculum gate error and focus checks are retained from isolated `live-22` contexts. | Pass; no page errors; 13 screenshots |
| 768×1024 tablet | Same island-shell journey; 16 navigation and return checks | Pass; no page errors; 13 screenshots |
| 844×390 short landscape | Island home, records, settings, Battle setup top and scrolled grade choices; retained `live-16` evidence | No horizontal overflow; measured visible controls are 44px or larger; Battle setup uses two columns and its scroll pane grows from 49px to 130px |
| 1280×720 | Same island-shell journey, including wide world and utility layouts; 16 navigation and return checks | Pass; no page errors; 13 screenshots; non-shell routes remain outside this pass |

The navigation E2E used a fresh isolated browser context with a sound-off fixture and `prefers-reduced-motion: reduce` at all three viewport sizes. It verified empty records, successful answer-to-record update, safe close/resume, back/forward, return focus, placement cancel/save, photo creation/detail/close, and settings scroll restoration. A separate throwaway profile verified wrong-answer feedback and same-question retry (cursor remains unchanged while the plan revision advances); its state was discarded when the context closed. Screens identify delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and learning candidate `mystic-island-learning-v2`. The local build revision is `development-local`; there is no production build/release claim.

The 1280×720 records screenshot shows the next-section `レベル進捗` label entering at the lower edge of the scroll viewport. A fresh isolated DOM measurement confirmed this is normal scroll clipping, not content under the fixed navigation: the scroll area is 586px high for 1,178px of content, has 592px of scroll range and 24px bottom padding, and the final action remains well above the viewport end at maximum scroll. No corrective UI change was warranted from this observation.

## Battle responsive evidence (`live-24`; gate text polish `live-25`)

The focused route is `/battle/play?mode=boss_coop`, opened through the actual two-player setup. Each run used a fresh isolated browser context, a throwaway sound-off profile, and `prefers-reduced-motion: reduce`; the context was closed after checking. Captures are in [live-24-battle](evidence/screens/live-24-battle/), with numeric keypad, choice buttons, and viewport measurements in its report.

| Viewport | Runtime state and checks | Result |
|---|---|---|
| 390×844 phone portrait | Two-player numeric questions, both keypads, enter-preview, clear, cancel back to setup | Pass; each keypad target ≥44px, all keys remain within its panel/screen; no horizontal overflow or page errors |
| 390×667 short phone portrait | Two stacked player panels and numeric questions | Pass; 44px keys remain visible. A dense question diagram uses a 49px scroll viewport and can be scrolled within its own frame |
| 390×844 phone portrait, vocabulary | Two 2×2 answer-choice grids | Pass; all choice buttons ≥53px in both panels |
| 844×390 short landscape | Two side-by-side panels and numeric questions | Pass; key targets ≥44px and stay within each panel/screen |
| 360×640 short phone portrait | Two stacked panels and numeric questions | Pass; 44px targets and both question frames stay reachable, with internal diagram scroll where needed |
| 320×568 small phone | Screen-size gate | Pass; explains the larger-screen requirement and returns to Other Games; this avoids an 8px question viewport that appeared when trying to fit both full-size keypads |
| 768×1024 tablet portrait | Orientation gate | Pass; shows a tablet rotation instruction and an in-app return, instead of blocking phone layouts |

Both local game modes were smoke-checked at phone portrait and short landscape. The mode still uses the existing synchronous two-player local rules; no learning answers or persistent progress were written. This evidence is a development-preview check, not a release build or participant evaluation.

## Route and return observations

| # | Entry → destination → return | Earlier local-preview observation (`live-06`; latest-candidate coverage is summarized above) |
|---|---|---|
| 01 | Settings → parent gate → parent page → settings | The `live-27` parent page opened after its gate, showed empty history, and now returns to the originating “テスト・保護者” section. The arithmetic gate prompt was the only answer entered; no learning exercise was answered. |
| 02 | Empty records → learn → close | The records empty state offered a clear `まなぶ` action. Opening learning and closing returned to `/stats`; no answer was entered. The inline desktop view keeps the primary action ahead of the calendar and progress cards. |
| 03 | Island tab → house tab → album → house | Routes changed to `/island`, `/island?view=keepsakes`, `/island?view=album`, then back to the house route. On `live-06`, return focus lands on `アルバム`; the next Tab reaches `しゃしん`. |
| 04 | House menu → Escape | The dialog received focus on open; Escape closed it and restored focus to the menu trigger. |
| 05 | Settings overview → Learning detail → back | The settings detail retained the selected category; returning restored focus to the `もどる` control. |
| 06 | Settings → Display & sound / Learning | Initially the sound and English read-aloud controls exposed only `ON`/`OFF`; segmented options exposed no selected state. After the change, the `live-06` accessibility tree exposed named pressed/checked states for the controls. |
| 07 | Settings → parent gate → cancel/Escape | Before the shared fix, the gate focused its answer field, but Tab after `やめる` escaped the dialog and closing left focus on the page root. After the fix, `live-06` and focused `live-19` checks confirm first-field focus, Shift+Tab to `やめる`, Tab wrap to the field, and Escape return to the `開く` trigger. The latest check was at 1280×720; no answer was entered. |
| 08 | Paper-test score dialog | A throwaway pending test was created only inside a fresh browser context. On `live-19`, the dialog exposes its name/description and labelled score group, keyboard focus is visible, 18/20 can be saved, and Escape restores focus to the opener. The temporary record added only a paper score; learning logs and memory stayed at 0. Closing the context discarded the profile. |
| 09 | Settings → profile actions → cancel rename | The profile name was a clickable `div`; pencil/trash icon buttons had no accessible names and were 40px. The name row is now a keyboard-operable button, both icon actions identify their target profile and purpose, and their source targets are 44px. The `live-06` preview accessibility tree confirmed the action names. Opening rename and pressing Escape returned focus to the same edit button; no profile was changed or deleted. |

## Finding and change

### F-01 — Focus was lost after returning from the house album (medium, fixed locally)

Reproduction: from the house, open `アルバム`, then activate `アルバムから もどる`. The route returned correctly, but the focused element became the page root; the next Tab entered the 3D floor surface instead of continuing at the album entry. This makes keyboard navigation disorienting even though the route itself is correct.

`src/pages/Island.tsx` now restores focus to the house album entry when returning from that route, with the house-menu trigger as a fallback. The same local browser path now ends with focus on `アルバム`, and one Tab advances to `しゃしん`. An assertion for this return path is included in `tools/e2e-island-navigation.mjs`; the `live-16` and `live-19` navigation suite passed.

### F-02 — Settings and shared segmented choices hid their accessible state (medium, fixed locally)

The live settings tree exposed the sound and English-read-aloud controls as anonymous `ON`/`OFF` buttons. Display-text, Japanese-mode, and subject choices had button names but no exposed selected state. The shared `SegmentedControl` now provides a named group and `aria-pressed` on each option; all six call sites name their group. Settings toggles now expose the setting name and pressed value while keeping their visible labels unchanged. A unit regression test covers the shared control. The `live-06` preview exposed named toggle buttons with pressed states in the accessibility tree; this is technical semantics evidence, not a full assistive-technology evaluation.

### F-03 — Shared dialogs let keyboard focus escape and did not restore the opener (medium, fixed locally)

On the actual settings parent-gate dialog, focus began in the answer field. Tab moved to `やめる`, then the next Tab escaped the dialog (the accessibility tree no longer reported a focused control). Closing with Escape left focus on the page root rather than the `開く` trigger. `Modal` had no shared focus boundary or return-focus handling; child `autoFocus` fields also could take focus before the modal recorded its opener.

The shared modal now focuses its first usable control by default, traps forward/reverse Tab (and redirects programmatic focus back inside), and restores focus to its opener when closed. A confirmation with a single consequential action can instead start on the dialog container; the paper-test cancel confirmation uses this to avoid making Enter an immediate destructive action. Modal field call sites defer initial focus to the shared component so the opener is captured first. The `live-06` in-app preview confirmed parent-gate first-field focus, wrap in both directions, and Escape return to `開く`. Pure regression tests cover both focus-boundary and safe initial-target selection; the visual/automated E2E suite remains outstanding.

### F-04 — Paper-test score dialog was unnamed and bypassed common focus behavior (medium, fixed and live-checked)

The score-entry surface set `role="dialog"` and `aria-modal="true"` but had no `aria-labelledby` or `aria-describedby`; it also used a separate overlay implementation rather than the shared focus boundary. Its title and instructions are now programmatically associated, and its animated open/exit lifecycle uses the same focus hook, including scroll lock, Escape, Tab boundaries, and opener restoration. The 0–20 score choices now form a labelled group, retain 44px minimum targets, and show a visible keyboard focus ring. Static regression tests cover semantics and focus styles. On `live-19`, a fresh isolated context created a temporary 20-question paper record, exercised keyboard entry and focus restoration, then saved 18/20. Only the disposable profile's paper score changed; no learning logs or memory were written, and closing the context discarded it.

### F-05 — Profile actions were unnamed, undersized, and partly pointer-only (medium, fixed locally)

On the `live-06` Settings profile screen, the name/grade block was clickable but rendered as a `div`, while the pencil and trash buttons were exposed only as emoji. They were 40px square, below the project's 44px interaction-size rule. The name block is now a semantic button with visible keyboard focus and a 44px minimum height; the icon buttons announce both the action and profile name and use 44px squares. The `live-06` accessibility tree exposed the target-specific names, and opening/canceling rename returned focus to the same opener. No profile value was saved and the delete path was not activated.

### F-06 — Shared compact buttons fell below the touch-target minimum (medium, fixed locally)

The shared `Button` primitive's `size="sm"` rendered at 40px high (and compact icon actions at 40px square), below the 44px minimum in the UI/layout specs. The shared primitive now enforces a 44px minimum for compact text buttons and 44×44px for compact icon buttons; two regression tests assert these classes. This is source/test evidence only. It has not yet been measured across phone, tablet, or short-landscape viewports.

### F-07 — Explore encounter back control is source-sized below 44px (medium, handed to existing owner)

`ExploreHud` applies `h-10 w-10` to its back button in the `encounter` variant, overriding the default 44px size. This conflicts with the shared touch-target contract. Exploration UI already belongs to the active Learning rhythm / island experience task, so the finding was routed there rather than duplicating its implementation here. This is a source-level observation only; the rendered size and surrounding compact HUD have not been checked at target viewports.

### F-08 — Settings choice controls were undersized and timer selection was not announced (medium, fixed locally)

The shared `SegmentedControl` buttons used 36px of line-height plus padding and had no 44px minimum. The periodic-test time choices were similarly compact and exposed neither a named group nor their selected state. Shared segmented options and timer choices now have a 44px minimum height; the timer group has an accessible name and each choice exposes `aria-pressed`. The shared control regression test asserts the 44px minimum and selected states. Settings timer markup has source/core evidence only; no screen-reader or viewport measurement has been performed.

### F-09 — Pet naming dialog lacked shared keyboard and semantic behavior (medium, fixed locally; live flow not checked)

The Home `NameModal` was visually modal but had no dialog name/description, input label, focus containment, or form submission on Enter. It now uses the shared focus hook, exposes a named dialog and labeled input, and wraps the existing normalization/submission in a form so Enter and the visible button use the same path. The 8-character limit, trimming, and blank-name fallback are unchanged. A static semantics test covers the dialog/form structure; live focus and keyboard behavior have not been checked on `live-09`.

### F-10 — Several owner-controlled learning/onboarding/pet actions appear below 44px (medium, handed to existing owner)

Source scan found compact `StudyLayout` controls using `py-1` or `p-2` (written-arithmetic toggle, English read-aloud, exit, skip, desktop toggle, and TTS), the onboarding return button using `py-2`, and pet name/speech actions using `py-1`; the pet-album close button is explicitly 36×36px. `.app-pill` itself supplies no minimum size. These are source-level estimates, not rendered measurements. They were routed to the active Learning rhythm / island experience task, which owns these child flows; each control still needs actual phone/tablet measurement and layout review.

### F-11 — Print preview's initial keyboard focus was invisible (medium, fixed and live-checked)

The preview moves focus to its `tabIndex=-1` heading when opened, but `.printable-test-toolbar h1:focus { outline: none; }` removed the only focus indicator. Keyboard users could lose their place before tabbing to the toolbar controls. The rule now shows a 2px `--app-accent-strong` outline with a 4px offset only for `:focus-visible`. On `live-19` at 390×844, opening via keyboard visibly focused the heading, changing print scope worked, and Escape returned focus to the same trigger element (whose label changes after a pending test is created). No OS print/PDF action was launched. Screen-reader output remains unverified.

### F-12 — Stats charts conflated missing data with 0% and had no accessible descriptions (medium, fixed and rendered phone/tablet checks complete)

The weekly accuracy series used `0` on days without answers, drawing an apparent 0% result instead of an unmeasured day. The skill radar tooltip also presented unpracticed categories as `0%`, and neither chart supplied an accessible title or description. The Stats SSOT now says unanswered days are unmeasured and the accuracy line must have a gap. The weekly chart maps only unanswered accuracy values to `null` (answer-count mode retains a real 0), explicitly leaves gaps, and distinguishes no-answer tooltips from recorded 0% results. Both charts now expose Japanese SVG titles/descriptions; the radar tooltip and description distinguish no-practice categories from recorded 0%.

The installed Recharts Radar renderer places `null` values at the radial baseline, so a caption alone could not distinguish missing practice geometrically. The custom radar shape now omits unpracticed points and splits the outline into open segments around each missing category; recorded 0% remains a plotted point, while complete data keeps the closed, filled radar. The caption remains as a nearby whole-word chip list, alongside category-specific tooltip and screen-reader descriptions. `live-19` screenshots and DOM checks with throwaway saved-data-shaped records at 390×844 and 768×1024 confirm readable chips, 12px text, and no horizontal overflow. New geometry tests cover first/last gaps, multiple separated runs, measured zero, and all-missing data.

`live-37` used a fresh disposable Grade 1 profile, then directly seeded three math memory rows with consistent counters and four matching attempt-log rows: `かず` 100%, `ならび` 50%, and `くらべる` recorded 0%; the other three categories had no practice. This fixture checks rendering and accessibility descriptions, not natural learning acquisition. At 390×844 and 768×1024, the radar path remained open and crossed only the three measured points; no fill or marker was added on unpracticed axes, and the adjacent whole-word list names all three missing categories. Hover content distinguished `ひく: まだ練習記録がないよ（0/1スキル）` from `くらべる: 0%（記録あり 1/8スキル）`. The SVG title/description expose each category's recorded or unpracticed state. In accuracy mode the 7-day line contains only the single Sunday 50% point; the other six days are not drawn at 0%, and the SVG description reports them as unmeasured. Document/body widths matched each viewport, with no browser console errors/warnings and no service-worker controller. The named browser session was closed and reopened; `/stats` returned to onboarding, confirming the QA profile was discarded.

Saved, visually inspected screenshots: [phone radar](evidence/screens/live-37-stats/stats-radar-390x844.png), [phone unpracticed tooltip](evidence/screens/live-37-stats/stats-radar-unpracticed-tooltip-390x844.png), [tablet radar](evidence/screens/live-37-stats/stats-radar-768x1024.png), [tablet recorded-zero tooltip](evidence/screens/live-37-stats/stats-radar-recorded-zero-tooltip-768x1024.png), and [tablet weekly accuracy](evidence/screens/live-37-stats/stats-weekly-accuracy-768x1024.png). Runtime identity is `buildRevision=development-local`, flags `VITE_ISLAND_ENABLED=false` / `VITE_NATURE_TOWN_ENABLED=false`, delivery `snap-root-v1`, visual lineage `pokko-field-v1`; the legacy screen has no distinct visual-candidate ID. This is not release-build, real screen-reader, or participant evidence.

### F-13 — Two-player setup options were too small and lacked player-specific selection semantics (medium, fixed in source)

In the battle setup, avatar buttons were `h-10` (40px), and grade buttons had only `text-[11px]` plus `py-1.5` (about 25px before borders), below the 44px target in UI guideline 07. Grade options did not expose their selected state; avatar options had no player-specific accessible names or pressed state; and the name inputs relied on placeholders. Subject groups had an accessible name, but both players shared the same generic name. The setup now gives both players labeled name fields and named icon/grade/subject groups, adds `aria-pressed` to avatar and grade options, provides visible focus rings, and raises avatar/grade targets to at least 44px. Selected options also receive a 2px player-accent outline, so selection is not conveyed by color alone. No game rules or saved data changed. Static markup tests cover labels, groups, pressed attributes, focus classes, and target-size classes; runtime at phone/tablet/landscape now has evidence in the `live-16` report.

### F-14 — Battle setup left only a 49px scroll viewport in short landscape (medium, improved in source)

At 844×390, the Battle setup had two long player forms in a single column, while the main form scroller was only 49px high (1,364px of content). The fixed mode header and start action remained visible, but most controls were below a very narrow scroll strip. Tablet-landscape layout now places the players in two columns, tightens non-interactive padding/gaps without reducing 44px targets, and contains scroll chaining. At the same viewport the scroll area measures 130px for 628px of content; the top and bottom captures show both players and reachable grade choices. There is no horizontal overflow. A vertical scroll is still required to reach every field; short-landscape density should remain a watch item.

### F-15 — Tailwind v4 did not load the project's custom responsive variants (high, fixed in source)

`src/index.css` imported Tailwind CSS but never registered the project's custom `mobile:`, `land:`, and `ipadland:` variants; Tailwind v4 also no longer auto-detects JavaScript config files. The classes were present in JSX but absent from generated CSS, leaving intended responsive layouts in study, stats, onboarding, and utility screens inactive. The entry CSS now defines these variants using Tailwind v4's `@custom-variant` directive, preserving the existing media-query contracts without loading legacy theme extensions or changing global color/radius tokens. Representative layouts were rechecked at all three target viewports and the 390×844/768×1024 navigation E2E passed. Tailwind documents [custom variants](https://tailwindcss.com/docs/adding-custom-styles). Candidate-wide core verification is recorded below.

### F-16 — Unpracticed chart labels broke across words at narrow widths (low, fixed and live-checked)

The first live non-empty Stats capture showed a category name split across lines under the radar, making the legend less comfortable to scan. The caption now uses compact no-wrap `Badge` chips and list semantics with a visible heading. `live-19` at 390×844 and 768×1024 confirmed all five unpracticed categories remain whole, fit without horizontal overflow, and use 12px text. Evidence: [phone](evidence/screens/live-19-stats-chart-390x844.png) and [tablet](evidence/screens/live-19-stats-chart-768x1024.png).

### F-17 — Score choices lacked an explicit group name and visible focus ring (medium, fixed and live-checked)

The score buttons were individually operable but not exposed as one group labelled by the modal's instructions, and their focus style was not explicit. The score grid now has `role="group"` linked to the description, a visible focus ring, and maintains a 44px minimum. The isolated `live-19` 18/20 entry confirmed the labelled group, keyboard focus styling, submission, and return focus; no learning data was added.

### F-18 — Arithmetic-gate answer had no explicit label or prompt association (medium, fixed and live-checked)

The shared parent/curriculum gate input relied on placeholder text and was not programmatically associated with the visible arithmetic question. A shared answer-field component now provides a screen-reader label, links the equation through `aria-describedby`, and exposes the invalid state and retry status accessibly. Two focused semantic regression tests cover the prompt/label relationship and error/status semantics. The screen spec now defines this interaction contract.

### F-19 — Wrong gate answers silently changed the challenge and dropped focus (medium, fixed and live-checked)

`ParentGuard` refreshed the equation on an incorrect answer without explaining what happened; submitting with either Enter or the pointer also moved focus to the document body. The refreshed challenge now has a neutral retry message, and validation returns focus to the answer field. A visible keyboard focus ring remains present in the error state. On `live-22`, isolated contexts confirmed the labelled equation/error association, `aria-invalid`, announced retry status, and visible focus at 390×844 on both `/parents` and `/settings/curriculum`; keyboard and pointer submissions restored focus. A 1280×720 focused check confirmed the same gate behavior. The curriculum remained at its initial level after a failed attempt. Screenshots: [parent gate](evidence/screens/live-22-parents-gate-error-focus-390x844.png), [curriculum gate](evidence/screens/live-22-curriculum-gate-error-focus-390x844.png).

### F-20 — Phone play was orientation-blocked and the two keypads could not fit side by side (high, improved and live-checked)

The battle screen spec already called for vertical player panels on phone portrait, but `OrientationGate` required a 768px-wide landscape viewport and displayed iPad-only instructions. When the gate was bypassed for diagnosis at 390px, the two player cards stayed side by side and their key targets measured about 36px. Simply stacking those cards did not solve the problem: each problem visual consumed the remaining height and hid lower keypad rows.

Phones with at least 640px viewport height now reach setup and play in either orientation; tablets at 768px or wider still require landscape. Smaller screens and tablet portrait show distinct guidance and a 44px in-app return action instead of opening a layout that cannot keep two keypads and the question readable. Phone portrait stacks player panels, while landscape retains side-by-side play. Compact battle-specific question visuals preserve their underlying items/values, and the question region scrolls internally when the short screen cannot show the full illustration at once. Player answer previews stay visible in the header, and the number keypad remains four rows of at least 44px; choice buttons, skip, cancel, and gate return also retain 44px minimum touch targets. The screen spec was clarified in `09_battle_spec.md` §3.2. Boss coop and tug-of-war mechanics were not changed.

On `live-24`, 390×844 measurements showed both numeric keypads fully inside their panels, 44px minimum targets, working `7` input/clear, and `やめる` returning to setup. At 390×667 and 360×640, the keys remained visible while question diagrams stayed reachable within their dedicated scroll frames. Vocabulary choice grids measured at least 53px; at 844×390 both panels remained usable side by side. The 320×568 diagnostic showed that continuing play left only 8px for the question, so the route now shows the larger-screen guidance and in-app return; tablet portrait shows a separate rotation instruction and return. At `live-25`, both gate cards were rechecked: the 197×44px return button remains a single text line at phone and tablet widths, and the phone return reaches `/battle`. Evidence: [390×844 setup](evidence/screens/live-24-battle/390x844-setup-responsive.png), [390×844 numeric](evidence/screens/live-24-battle/390x844-playing-responsive.png), [390×844 choices](evidence/screens/live-24-battle/390x844-vocabulary-responsive.png), [390×667](evidence/screens/live-24-battle/390x667-playing-responsive.png), [844×390](evidence/screens/live-24-battle/844x390-playing-responsive.png), [320×568 gate](evidence/screens/live-24-battle/320x568-screen-size-gate-nowrap.png), and [768×1024 rotation gate](evidence/screens/live-24-battle/768x1024-tablet-portrait-card-nowrap.png). One generated question per player/viewport is not an exhaustive visual-type or real-device evaluation.

## Battle app-frame and short-wide-height evidence (`live-26`)

| Viewport / state | Check | Result |
|---|---|---|
| 1280×720 setup | The app frame is centered at x=50 with width 1180. The old `w-screen` Battle child expanded to 1280px from that offset, so its player-2 controls were clipped at the app-frame edge. | Replaced the nested viewport sizing with `w-full h-full`; both player panels now fit inside x=50…1230 and the document remains 1280px wide. [Setup screenshot](evidence/screens/live-26-battle/live-26-battle-setup-1280x720.png) |
| 1280×720 play, numeric | A tall subtraction/counting visual previously pushed the fourth keypad row below the clipped player panel. The new wide-short-height rule caps the problem frame at 176px and lets the full diagram scroll in place. | All 26 keypad targets measured 126×45px and remained inside their player panels and the viewport. Both question frames measured 174px high; the longer one scrolled 75px to expose its final prompt while keys stayed fixed. [Play](evidence/screens/live-26-battle/live-26-battle-play-1280x720.png), [scrolled question](evidence/screens/live-26-battle/live-26-battle-play-question-scroll-1280x720.png) |
| 1280×720 play, three-choice | Both players used early-math picture questions with three answers. | All six choice targets measured 257×144px inside their panels; no document overflow. [Choice screenshot](evidence/screens/live-26-battle/live-26-battle-play-choice-1280x720.png) |
| 390×844 setup | The selected-mode badge duplicated information and squeezed the mode description; the long selected option wrapped into multiple lines. | The redundant badge is hidden on phone width and mode labels use a single compact line. The app frame and document both remain 390px wide. [Setup screenshot](evidence/screens/live-26-battle/live-26-battle-setup-390x844.png) |

The same isolated browser journey reached setup → start/countdown → play → timeout result → replay/setup without entering a learning answer. A temporary profile existed only in the disposable browser context. This is local preview evidence, not a release build, screen-reader session, child-participant study, or exhaustive sample of question visuals.

## Parent page route and scroll evidence (`live-27`)

The preview used an isolated browser context and a disposable profile created through the visible onboarding flow. The parent gate was completed using its arithmetic prompt; no learning exercise was answered, and the context data was removed afterward. Runtime identity remained `development-local:bd9e244d-6b20-426d-9875-d3034f81865e`, delivery `mystic-island-v1`, visual `mystic-island-shore-garden-v18`, learning `mystic-island-learning-v2`. This is a working-tree preview, not a release build.

| Viewport / state | Check | Result |
|---|---|---|
| 1280×720, top of `/parents` | Empty learning history and no weak points; document width 1280px. Scroll area is 586px high for 815px of content (229px scroll range). | No horizontal overflow. The `学習履歴` heading begins at the lower viewport edge and is naturally clipped by the scroll area; after scrolling, its full heading/content are visible. [Top screenshot](evidence/screens/live-27-parents/parents-top-1280x720.png) |
| 1280×720, bottom of `/parents` | Scrolled the parent content to its 229px maximum; the final empty-history panel and `設定に戻る` are visible. | Return action ends at y=598px, 56px above the fixed navigation at y=654px. It is reachable without being covered. [Bottom screenshot](evidence/screens/live-27-parents/parents-bottom-1280x720.png) |
| 390×844, `/parents` | Document width remains 390px. The scroll area is 710px high for 815px of content (105px range); the fixed navigation stays visible at y=778px. | No horizontal overflow. All three empty-state sections fit before the navigation; the return action appears after a short 105px scroll, at y=678–722px with 56px clearance above navigation. [Top screenshot](evidence/screens/live-27-parents/parents-top-390x844.png), [scrolled bottom](evidence/screens/live-27-parents/parents-bottom-390x844.png) |
| Settings → parent gate → `/parents` → return | The return button originally sent the user to bare `/settings`, losing the selected “テスト・保護者” section. It now uses `islandParentUrl`; the gate cancel uses the same target. | Return reaches `/settings?section=parent` with that section expanded; cancel stays on the same section and restores focus to its `開く` trigger. `navigation.test.ts` covers both `/parents` and `/dev` fallback targets. |

The ordinary lower-edge clipping is consistent with the existing records-page scroll pattern and does not hide an action. The global Settings tab continues to reset to the settings overview, as specified for tab navigation; the explicit parent return button and direct-route gate cancel preserve the “テスト・保護者” section. `live-27` covered the empty parent state at desktop and phone sizes; `live-28` added one populated phone history; `live-29` checked the empty tablet layout. Populated weak-point cases and history on tablet, actual assistive-technology announcements, and participant evaluation remain open.

## Parent history labels evidence (`live-28`)

A fresh isolated browser context used a disposable Grade 1 profile and three attempts (two correct, one skipped) to exercise the populated history. The profile and records existed only in this temporary context, which was removed after the check. Runtime identifiers remained the same as `live-27`; this is still a local development preview, not a release build.

| Viewport / state | Check | Result |
|---|---|---|
| 390×844, populated `/parents` | Three recent attempts; content scroller is 710px high for 1,004px of content (294px scroll range). | Math skills render as `ゆびたしざん`, `おなじにわける`, and `2つおおい` instead of internal IDs. No page errors or horizontal overflow. [Top screenshot](evidence/screens/live-28-parents/parents-history-top-390x844.png), [scrolled bottom](evidence/screens/live-28-parents/parents-history-bottom-390x844.png) |
| 390×844, scrolled bottom | Return button after the longer populated history; fixed navigation starts at y=778px. | The return action is at y=678–722px, 56px clear of navigation and reachable at maximum scroll. |

The vocabulary label path uses the displayed word surface when available; unknown IDs retain their existing fallback instead of being silently rewritten. The check confirms readable math labels in the tested history state only; populated weak-point cases, tablet layout, screen-reader announcements, and caregiver/child comprehension remain unverified.

## Parent empty-state tablet evidence (`live-29`)

The app source is unchanged from `live-28`. In a fresh isolated browser context, a disposable Grade 1 profile was used only to pass the parent gate; no learning answer or record was created. The context/data was removed after the check. Runtime version and feature flags are the same values listed above.

| Viewport / state | Check | Result |
|---|---|---|
| 768×1024, empty `/parents` | Document is 768×1024; content scroller is 890px high with 890px content (zero scroll range). | No horizontal overflow. The 180×44px `設定に戻る` button sits at x=294, y=783; fixed navigation begins at y=958, leaving 132px of clearance. The empty-state cards and all sections fit without scrolling. [Screenshot](evidence/screens/live-29-parents/parents-empty-768x1024.png) |

No page errors occurred; two Three.js deprecation warnings were emitted while the onboarding world initialized, outside the parent page. This confirms only the empty tablet state, not populated history or weak-point cards at tablet width.

### F-21 — Battle ignored the centered desktop app frame and clipped its right side (high, fixed locally)

At 1280×720, `.app-container` is centered at x=50 and capped at 1180px. The nested Battle root used viewport-wide `w-screen`, so player-2 controls extended past the app frame and were cut off by its `overflow:hidden`. The Battle root now uses `w-full h-full`, matching its parent on both desktop and phone widths. Runtime geometry and the 1280 screenshot confirm the full player panels stay inside the 1180px app frame with no document overflow.

### F-22 — A long Battle question visual pushed the bottom keypad row out of a 720px-high panel (high, fixed locally)

At 1280×720, the question frame could consume 240px while the panel also needed a 4-row 44px keypad; the zero and submit keys fell below the clipped player card. For screens at least 768px wide and at most 760px high, Battle now tightens vertical spacing and gives the question frame a 176px independently scrollable viewport. All question content remains present, while the tested 26 numeric targets stay 45px high inside their panels. Mouse-wheel scrolling revealed the full prompt without moving the keypads; a second run confirmed both 3-choice panels fit with 144px-high options. The exact breakpoint is recorded in `09_battle_spec.md` §3.2. Other visual types and physical-device feel remain to be checked.

### F-23 — The phone Battle mode picker wrapped its selected label and orphaned the description ending (low, polished locally)

At 390×844, the duplicated mode badge narrowed the description enough to leave its last character alone; the selected boss-mode option also wrapped to several lines. The phone layout now omits the redundant badge and keeps both mode labels on one compact line. This changes presentation only; the selected mode remains exposed by the existing pressed state.

### F-24 — The parent-page return discarded the selected settings section (medium, fixed locally)

From Settings → “テスト・保護者”, the successful parent-gate path reached `/parents`, but its “設定に戻る” action navigated to bare `/settings`, which reopened the section picker instead of the section the user came from. The direct-route gate cancel had the same bare-settings fallback. Both exits now use the shared `islandParentUrl` route contract, which returns `/parents` and `/dev` to `/settings?section=parent`; regression cases were added to `navigation.test.ts`, and `43_island_navigation_spec.md` now states the return-context requirement. On `live-27`, the return button reopened the expanded “テスト・保護者” section at both 1280×720 and 390×844; cancel from a direct `/parents` gate also returned there. Separately, tapping the selected global Settings tab reset to `/settings`, matching the tab-to-overview policy. See the route/scroll evidence and screenshots above.

### F-25 — Parent history exposed internal math skill IDs (medium, fixed locally)

With recent attempts present, the caregiver-facing history showed identifiers such as `add_finger`, `share_equal`, and `two_more` rather than skill names. `ParentsPage` now resolves math IDs through `MATH_SKILL_LABELS` and vocabulary IDs through their displayed word surface; unknown values use a generic label, never the raw ID. `parentAttemptLabel.test.ts` covers known and unknown math/vocabulary values, and `docs/product/06_screen_specs.md` §8 requires user-facing names without internal IDs. The populated phone history showed Japanese skill labels in `live-28`; populated weak-point details were later checked under F-28. `live-29` checked only the empty tablet layout; `live-38` subsequently checked populated history at tablet width using an explicit display fixture.

### F-26 — Standalone records exit was an unlabeled icon button (medium, fixed locally; flag-off runtime checked)

Source review found that the non-island `/stats` route rendered a right-side close action containing only a Lucide `X`, with no accessible name. The button now exposes a Japanese `閉じる`/`とじる` name and treats the `X` as decorative. `StatsCloseAction.test.tsx` verifies both names, decorative-icon semantics, and the minimum touch-target class. In the fresh flag-off `live-34` browser session, a throwaway profile was created through onboarding, the records tab rendered the standalone route, the AX tree exposed `button "閉じる"`, and activating it returned through `/` to the configured launch screen `/battle`. The rendered screen is saved as [live-34-stats-390x844.png](evidence/screens/live-34-35-whole-app/live-34-stats-390x844.png). This is a local development-preview check, not a release build or real screen-reader evaluation.

### F-27 — Legacy onboarding nickname input had no accessible name (medium, fixed locally; runtime checked)

At 390×844 on the local `VITE_ISLAND_ENABLED=false`, `VITE_NATURE_TOWN_ENABLED=false` preview, the nickname step showed “ニックネームをおしえてね” and “あとで かえられるよ”; the AX tree now exposes the textbox under the visible question, with the helper connected by `aria-describedby` and `autocomplete="nickname"`. The empty-name disabled state is preserved and `OnboardingNameStep.test.tsx` checks those relationships. The blank-name screen is saved as [live-34-onboarding-name-390x844.png](evidence/screens/live-34-35-whole-app/live-34-onboarding-name-390x844.png). A disposable profile was subsequently completed only to reach F-26's private route and was removed with the browser session data. This is not a screen-reader or participant evaluation; the onboarding back-control tap-size handoff remains with its owner.

### F-28 — Parent weak-point badges exposed internal skill IDs (medium, fixed in source; populated rendering checked with an explicit fixture)

The Japanese weak-math list rendered `weakMathIds` directly as badge text, unlike the recent-history path. The weak-vocabulary fallback also displayed its raw ID when a word lookup failed. Both now use shared parent-facing label resolvers: known math/vocabulary values use their visible names, while unknown values show generic `学習項目` or `単語` labels. The §8 screen contract forbids unknown internal IDs, and tests cover known/unknown IDs plus vocabulary IDs whose catalog row omits `surface`. The actual `ENGLISH_WORDS` catalog uses optional `surface` only for aliases; live-36 exposed that the resolver was treating known IDs such as `apple` as unknown and rendering `単語`. The resolver now uses a non-empty `surface` when present, otherwise the known catalog ID (the English spelling), while retaining the generic unknown fallback.

For layout evidence, `live-36` created a disposable Grade 1 mixed-subject profile through onboarding and then directly seeded explicit QA-only `isWeak: true` memory rows: four known math IDs and three known vocabulary IDs. Its attempt-log count remained zero. This deliberately bypassed the natural weak-state threshold and proves badge rendering only—not acquisition, threshold behavior, or persisted user data. The rendered labels and wrapping were checked at both phone and tablet widths below. No learning threshold or production data was changed.

### F-29 — Parent history result badges wrapped mid-word on phone (low, fixed locally)

At 390×844, the compact `不正解` badge wrapped into two lines beside a long skill label. The result badge now uses `shrink-0 whitespace-nowrap`; its text remains a single line while the adjacent skill label can wrap. The before/after captures are [live-34](evidence/screens/live-34-35-whole-app/live-34-parents-before-390x844.png) and [live-35](evidence/screens/live-34-35-whole-app/live-35-parents-after-390x844.png). This only adjusts presentation; learning results and history data are unchanged.

### F-30 — Wide Explore prompt and instruction overlap the answer keypad (high, fixed and runtime checked)

On the actual `/explore` encounter at 1280×720 (`live-39`), the equation occupied y=473–525 while the first keypad row began at y=509, covering its bottom 16px. The supporting line `こたえるたびに つちが ほれるよ` occupied y=530–545, behind the keypad row at y=509–553. This hid part of the active question and its explanation under interactive targets. At 390×844 and 768×1024, the same helper line ended 15px and 14px above the first keypad row, respectively; those widths had no document overflow. The original wide screen remains available as [the live-39 baseline](evidence/screens/live-39-routes-1280/explore-overlap-1280x720.png).

The correction is scoped to short landscape viewports (width ≥1024px, height ≤800px). Answer-only encounters use a 53% shelf; encounters with visual support use 63%. Long prompts (16+ characters) use compact 22–24px type in this short-wide layout, and the visual hint drops its duplicate prompt caption while keeping the labelled count cards. At heights ≤736px the Snap Root scene is framed at 90% width so both characters and the root remain visible. The screen-flow contract in [12_screen_flow_spec.md](../../../product/12_screen_flow_spec.md) keeps the question, helper, and visual support above—not behind—the TenKey while preserving the 44px target.

The current same-candidate word-problem captures are [1280×720](evidence/screens/live-44-explore/explore-1280x720-word-problem.png), [1024×700](evidence/screens/live-44-explore/explore-1024x700-word-problem.png), [1024×768](evidence/screens/live-44-explore/explore-1024x768-word-problem.png), [768×1024](evidence/screens/live-44-explore/explore-768x1024-word-problem.png), and [390×844](evidence/screens/live-44-explore/explore-390x844-word-problem.png). At 1280×720, hint cards end at y=498 and the first key begins at y=509; at 1024×700 the cards end at y=482 and the first key begins at y=489. The first-row keys measure at least 44px at all five sizes; each viewport has no document overflow. Candidate `sansu-whole-ux-2026-09-20-28bd252-live-44` is a local preview (`buildRevision=development-local`), with `VITE_ISLAND_ENABLED=false`, `VITE_NATURE_TOWN_ENABLED=false`, `delivery=snap-root-v1`, `visualCandidateId=dig-pop-carry-bloom-v3`, and no service-worker controller. A separate equation-only check from `live-41` covers the no-visual-support branch (53% shelf); its short prompt does not activate the new long-prompt styling. No learning answer was entered in these captures. Profile disposal is recorded with final verification below. These are preview screenshots, not release, physical-device, assistive-technology, or participant-evaluation evidence.

### F-31 — Study skip action was covered by the question layer on phone (medium, fixed and runtime checked)

The repeated `e2e:smoke` failures on the direct Study route and Stats → periodic-test route were real hit-testing failures, not just flaky assertions. At 390×844, the skip button measured 75×26px and `document.elementFromPoint()` at its center returned the question-content layer: both the absolute top row and full-height question region used `z-index: 10`, with the latter painted afterward. The top row now uses `z-index: 20`, and the skip button has a 44px minimum height. After the change the same visible target measures 75×44px and receives its center hit; clicking it displays the expected skip state and `次へ` action.

Before/after captures: [covered 75×26px target](evidence/screens/live-43-study-skip/study-skip-before-390x844.png), [working 75×44px target](evidence/screens/live-44-study-skip/study-before-390x844.png), and [skip confirmation with next action](evidence/screens/live-44-study-skip/study-after-390x844.png). Candidate `sansu-whole-ux-2026-09-20-28bd252-live-44` is a local `/study` preview (`buildRevision=development-local`, `VITE_ISLAND_ENABLED=false`, `VITE_NATURE_TOWN_ENABLED=false`, no service-worker controller). One skip was exercised only in a disposable browser profile; no correct answer was entered. Final smoke and profile disposal results are recorded below. This is not physical-device or participant-evaluation evidence.

### F-32 — Short-landscape play continuation had no scroll affordance (medium, fixed and runtime checked)

At 844×390 and 1024×390, `.island-play-body` was shorter than its content, so `ひかりを とどける` was reachable only after scrolling but nothing in the initial view indicated that more content existed. The screen contract now calls for a brief, text-backed downward cue while content remains below. `IslandPlay` measures overflow/scroll state and shows `↓ つづき` only until the bottom; it reappears after returning to the top. This adds no overlay over buttons or camera controls and changes no learning/domain action.

`islandPlayScroll.test.ts` covers fit, overflow, and end-of-scroll states. `live-49` passed 17 navigation/action checks at each short-landscape viewport and saved both initial/scrolled states. The CTA remains reachable after scrolling and at least 44px high. See the [live-49 runtime evidence](#play-scroll-continuation-cue-live-49-2026-09-21). Real-device touch discovery, child comprehension, and screen-reader behavior remain untested.

## Focused runtime steps (`live-34` / `live-35`)

1. **Onboarding name step — healthy:** 390×844; the blank textbox is visibly labelled and named in the browser accessibility tree. See [capture](evidence/screens/live-34-35-whole-app/live-34-onboarding-name-390x844.png).
2. **Standalone records (`/#/stats`) — healthy:** 390×844; the close control is visible and named “閉じる”; activating it returns through the root route to `/battle`. See [capture](evidence/screens/live-34-35-whole-app/live-34-stats-390x844.png).
3. **Parent history before/after — improved:** the visible result badge changed from a two-line wrap to one line at the same 390×844 viewport. Recent math labels are human-readable. The weak-candidate list was empty in this profile, so its populated state was not confirmed.

Both are local development previews with `buildRevision=development-local`, flags `VITE_ISLAND_ENABLED=false` and `VITE_NATURE_TOWN_ENABLED=false`, delivery `snap-root-v1`, and visual lineage `pokko-field-v1`. The focused rendered candidates are task labels `sansu-whole-ux-2026-09-20-28bd252-live-34` (onboarding/stats and pre-fix parent view) and `...-live-35` (post-fix parent view); the legacy screen does not expose a separate visual-candidate ID. A fresh named Playwright session was used; `navigator.serviceWorker.controller` was false. These checks are not production cache/update evidence.

## Populated parent weak-candidate display (`live-36`)

The fresh named Playwright session used the actual onboarding and Settings → parent gate route. The gate was answered in the UI; only after onboarding, a dev-only page-side fixture wrote `isWeak: true` to four known active math rows (`add_finger`, `add_1d_1_bridge`, `add_1d_1`, `add_1d_2`) and three known vocabulary rows (`apple`, `orange`, `milk`). Every fixture row had zero answer counters, and the profile had zero attempt logs. This is explicitly a display fixture, not evidence that the learning rules naturally classified any item as weak.

| Viewport / state | Check | Result |
|---|---|---|
| 390×844 phone, populated `/parents` | Four Japanese math skill badges and three English-word / Japanese-meaning badges; `documentElement.scrollWidth` and `body.scrollWidth` both 390px. | No horizontal overflow. Chips wrap between items without splitting a label; the entire weak-candidate card remains above the fixed navigation. [Screenshot](evidence/screens/live-36-parents/parents-populated-390x844.png) |
| 768×1024 tablet, populated `/parents` | Same fixture and content; document/body width both 768px; fixed navigation begins at y=968px. | No horizontal overflow. Math chips arrange in two columns; vocabulary chips wrap naturally. History empty state and `設定に戻る` are visible above navigation. [Screenshot](evidence/screens/live-36-parents/parents-populated-768x1024.png) |

The phone capture initially revealed the known-vocabulary fallback defect described under F-28. After the resolver correction, both screenshots show `apple`, `milk`, and `orange` with their Japanese meanings rather than the generic `単語`. Browser console reported zero errors or warnings. The screenshots were saved and visually inspected at their durable audit paths. A local dev module supplied the fixture; the only weak rows existed in this disposable browser session. The named session was then closed and reopened for cleanup verification; no release, natural-threshold, screen-reader, or participant-evaluation claim is made.

## Populated parent history on tablet (`live-38`)

A fresh disposable Grade 1 mixed-subject profile was created through the actual onboarding UI. Five synthetic `recentAttempts` rows were then added only to that browser profile to display a mix of known math/vocabulary labels, all three result badges, long Japanese math labels, and the generic unknown-word fallback. No learning attempt logs, memory rows, answer counters, or weak-candidate flags were written. The Settings → parent gate was traversed in the UI.

| Viewport / state | Check | Result |
|---|---|---|
| 768×1024 tablet, populated `/parents` at top | Document and body widths both 768px. The page content is a 896px scroll viewport over 1,192px of content (296px scroll range). | No horizontal overflow. Five mixed-subject history rows render; long Japanese math labels wrap naturally, result badges stay on one line, and unknown vocabulary does not expose its internal ID. [Top screenshot](evidence/screens/live-38-parents/parents-history-top-768x1024.png) |
| 768×1024 tablet, populated `/parents` at maximum scroll | `設定に戻る` is 180×44px at y=868–912; fixed nav begins at y=968, leaving 56px of clearance. | All five rows and the return action are reachable above the fixed navigation. [Bottom screenshot](evidence/screens/live-38-parents/parents-history-bottom-768x1024.png) |

Browser console had zero errors or warnings; the service worker did not control the page. The named browser session was closed and reopened, and `/parents` redirected to onboarding, confirming the disposable profile/fixture did not persist. This validates tablet rendering only—not natural learning acquisition, production data, release behavior, screen-reader output, or participant experience.

## Flag-off primary-route survey at 1280×720 (`live-39`)

A fresh disposable Grade 1 mixed-subject profile was created through the onboarding UI. The route review did not answer learning prompts or change settings; the parent gate was passed only to inspect the real parent page. Document and body widths remained exactly 1280px on the surveyed routes. The shell intentionally stays in its 430px centered phone frame, while `Study`, `Explore`, and Battle play/setup use the 1180px fullscreen workspace; the different width modes match `Layout`'s explicit route policy, so the audit did not force them into one layout.

| Route / state | Rendered layout and checks | Evidence |
|---|---|---|
| Onboarding entry | Centered 430px phone-shaped welcome screen; no viewport overflow. | [Welcome](evidence/screens/live-39-routes-1280/onboarding-start-1280x720.png) |
| `/battle` exploration base | 430px shell; content scrolls 584px over 856px; no document overflow. | [Base](evidence/screens/live-39-routes-1280/battle-hub-1280x720.png) |
| `/battle/play`, co-op and tug-of-war setup | Full 1180px workspace; 1280×720 document/body; mode-specific controls and pinned start action render. | [General setup](evidence/screens/live-39-routes-1280/battle-setup-1280x720.png), [co-op](evidence/screens/live-39-routes-1280/coop-setup-1280x720.png), [tug-of-war](evidence/screens/live-39-routes-1280/tug-setup-1280x720.png) |
| `/study` English choice prompt | Full 1180px workspace; question and four options fit with no document overflow. | [Study](evidence/screens/live-39-routes-1280/study-1280x720.png) |
| `/explore` math encounter | Full 1180px workspace; no document overflow; the prompt/keypad collision is F-30. | [Encounter](evidence/screens/live-39-routes-1280/explore-overlap-1280x720.png) |
| `/stats` empty records, `/settings`, `/settings/curriculum` | Centered 430px shell; records and curriculum have their own scroll areas (588/2,016px and 432/4,031px); no document overflow. | [Records](evidence/screens/live-39-routes-1280/stats-empty-1280x720.png), [settings](evidence/screens/live-39-routes-1280/settings-1280x720.png), [curriculum](evidence/screens/live-39-routes-1280/curriculum-1280x720.png) |
| `/parents` empty page after gate | Centered 430px shell; 592/815px scroll region; no document overflow. At maximum scroll, the return control remains above the fixed nav per the separate `live-27` measurement. | [Parent page](evidence/screens/live-39-routes-1280/parents-empty-1280x720.png) |
| `/dev` profile tab | Centered 430px internal utility; 553/623px scroll region; no document overflow. | Internal screenshot kept in local Playwright output; not included in the product evidence set. |

Browser console had zero errors and zero warnings across this route review; `serviceWorker.controller` was false. The AuditQA profile was discarded by closing the named session and confirming `/parents` redirects to onboarding in a fresh session. This is a local preview, not a production/release or assistive-technology evaluation. Feature-flag-on island/Nature Town routes are excluded here; Island shell has separate route evidence and Nature Town remains with its owner.

## Verification performed

- Latest `npm run verify:core` after `live-53` passed: docs check, current-UI entry guard, lint, typecheck, 456 Vitest files / 4,138 tests, production build, and asset budget. The latest full route evidence remains `live-50` (65 checks / 53 captures), `live-51` (first-run Welcome), and `live-52` (57 captures across 390×844, 768×1024, 1280×720, and 844×390; four contrast passes, zero page errors; see the [live-52 contact sheet](evidence/screens/live-52-contrast/contact-sheet.html) and [report](evidence/screens/live-52-contrast/report.json)). `live-53` adds a fresh isolated runtime identity check only. The old Slate-400 contrast sentinel measured 2.59:1 and the shared muted token measured 5.40:1 on paper. The isolated flag-off classic Explore smoke suite previously passed 31/31 scenarios; it was not rerun for this Utility/identity-marker change. Lint reported zero errors and one existing Fast Refresh warning in `IslandMilestone.tsx`; build retained the existing stale Browserslist, empty `pdf` chunk, and >500 kB chunk advisories. PWA precache is 11.60 MiB / 12.00 MiB. `docs:check` reports the unrelated overdue Review By on `docs/tasks/active/2026-09-13-mysterious-island-v3.md`.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-35` passed `npm run verify:core`: docs check, lint, typecheck, 453 Vitest files / 4,127 tests, production build, and asset-budget check. Lint had zero errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retained the stale Browserslist notice, empty `pdf` chunk, and >500 kB chunk advisory. PWA precache was 11.59 MiB / 12.00 MiB. After the final helper assertion and formatting-only adjustment, the 3 focused test files (6 tests), target ESLint, typecheck, docs check, and `git diff --check` passed again.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-36` passed the focused parent-label test (1 file / 2 tests), target ESLint, typecheck, `npm run docs:check`, and `git diff --check`. The `live-35` full `verify:core` result remains the latest full-suite/build check; `live-36` changes only the known-vocabulary fallback and its regression test/spec/evidence. The live parent route had zero browser console errors or warnings.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-37` adds rendered F-12 evidence only (no app-source change): phone/tablet radar gaps, measured-zero and unpracticed tooltips, chart SVG title/description, and weekly accuracy gaps. Both tested viewport widths had no horizontal overflow; browser console errors/warnings were zero. The disposable profile/log fixture was discarded by closing the session and confirming a fresh `/stats` visit redirects to onboarding.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-37` also underlies `live-38` (no app-source change): populated parent history rendered at 768×1024 with no horizontal overflow or console errors/warnings; at maximum scroll the 180×44px return control had 56px clearance above fixed navigation. The display-only attempt fixture was discarded by closing the session and confirming a fresh `/parents` visit redirects to onboarding.
- The same source candidate underlies `live-39` (no app-source change): reviewed the feature-flag-off primary routes at 1280×720; document/body widths stayed at the viewport width, and console errors/warnings were zero. F-30 on `/explore` was routed to the active experience owner and then fixed/rendered at `live-40`.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-40` passed `npm run verify:core`: docs check, lint, typecheck, 453 Vitest files / 4,127 tests, production build, and asset-budget check. Lint had zero errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retained the stale Browserslist notice, empty `pdf` chunk, and >500 kB chunk advisory. PWA precache was 11.59 MiB / 12.00 MiB. The full `npm run e2e:smoke` exited 1: Explore route/answer/retry scenarios passed, while the direct Study and Stats→periodic-test skip scenarios failed because a flex container intercepted pointer input; this is outside F-30's CSS selector and remains to be investigated.
- Latest app source candidate `sansu-whole-ux-2026-09-20-28bd252-live-44` passed `npm run verify:core` (exit 0): `docs:check`, lint (0 errors; one existing Fast Refresh warning in `IslandMilestone.tsx`), typecheck, 453 Vitest files / 4,127 tests, production build, and asset-budget check. Existing build notices remain: stale Browserslist data, empty `pdf` chunk, and >500 kB chunk; PWA precache is 11.59 MiB / 12.00 MiB. `ExploreProblemPanel.test.tsx` passed 1 file / 16 tests. The full `npm run e2e:smoke` exited 0 with every scenario PASS (31 scenarios), including direct Study, Stats→periodic-test, Root Pull v2 with an arithmetic visual sequence, Explore recovery, and tablet routes. The test-only numeric solver reads a single hidden term from a rendered arithmetic sequence; app behavior was not changed for that harness issue. `eslint tools/e2e-smoke.mjs`, `docs:check`, and `git diff --check` passed after the final evidence update.
- The `live-44` disposable profile was closed after one skip-only interaction and no correct answer. A fresh `/parents` context redirected to `/#/onboarding`; localStorage was empty, every user-data store in `SansuDatabase` had 0 rows, and there was no service-worker controller. Only the app's empty baseline `appData` row remained. The evidence is local preview, not a production/release or physical-device run.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-03` passed `npm run verify:core` (exit code 0): docs check, lint, typecheck, 443 Vitest files / 4,099 tests, production build, and asset-budget check. The earlier `live-02` core run also passed.
- Lint reported zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`. The build also reported the existing stale Browserslist data and large-chunk advisory; build and asset checks passed (PWA precache 11.56 MiB / 12.00 MiB).
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-05` passed `npm run verify:core` (exit code 0): docs check, lint, typecheck, 444 Vitest files / 4,101 tests, production build, and asset-budget check. Candidate `live-04` also passed before the safe-initial-focus adjustment.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-06` passed `npm run verify:core` (exit code 0): docs check, lint, typecheck, 444 Vitest files / 4,101 tests, production build, and asset-budget check. This includes the profile-action semantics/touch-size change.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-09` passed `npm run verify:core` (exit code 0): docs check, lint, typecheck, 446 Vitest files / 4,104 tests, production build, and asset-budget check. This includes F-06, F-08, and F-09. PWA precache is 11.57 MiB / 12.00 MiB. Lint still reports the existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build still reports stale Browserslist data and a large-chunk advisory. This result predates F-11's CSS change.
- Latest source candidate `sansu-whole-ux-2026-09-20-28bd252-live-10` contains F-11's focus-visible outline. `npm run docs:check`, `git diff --check`, `npm run build`, and `npm run test:run -- src/components/domain/PrintableTestPreview.test.tsx` passed (6 tests). Generated CSS includes the outline rule. Build reported the existing stale Browserslist data and large-chunk advisory. There is no runtime screenshot evidence for `live-10`.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-11` passed `npm run test:run -- src/components/charts/WeeklyTrendChart.test.ts src/components/charts/SkillRadarChart.test.ts` (2 files / 3 tests), `npm run typecheck`, targeted ESLint (0 warnings), `npm run docs:check`, `git diff --check`, and `npm run build` including `assets:check` (PWA precache 11.57 MiB / 12.00 MiB). Build retains the existing stale Browserslist and >500 kB chunk advisories. This is source/build evidence only; no runtime screenshot or chart navigation check was performed.
- Candidate `sansu-whole-ux-2026-09-20-28bd252-live-12` adds F-13 to the battle setup and battle spec. The BattleSetup test passed (1 file / 2 tests), as did typecheck, targeted ESLint (0 warnings), docs check, diff check, and production build including `assets:check` (PWA precache 11.57 MiB / 12.00 MiB). Build retains the stale Browserslist and >500 kB chunk advisories. This candidate has focused rather than full `verify:core` evidence; no runtime screenshot, responsive measurement, or keyboard journey was performed.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-12`, including F-13 before the final outline polish, passed `npm run verify:core`: docs check, lint, typecheck, 449 Vitest files / 4,109 tests, production build, and asset-budget check. Lint had zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build retains the stale Browserslist and >500 kB chunk advisories. PWA precache is 11.57 MiB / 12.00 MiB. This remains source/build/test evidence only; no runtime screenshot or keyboard journey was performed.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-13` includes stronger 2px selected-option outlines for F-13. The focused BattleSetup test passed (1 file / 2 tests), as did typecheck, targeted ESLint (0 warnings), docs check, diff check, and production build including `assets:check` (PWA precache 11.57 MiB / 12.00 MiB).
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-16` passed `npm run e2e:island-navigation` at 390×844 and 768×1024 (16 journey assertions per viewport, no page errors). The `live-16` 844×390 DOM check showed no horizontal overflow or visible control below 44px; Battle setup players were 400px wide each, with 130px scroll viewport and 628px content. A separate wrong-answer UI check kept the question cursor on the same problem and showed retry feedback without page errors. Its short-landscape captures remain the latest evidence for that viewport.
- The same `live-16` candidate passed `npm run verify:core`: docs check, lint, typecheck, 449 Vitest files / 4,110 tests, production build, and asset-budget check. Lint had zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build retains the stale Browserslist and >500 kB chunk advisories, with no custom-variant CSS warning. PWA precache is 11.58 MiB / 12.00 MiB.
- Final `live-11` source candidate, including the below-chart no-practice category caption, passed `npm run verify:core`: docs check, lint, typecheck, 448 Vitest files / 4,107 tests, production build, and asset-budget check. Lint had zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build retains the stale Browserslist and >500 kB chunk advisories. PWA precache is 11.57 MiB / 12.00 MiB. No runtime screenshot or chart navigation check was performed.
- Targeted typecheck/ESLint and focused `PaperTestScoreModal`, `modalFocus`, `SegmentedControl`, `Button`, and `NameModal` tests passed. Live checks reconfirmed F-01, F-02, shared-modal F-03, and F-04 on their recorded candidates; F-09 live behavior remains unverified.
- F-07 was added to the existing [Learning rhythm and island experience task](../../../tasks/active/2026-09-07-experience-improvements.md) for owner handling; no Explore source was changed in this audit.
- F-10 was added to the same owner task; no learning, onboarding, or pet-flow source was changed for this handoff.
- The `live-06` local preview confirmed F-05's named controls and the rename modal's Escape focus restoration. F-05/F-06/F-08/F-09 target and semantics changes on `live-09` are source/test evidence; responsive and live keyboard measurements are still pending.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-19` passed `npm run e2e:island-navigation` at 390×844 and 768×1024 (16 assertions per viewport, 26 captures, no page errors). All captures report runtime `development-local:bd9e244d-6b20-426d-9875-d3034f81865e`, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and learning candidate `mystic-island-learning-v2`; local dev service worker was not controlling. Evidence is in `evidence/screens/live-19-navigation/` and the phone/tablet contact sheets.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-22` passed `npm run e2e:island-navigation` at 390×844 and 768×1024 (16 assertions per viewport, 26 captures, no page errors). Runtime and delivery/visual/learning identifiers match `live-19`; the local dev service worker was not controlling. Evidence is in `evidence/screens/live-22-navigation/` and `evidence/live-22-navigation/`.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-23` passed `npm run e2e:island-navigation` at 390×844, 768×1024, and 1280×720 (16 journey checks per viewport, 39 captures, no page errors). The harness accepts an optional comma-separated viewport list via `SANSU_NAVIGATION_VIEWPORTS`; its default remains phone/tablet. Runtime and delivery/visual/learning identifiers match `live-22`; the local dev service worker was not controlling. Evidence is in `evidence/screens/live-23-navigation/` and its three contact sheets.
- At 1280×720, a fresh isolated browser measured the records scroll container before/after scrolling: 586px visible height, 1,178px content height, 24px bottom padding, and 592px scroll range. The last action remains reachable above the fixed bottom navigation; the partially entering section label is ordinary viewport clipping, not an occlusion defect.
- Focused `live-22` checks at 390×844 covered `/parents` and `/settings/curriculum`; the same error/focus behavior was checked at 1280×720. Wrong submissions by keyboard and pointer returned focus to the labelled field, exposed the retry message and invalid state, and did not change curriculum settings. Disposable browser contexts were discarded. Screens are linked above.
- Focused `live-19` browser checks used fresh isolated contexts for non-empty Stats data at phone/tablet, parent gate at 1280×720, paper-score entry at 390×844, and print preview at 390×844. These confirmed no chart-label wrapping/overflow, visible focus and return behavior, and no accidental learning-log/memory writes. The temporary profile state was discarded when each context closed. The print preview was opened but no OS print/PDF action was launched.
- Final focused tests for paper scoring and both charts passed (3 files / 5 tests); `npm run typecheck`, full ESLint (0 errors, one existing `IslandMilestone.tsx` Fast Refresh warning), and `git diff --check` passed. Candidate-wide `npm run verify:core` is recorded after the latest docs check below.
- Latest `live-22` `npm run verify:core` passed: `docs:check`, lint, typecheck, 450 Vitest files / 4,113 tests, production build, and `assets:check`. Lint had 0 errors and the existing `src/components/island/IslandMilestone.tsx` Fast Refresh warning. Build retains the existing stale Browserslist data and >500 kB chunk advisory; PWA precache is 11.58 MiB / 12.00 MiB.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-24` passed `npm run verify:core`: docs check, lint, typecheck, 450 Vitest files / 4,113 tests, production build, and asset-budget check. BattleSetup's focused test also passed (1 file / 3 tests). The final one-line gate-label polish was then verified on live candidate `live-25` at phone/tablet sizes. Its final `npm run verify:core` and post-report docs/diff checks are recorded below. Lint had zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build retains the stale Browserslist data and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB.
- Latest source candidate `sansu-whole-ux-2026-09-20-28bd252-live-25` passed `npm run verify:core`: docs check, lint, typecheck, 450 Vitest files / 4,113 tests, production build, and asset-budget check. At 320×568 and 768×1024, the gate's 197×44px return target rendered its label on one line; the phone action returned to `/battle`. Lint had zero errors and one existing Fast Refresh warning in `src/components/island/IslandMilestone.tsx`; build retains the stale Browserslist data and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB. The updated evidence report passed `docs:check` and `git diff --check`.
- Latest focused candidate `sansu-whole-ux-2026-09-20-28bd252-live-26` passed `npm run verify:core`: docs check, lint, typecheck, 450 Vitest files / 4,114 tests, production build, and asset-budget check. `BattleSetup.test.tsx` passed all 4 tests. Lint had no errors and the existing Fast Refresh warning in `IslandMilestone.tsx`; build retained the stale Browserslist, empty `pdf` chunk, and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB. The local browser had 0 errors; two duplicate `PCFSoftShadowMap` deprecation warnings appeared while initializing the 3D onboarding world, not during Battle play. This source tree also contained concurrent Nature Town edits; this slice did not modify them.
- Latest source candidate `sansu-whole-ux-2026-09-20-28bd252-live-27` passed `npm run verify:core`: docs check, lint, typecheck, 450 Vitest files / 4,116 tests, production build, and asset-budget check. Lint had 0 errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retained the stale Browserslist, empty `pdf` chunk, and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB. The browser had 0 page errors; two Three.js deprecation warnings appeared during onboarding scene initialization. The parent page itself was checked in an isolated context at 1280×720 only. This result predates the populated-history label change.
- Latest source candidate `sansu-whole-ux-2026-09-20-28bd252-live-28` passed `npm run verify:core`: `docs:check`, lint, typecheck, all tests, production build, and `assets:check`. The test summary is 451 files / 4,118 tests. Lint had 0 errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retained stale Browserslist data, an empty `pdf` chunk, and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB. Browser page errors: 0; two Three.js deprecation warnings appeared during onboarding scene initialization. Disposable data was removed. The updated report/task passed `docs:check` and `git diff --check`.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-30` passed `npm run verify:core` after F-26: docs check, lint, typecheck, full test suite, production build, and `assets:check` (exit 0). Lint had 0 errors and the existing `IslandMilestone.tsx` Fast Refresh warning. Build retained stale Browserslist data, an empty `pdf` chunk, and >500 kB chunk advisories; PWA precache was 11.59 MiB / 12.00 MiB. This is shared dirty-working-tree source/build/test evidence, not a rendered flag-off `/stats` or release result.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-31` passed `npm run verify:core` after the F-26 regression test: docs check, lint, typecheck, 452 Vitest files / 4,120 tests, production build, and `assets:check` (exit 0). Lint had 0 errors and the existing `IslandMilestone.tsx` Fast Refresh warning. Build retained stale Browserslist data, an empty `pdf` chunk, and >500 kB chunk advisories; PWA precache was 11.59 MiB / 12.00 MiB. This is shared dirty-working-tree source/build/test evidence, not a rendered flag-off `/stats` or release result.
- Two early print-preview harness attempts were corrected before the passing run: one locator matched both math and vocabulary actions, and one assertion assumed the trigger label would not change after creating a pending test. The app correctly switched to `同じ問題を印刷` and focus returned to the same DOM button; these were harness assumptions, not product failures.
- Actual assistive-technology/screen-reader evaluation and child-participant evaluation remain unperformed. The live screenshots and browser interactions use isolated contexts; they do not represent a released build or user records.

## Separate quality gates and remaining work

- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-32` passed `npm run verify:core`: `docs:check`, lint, typecheck, 452 Vitest files / 4,125 tests, production build, and `assets:check` (exit 0). Lint had no errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retains stale Browserslist data, the empty `pdf` chunk, and >500 kB chunk advisories. PWA precache is 11.59 MiB / 12.00 MiB. This is shared dirty-working-tree source/build/test evidence, not rendered chart or release evidence.
- Source candidate `sansu-whole-ux-2026-09-20-28bd252-live-33` passed `npm run verify:core` after F-27: docs check, lint, typecheck, full Vitest suite, production build, and `assets:check`. Lint had no errors and the existing `IslandMilestone.tsx` Fast Refresh warning; build retained stale Browserslist data, an empty `pdf` chunk, and >500 kB chunk advisories. PWA precache was 11.59 MiB / 12.00 MiB. The 390×844 focused preview confirmed the new input name in the browser accessibility tree, but its screenshot was not saved locally; this remains a focused live observation, not a screen-reader or visual-evidence pass.
- Visual appeal: the same-candidate contact sheets were reviewed at 390×844, 768×1024, and 1280×720; short-landscape evidence includes `live-16`, focused `live-44` Explore renders, and the `live-49` play cue. Distinct Island, learning, records, and settings roles remain visually recognizable; the F-30 question/helper/hint/keypad do not collide, and `↓ つづき` sits on one line without covering the play controls. This is a design review, not a participant preference score.
- Silent comprehension and safety: accessible names/pressed states, empty records, correct and incorrect learning states, retry, return behavior, and reduced-motion/sound-off operation were technically inspected. No child participant evaluation was performed; comprehension or enjoyment is not claimed.
- Runtime integrity: older focus/dialog observations for F-01–F-05 are from `live-06` with focused F-03/F-04 retests on `live-19`; source/test handoffs F-06–F-10 are recorded at `live-09`; F-11 and the earlier F-12 tooltip/chip treatment received live checks on `live-19`, and the latest F-12 radar/weekly geometry and tooltip behavior were rendered at phone/tablet sizes on `live-37`; F-13–F-15 retain `live-16` runtime evidence; F-16/F-17 received `live-19` checks; F-18/F-19 received `live-22` semantic and live checks; F-20 received focused `live-24` responsive-game checks; F-21–F-23 received focused `live-26` runtime geometry, responsive-screen, and return-flow checks; F-24 received `live-27` parent return-context and empty-state checks at desktop/phone; F-25 received `live-28` populated-history phone and `live-38` populated-history tablet checks; `live-29` checked the empty parent tablet layout; F-26 received flag-off route/rendered return evidence at `live-34` but still lacks assistive-technology evaluation; F-27's nickname input name and screenshot are saved from `live-34` but still lack real screen-reader evaluation; F-30 has same-candidate rendered evidence at `live-44` across short-wide, tablet, and phone sizes; F-31's skip hit target and click response are rendered and exercised at `live-44`. Both fixes are local preview evidence, not release evidence.
- F-32's conditional play scroll cue was rendered on canonical Island at 844×390 and 1024×390 in `live-49`; the hint appeared/disappeared/restored with scroll and the continuation CTA remained reachable. A participant, actual touch-device, and screen-reader check is still required before claiming discoverability or comprehension.
- F-12's source geometry leaves gaps for unpracticed categories and preserves recorded zero points. `live-37` confirms the rendered open path, adjacent unpracticed chips, recorded-zero and unpracticed tooltips, SVG descriptions, and weekly accuracy no-data gaps at 390×844 / 768×1024. The data was a disposable explicit QA fixture, not a natural-learning threshold evaluation.
- Not checked: actual assistive-technology/screen-reader announcement order, child-participant comprehension and enjoyment, every state and route under every feature-flag configuration (Nature Town remains with its owner), natural weak-state acquisition at the per-skill threshold/hysteresis, every Battle question-visual type/physical-device scrolling feel, and OS print/PDF output. The flag-off primary routes were surveyed at 1280×720 on `live-39`; Island shell has separate route evidence, while Nature Town/other flag-on configurations and exhaustive states remain open. Battle setup/play at 1280×720 is also covered by `live-26`; the empty parent state is covered at 1280×720 and 390×844 by `live-27`, and at 768×1024 by `live-29`; populated parent history labels are covered at 390×844 by `live-28` and at 768×1024 with a display fixture by `live-38`; populated weak-badge presentation is covered with an explicit fixture at phone/tablet sizes by `live-36`. Remaining gaps are not inferred from DOM checks or screenshots.
- Scope boundary: the existing island v3, house, Nature Town, and whole-app brand owners remain authoritative for their owned issues. This audit does not change those owners' work or clear the external brand gate.

## Parent review-candidate terminology (`live-55`, 2026-09-21)

### F-35 — Parent weak-state copy was deficit-framed and omitted the actual entry/recovery rules (medium, fixed)

The gated Parent page called weak items “苦手なところ” and described only “正答率が60%未満”. The implementation actually enters review status after at least five answers when the latest-ten accuracy falls below 60%, retains the prior status in the 60–79% band, and clears it at 80% or higher. This made the parent explanation incomplete and framed a temporary learning state as a learner deficit. Parent display is the explicit exception to the child-facing rule: weak status remains hidden from child screens.

The Parent page now labels the section and count “復習候補”, explains the answer-count and latest-ten thresholds including the 80% exit rule, and uses “今は復習候補がありません。” for empty states. Specs 01 and 06 §8.1 now align on the gated parent-only exception and non-judgmental wording. The shared copy has focused tests. The explanation is visible to a parent only after the existing gate.

`tools/e2e-island-navigation.mjs` now traverses the actual settings → parent gate → `/parents` path and verifies the copy, both empty states, horizontal-overflow/wrapping, ≥44px return target, and return to the originating settings section. The 5-view run passed 112 route/action checks and saved 77 captures with no page errors: 22 checks each at 390×844, 768×1024, and 1280×720, and 23 each at 844×390 and 1024×390. All evidence comes from `http://127.0.0.1:5198`, app-root Island=true / Nature Town=false, build revision `development-local`, reduced motion enabled, and no service-worker controller. `/parents` is a shared Utility route: its route-specific delivery/candidate are not applicable; the app root's configured delivery is recorded separately as `snap-root-v1`. The test profile was disposable and closed after the run.

All five screenshots were visually reviewed in the [F-35 contact sheet](evidence/screens/live-55-parent-review/contact-sheet.html). Portrait phone/tablet and desktop show the copy, empty states, and return affordance without horizontal clipping. At 390px landscape height the initial viewport shows only the learning summary; the review section is below and requires vertical scrolling. This live DOM/geometry check does not establish physical-touch discoverability. `live-55` used a zero-candidate profile; the separate `live-56` follow-up checks populated candidate presentation with an explicit display-only fixture. Natural candidate acquisition and threshold/hysteresis behavior remain separate learning-validation items. This is local-preview evidence, not release evidence or a participant/screen-reader evaluation.

After F-35, `npm run verify:core` passed docs check, current-entry guard, lint, typecheck, the full Vitest suite, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing warnings remain: the unrelated overdue Review By on `2026-09-13-mysterious-island-v3.md`, the `IslandMilestone.tsx` Fast Refresh lint warning, and build advisories for stale Browserslist data, an empty `pdf` chunk, and a >500 kB chunk. This verifies the current source tree; it does not close the broader accessibility, participant, release, or route/state coverage gates.

## Current Island Parent populated review candidates (`live-56`, 2026-09-21)

The current Island navigation journey now checks both the empty Parent review state (`live-55`) and a populated display state. In the same disposable browser context, it first completes the real Settings → parent gate → `/parents` path with no candidates, returns to Settings, and then writes one synthetic active math row and one synthetic vocabulary row with `isWeak: true`. Both fixture rows have zero answer counters; the profile's attempt-log collection is asserted unchanged across fixture insertion. The UI is reopened through the same gate and must show `復習候補：2件`, a resolved Japanese math label, and the known English word `apple`. The fixture bypasses learning resolution deliberately and is not evidence of natural acquisition, thresholds, or hysteresis.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone, populated `/parents` | Both subject groups show one resolved candidate; the phone layout keeps the badges within their panels. Returning from the populated page reaches the original Settings section. | [Phone capture](evidence/screens/live-56-parent-review/390-parent-candidates.png) |
| 768×1024 tablet, populated `/parents` | The same two candidates and labels render in the wider shared Utility layout; the return journey succeeds. | [Tablet capture](evidence/screens/live-56-parent-review/768-tablet-candidates.png) |

Both viewport journeys passed with zero page errors. The browser used `http://127.0.0.1:5198`, app-root `Island=true` / `NatureTown=false`, root-configured delivery `snap-root-v1`, build revision `development-local`, reduced motion enabled, and no service-worker controller. `/parents` is a shared Utility route, so its route-specific delivery and visual candidate are not applicable. The disposable contexts were closed. See the [contact sheet](evidence/screens/live-56-parent-review/contact-sheet.html) and [run summary](evidence/screens/live-56-parent-review/report.json). This closes the populated-display screenshot gap for the current Island target only; natural weak-state acquisition, real-device discoverability, participant evaluation, and real screen-reader evaluation remain open.

## Current Island Other Games and two-player setup (`live-58`, 2026-09-21)

The five-viewport current-Island journey now follows the visible route from the Island menu's `もっと あそぶ` → `ほかの あそび` into `/battle`, opens `2人あそびを くわしく えらぶ`, and verifies the return path to `/#/island`. The shared Other Games and Battle surfaces have no route-specific Island visual candidate; their evidence records the `.app-container` flags and root delivery separately rather than assigning an Island candidate.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone, Other Games → Battle setup | The two-player groups have distinct accessible names; the start action is disabled until both grades are selected. Selecting both enables it. All pressed-state option targets are at least 44×44px; no horizontal document overflow. | [Other Games](evidence/screens/live-58-current-island-battle/390-other-games.png), [Battle setup](evidence/screens/live-58-current-island-battle/390-battle-setup.png) |
| 768×1024 tablet portrait | The setup is gated with the tablet-rotation instruction; the 44px-or-larger return action leads back through Other Games to the Island. | [Guidance](evidence/screens/live-58-current-island-battle/768-battle-rotate-guidance.png) |
| 1280×720 desktop | Two-player setup renders side-by-side. Group names, 44px options, start readiness, and horizontal overflow are checked. | [Other Games](evidence/screens/live-58-current-island-battle/1280-other-games.png), [Battle setup](evidence/screens/live-58-current-island-battle/1280-battle-setup.png) |
| 844×390 and 1024×390 short landscape | Setup remains available and start stays visible. Player fields are in scrollable panels; the UI click check reached each grade selector and enabled start. The screenshots show the compact initial viewport, not proof of physical-device scroll discoverability. | [844×390 setup](evidence/screens/live-58-current-island-battle/844-battle-setup.png), [1024×390 setup](evidence/screens/live-58-current-island-battle/1024-battle-setup.png) |

`npm run e2e:island-navigation` passed all five viewport runs (25 scenario check groups each, 26 for each short-landscape run), with 92 captures and zero page errors. Runtime: `http://127.0.0.1:5198`, `Island=true`, `NatureTown=false`, build revision `development-local`, runtime version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, reduced motion on, service worker off. Island screens identify delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and learning candidate `mystic-island-learning-v2` where applicable; shared screens record root delivery `snap-root-v1` and candidate `not-applicable-shared-utility`. The QA profile and prior fixture data existed only in disposable test contexts, which were closed. Battle was not started and no battle answer or persistent user data was created.

The current screenshots show the Battle form's existing short-landscape scroll density; that behavior is already a physical-device/participant watch item, so this audit adds no speculative hint. No new ownerless high-impact visual or interaction defect was found. The [contact sheet](evidence/screens/live-58-current-island-battle/contact-sheet.html) and [run report](evidence/screens/live-58-current-island-battle/report.json) contain the complete capture set and route identity. This is local-preview evidence, not release evidence, real screen-reader testing, or a child-participant evaluation.

## Current Island Battle play and result (`live-59`, 2026-09-21)

Extended the `live-58` route from Other Games and co-op setup through timed play, timeout result, replay setup, a second timeout, exit, and return to Island. The play fixture accelerates only the Battle's one-second interval to 10ms after setup; the opening countdown remains normal. No answer is submitted, and disposable-profile learning/progression records are compared before and after. This checks reachable UI states and controls, not gameplay pacing, answer correctness, learning behavior, or a child's replay motivation.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone | Both play panels expose a question and in-panel answer controls of at least 44×44px. The timeout summary stays within the viewport; the three metric labels remain on one line. | [Play](evidence/screens/live-59-current-island-battle-runtime/390x844-battle-playing.png), [result](evidence/screens/live-59-current-island-battle-runtime/390x844-battle-timeout-result.png) |
| 768×1024 tablet portrait | Rotation guidance and the large return action remain visible; gameplay is correctly gated until landscape. | [Guidance](evidence/screens/live-59-current-island-battle-runtime/768x1024-tablet-rotate-guidance.png) |
| 1024×768 and 1280×720 landscape/desktop | The question region can scroll internally when short, while every numeric/answer control remains within its player panel. Setup, timed play, timeout result, replay setup, and exit all pass. | [1024×768 play](evidence/screens/live-59-current-island-battle-runtime/1024x768-battle-playing.png), [1280×720 play](evidence/screens/live-59-current-island-battle-runtime/1280x720-battle-playing.png) |
| 844×390 and 1024×390 short landscape | The compact timeout result keeps both 48px replay/exit buttons fully visible; setup/play actions also remain reachable. Scrolling the setup fields on a physical touch device is still a human check. | [844×390 result](evidence/screens/live-59-current-island-battle-runtime/844x390-battle-timeout-result.png), [1024×390 result](evidence/screens/live-59-current-island-battle-runtime/1024x390-battle-timeout-result.png) |

The first six-viewport pass revealed two real clipping edges: at 1024×768 the existing short-height rule stopped at 760px while a 234px math visual reduced the keypad region enough to clip its last row; at 844×390 the result card put its actions below the viewport. The tablet short-height breakpoint now includes 800px and constrains only the scrollable question region, preserving full prompt content and all answer targets. Short-landscape results use a compact two-row player summary and tighter card spacing while preserving 48px actions. The phone result's secondary metric labels were also tightened to avoid awkward two-line wraps. Battle spec 09 records the 800px and result-fit contracts.

`npm run e2e:battle-runtime` passed all six viewports with 27 captures and zero page errors. Every run checks mode selection, setup readiness, question visibility, in-panel ≥44px answer controls, timeout copy/actions, replay readiness, no horizontal document overflow, and unchanged learning records. Runtime identity is recorded per capture: `http://127.0.0.1:5198`, Island=true, NatureTown=false, revision `development-local`, the unique app version (`revision:UUID`), root-configured delivery `snap-root-v1`, shared Utility route candidate not applicable, reduced motion enabled, and no service-worker controller. The shared root now publishes that exact app version on every route and the current-entry guard requires it. The QA profiles are disposable. See the [contact sheet](evidence/screens/live-59-current-island-battle-runtime/contact-sheet.html) and [run report](evidence/screens/live-59-current-island-battle-runtime/report.json).

This shared Utility-route evidence is local-preview layout/runtime evidence only. It does not score visual magnetism against an Island art candidate, establish natural battle timing, verify every question visual, or replace a real-device scroll check, real screen-reader evaluation, or child-participant session.

## Current Island Tug of War result (`live-60`, 2026-09-21)

Extended the current-Island Other Games journey through Tug of War setup, play, five-step win, result, replay, and return. The 390×844 result initially let its three-column winner/duration/score-difference row overflow the card: the winner card's minimum content width pushed the last metric past the phone edge. The portrait-phone result now gives the winner the full row, places the two compact metrics beneath it, and keeps the victory badge on one line (F-36).

The disposable test profile uses grade -2 and temporarily fixes problem generation to the count_5 visual. The test answers by selecting the number visibly shown in the five-frame illustration until the rope reaches the real five-step result; it does not inject Battle reducer state or write learning history. The previously covered co-op timeout journey still accelerates only its one-second Battle interval inside the isolated page. Native learning/progression stores match before and after.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone | Winner, duration, and score difference stay within the result card; the badge stays on one line. Both 48px replay/exit actions are visible. | [Setup](evidence/screens/live-60-current-island-tug-of-war-runtime/390x844-tug-setup-ready.png), [play](evidence/screens/live-60-current-island-tug-of-war-runtime/390x844-tug-playing.png), [result](evidence/screens/live-60-current-island-tug-of-war-runtime/390x844-tug-result.png), [replay](evidence/screens/live-60-current-island-tug-of-war-runtime/390x844-tug-replay-setup.png) |
| 768×1024 tablet portrait | Both two-player modes show the rotation instruction and a large return action; no play is claimed in portrait. | [Co-op guidance](evidence/screens/live-60-current-island-tug-of-war-runtime/768x1024-tablet-rotate-guidance.png), [Tug guidance](evidence/screens/live-60-current-island-tug-of-war-runtime/768x1024-tug-tablet-rotate-guidance.png) |
| 1024×768 and 1280×720 landscape/desktop | Five-step Tug win, contained result metrics, replay readiness, and return path pass. | [1024×768 result](evidence/screens/live-60-current-island-tug-of-war-runtime/1024x768-tug-result.png), [1280×720 result](evidence/screens/live-60-current-island-tug-of-war-runtime/1280x720-tug-result.png) |
| 844×390 and 1024×390 short landscape | Result card and both replay/exit actions stay within the 390px-high viewport; all metric cards remain inside the result panel. | [844×390 result](evidence/screens/live-60-current-island-tug-of-war-runtime/844x390-tug-result.png), [1024×390 result](evidence/screens/live-60-current-island-tug-of-war-runtime/1024x390-tug-result.png) |

`npm run e2e:battle-runtime` passed all six viewports with 48 captures and zero page errors. Tug play/result/replay ran at the five supported landscape/phone viewports; the tablet-portrait case correctly stayed at the rotation gate. The full route also verifies the Island menu → Other Games entry and return to Island. Each capture identifies `http://127.0.0.1:5198`, Island=true, NatureTown=false, revision `development-local`, runtime version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, root delivery `snap-root-v1`, shared Utility candidate not applicable, reduced motion enabled, and no service-worker controller. See the [contact sheet](evidence/screens/live-60-current-island-tug-of-war-runtime/contact-sheet.html) and [run report](evidence/screens/live-60-current-island-tug-of-war-runtime/report.json).

This remains local-preview layout/runtime evidence only. One deterministic counting visual does not cover all Battle question visuals. Natural play timing, physical-device scroll discoverability, real screen-reader order, visual-appeal scoring, and child-participant evaluation remain open.

## Current Island Battle question-visual matrix (`live-61`, 2026-09-21)

Expanded the current-Island Other Games journey to render every math question-visual kind available to Battle grades. The first inspection found that the short-landscape answer keypad left the question frame only about one line high. For landscape viewports at or below 500px tall, each player panel now places its scrollable question beside the keypad and preserves a 96px minimum question area; the live runs rendered 176px. Battle spec 09 now records this contract (F-37).

The isolated matrix selects representative grade-eligible skills for `single-items`, `addition-items`, `subtraction-items`, `number-line`, and `operation-base10`. It advances these disposable questions only with Skip and does not submit answers. The separate Tug of War result journey still answers its visible five-frame count to reach the real five-step result. Each viewport uses a fresh browser context/profile, and the learning/progression stores compare unchanged before and after.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone | All five kinds render; the longer operation-base10 drawing uses the question frame's internal scroll while answer controls remain visible. | [operation-base10 top](evidence/screens/live-61-current-island-battle-visuals/390x844-battle-visual-operation-base10.png), [scroll end](evidence/screens/live-61-current-island-battle-visuals/390x844-battle-visual-operation-base10-scroll-end.png) |
| 768×1024 tablet portrait | Both two-player modes show rotation guidance and a large return action; no play is claimed in portrait. | [Co-op guidance](evidence/screens/live-61-current-island-battle-visuals/768x1024-tablet-rotate-guidance.png), [Tug guidance](evidence/screens/live-61-current-island-battle-visuals/768x1024-tug-tablet-rotate-guidance.png) |
| 1024×768 and 1280×720 | All five kinds render beside their 44px-or-larger answer controls; long diagrams remain reachable inside the question frame. | [1024×768 operation-base10](evidence/screens/live-61-current-island-battle-visuals/1024x768-battle-visual-operation-base10.png), [1280×720 operation-base10](evidence/screens/live-61-current-island-battle-visuals/1280x720-battle-visual-operation-base10.png) |
| 768×390, 844×390, and 1024×390 short landscape | The per-player question area stays 176px high beside the complete keypad; operation-base10 scrolls to its full diagram without moving or hiding the controls. | [768×390 addition](evidence/screens/live-61-current-island-battle-visuals/768x390-battle-visual-addition-items.png), [1024×390 operation-base10 top](evidence/screens/live-61-current-island-battle-visuals/1024x390-battle-visual-operation-base10.png), [scroll end](evidence/screens/live-61-current-island-battle-visuals/1024x390-battle-visual-operation-base10-scroll-end.png) |

`npm run e2e:battle-runtime` passed seven viewports with 90 captures and zero page errors: six playable layouts and the tablet-portrait rotation gate. Runtime identity in every capture: `http://127.0.0.1:5199`, build revision `f31d44ed`, runtime version `f31d44ed:85f6b925-d58a-43fb-a163-2e3c04caf1a5`, Island=true, NatureTown=false, root delivery `snap-root-v1`, shared Utility candidate not applicable, reduced motion enabled, and no service-worker controller. The [contact sheet](evidence/screens/live-61-current-island-battle-visuals/contact-sheet.html) and [run report](evidence/screens/live-61-current-island-battle-visuals/report.json) retain per-capture identity and the diagnostic fixture boundary.

Review gates remain separate: runtime integrity passes for this local browser journey; visual appeal was not scored because this shared Utility surface has no route-specific candidate; silent comprehension/safety, natural timing, physical-device scroll discoverability, real screen-reader behavior, and child-participant evaluation remain open. In particular, the phone-portrait operation-base10 sample also needs internal scrolling; no physical-device or participant evidence is claimed.

## Current Island route sweep and root identity gate (`live-62`, 2026-09-21)

The additional 1024×768 sweep traversed first-run Welcome, Island home and tabs, learning, furniture placement, home/house, photos, settings, parent review, and the Island → Other Games → Battle setup/return path. During this sweep, the route-level `.island-page` identity was found to omit the common `.app-container` Nature Town flag. An Island child marker alone could therefore label a mixed Island+Nature Town runtime as current Island evidence (F-38).

`tools/island-e2e-helpers.mjs` now records both identities for Island captures: page delivery/candidates and the app-root revision, unique version, configured delivery, visual lineage, Island flag, and Nature Town flag. Missing root flags remain `null`, never silently become `false`. The current whole-app navigation runner strictly requires root Island=true, NatureTown=false, configured delivery `snap-root-v1`, and matching root/page revision and version before writing each screenshot. Shared Utility captures use that same helper and strict flag gate. Two focused helper tests cover the complete root record and missing-marker behavior.

| Viewport / state | Result | Evidence |
|---|---|---|
| 1024×768 tablet-landscape / desktop | All 25 route/action checks pass across 18 captures, with zero page errors. Checks include first-run and profile-add framing, five-tab routes, home reload and history, learning draft/close, furniture cancel/save, camera close, house/photos, parent empty/populated states and return, and Battle setup/return. | [Contact sheet](evidence/screens/live-62-current-island-navigation-1024x768/contact-sheet.html), [run report](evidence/screens/live-62-current-island-navigation-1024x768/report.json) |

Runtime identity recorded for every capture: `http://127.0.0.1:5200`, build revision `e115e896`, runtime version `e115e896:a1504f4e-471b-4a2e-9a45-7a5cb0d84f95`, root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`. Island screens additionally identify `mystic-island-v1` and their visual/learning candidates; shared Utility screens correctly report no route-specific candidate. Reduced motion was enabled and there was no service-worker controller. Disposable browser contexts were closed; no persistent user data was changed. Nature Town's separate uncommitted working-tree changes were not edited or staged, and the preview flag remained false.

This is local-preview layout, navigation, and runtime-identity evidence—not release evidence, a complete route/state audit, or a score of visual appeal, silent comprehension/safety, real screen-reader behavior, physical-touch discoverability, or child-participant experience.

## Current Island narrow-phone first-run (`live-63`, 2026-09-21)

### F-39 — Welcome CTA was below the first viewport on a 320×568 phone (medium, fixed and runtime checked)

The first-run Island Welcome had not been checked at the 320×568 minimum-phone viewport. Before the fix, the welcome page's content measured 621px tall; the “まなぶ” button ended at y=597, 29px below the viewport, and the “おはな” / “あかり” labels wrapped. At widths up to 360px, the item controls now use tighter gaps and padding with smaller icons. At widths up to 360px and heights up to 600px, the scene and surrounding controls compact while preserving a one-line choice label, 44px-or-larger choice targets, and a 48px-or-larger learning CTA. The 320×568 render now keeps the whole CTA visible (top y=489, bottom y=537).

The first rerun then exposed an E2E assumption, not an app regression: at 320×568 the existing Battle `OrientationGate` intentionally shows the “もうすこし 大きな画面で あそんでね” screen instead of setup. The navigation runner now validates that existing guidance, its 44px-or-larger return action, and return to Other Games rather than waiting for the unsupported setup screen.

| Viewport / state | Result | Evidence |
|---|---|---|
| 320×568 first-run Welcome | Both choice labels stay on one line; all choice actions and the full “まなぶ” CTA are visible without page scrolling. | [Welcome capture](evidence/screens/live-63-current-island-narrow-phone-320x568/320-welcome.png) |
| 320×568 Battle entry | The intentional small-screen guidance appears with a 44px-or-larger return action and returns to Other Games. | [Guidance capture](evidence/screens/live-63-current-island-narrow-phone-320x568/320-battle-small-screen-guidance.png) |
| 320×568 route sweep | 26 route/action checks pass across 18 captures with zero page errors. | [Contact sheet](evidence/screens/live-63-current-island-narrow-phone-320x568/contact-sheet.html), [run report](evidence/screens/live-63-current-island-narrow-phone-320x568/report.json) |

Runtime identity for every capture: `http://127.0.0.1:5200`, revision `f720ca5a`, version `f720ca5a:65ae4ed5-17a0-4ed3-8179-8f81f9045997`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`. Welcome identity is delivery `mystic-island-v1` and candidate `mystic-island-shore-garden-v18`; the navigation runner is `island-navigation-five-tabs-v2`. Reduced motion was enabled, there was no service-worker controller, and all profile fixtures lived in disposable browser contexts. The target is the current Island app, not classic Explore or Nature Town.

The layout and runtime behavior are checked in a local preview. The screenshot confirms the visible scene and actions, but this pass does not score independent visual appeal or establish child comprehension/enjoyment, screen-reader behavior, physical-device feel, release/update behavior, or whole-app completion. Those quality gates remain separate and open.

## Current Island photo gallery compact-screen action (`live-64`, 2026-09-21)

### F-40 — The populated photo action overlapped fixed navigation or sat below the short-landscape viewport (medium, fixed and runtime checked)

The earlier photo check used an empty album, so it missed the taller populated state. On the pre-fix current-Island build (`79f343ed`), the camera action measured y=463.8–507.8 while fixed navigation began at y=493.2 on 320×568, hiding about 14.5px of its hit area. At 390×600, y=516.3–560.3 overlapped navigation beginning at y=534.4 by about 25.9px. On 844×390 the action started at y=356, below the app root's y=324.4 viewport edge. The screenshots below are the populated state, not the empty-album false negative.

The cause was a tall, single-column photo flow whose camera action followed all gallery content while the app navigation remained fixed. It did not adapt to either the short portrait height or short-landscape width. On phones up to 480px wide and 640px high, the first photo thumbnail is now capped at 152px. At widths from 600px and heights up to 430px, photos use a separate scrolling list and a dedicated camera-action rail; opening a photo restores the full-size single-column detail view.

| Viewport / state | Result | Evidence |
|---|---|---|
| 320×568 populated gallery | Camera action remains at least 44px high and fully above fixed navigation. | [Before, revision `79f343ed`](evidence/screens/live-64-photo-gallery-2026-09-21/before-320-populated-photos-79f343ed.png), [after](evidence/screens/live-64-photo-gallery-2026-09-21/320-photos.png) |
| 390×600 populated gallery | Camera action remains fully visible and hit-testable above navigation. | [After](evidence/screens/live-64-photo-gallery-2026-09-21/390-photos.png) |
| 844×390 and 1024×390 populated galleries | The photo list scrolls independently; the camera action stays in the visible side rail. | [Before, revision `79f343ed`](evidence/screens/live-64-photo-gallery-2026-09-21/before-844-populated-photos-79f343ed.png), [844 after](evidence/screens/live-64-photo-gallery-2026-09-21/844-photos.png), [1024 after](evidence/screens/live-64-photo-gallery-2026-09-21/1024-photos.png) |
| Photo detail | Full-size single-column detail remains available without being covered by navigation. | [320 detail](evidence/screens/live-64-photo-gallery-2026-09-21/320-photo-detail.png), [844 detail](evidence/screens/live-64-photo-gallery-2026-09-21/844-photo-detail.png) |
| Four-viewport current-Island route sweep | 107 route/action checks and 74 captures pass; zero page errors. | [Contact sheet](evidence/screens/live-64-photo-gallery-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-64-photo-gallery-2026-09-21/report.json) |

Runtime identity for every capture: `http://127.0.0.1:5200`, revision `b1f72572`, version `b1f72572:471181dc-8870-4f91-8725-04115895aed0`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`, photo delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, navigation candidate `island-navigation-five-tabs-v2`. Reduced motion was enabled, there was no service-worker controller, and the populated photo fixture lived in a disposable browser context. Nature Town and classic Explore were not the target.

This verifies local-preview geometry, navigation separation, and route behavior for the tested sizes. It does not establish independent visual appeal, physical-touch discoverability, assistive-technology behavior, child comprehension/enjoyment, other route/state coverage, or whole-app completion.

## Current Island tablet-portrait and desktop route sweep (`live-65`, 2026-09-21)

The compact-phone and short-landscape passes did not cover the current candidate at tablet portrait and desktop sizes. The same current-Island navigation journey now passes at both 768×1024 and 1280×720: 26 route/action checks per viewport, 36 captures total, and zero page errors. Tablet portrait correctly presents the existing Battle rotation guidance and return path; desktop reaches the two-player setup and verifies the distinct player groups, 44px-or-larger options, grade selection, and start readiness.

| Viewport / state | Result | Evidence |
|---|---|---|
| 768×1024 tablet portrait | 26 route/action checks pass across 18 captures; Battle presents its intended rotation guidance and returns to Other Games. | [Contact sheet](evidence/screens/live-65-current-island-tablet-desktop-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-65-current-island-tablet-desktop-2026-09-21/report.json) |
| 1280×720 desktop | 26 route/action checks pass across 18 captures, including two-player setup semantics, option target sizes, and start readiness. | [Battle setup capture](evidence/screens/live-65-current-island-tablet-desktop-2026-09-21/1280-battle-setup.png), [contact sheet](evidence/screens/live-65-current-island-tablet-desktop-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-65-current-island-tablet-desktop-2026-09-21/report.json) |

Runtime identity for every capture: `http://127.0.0.1:5200`, revision `e7978858`, version `e7978858:ad5ad720-9281-4fbd-b57e-d4339b02edfd`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`, navigation candidate `island-navigation-five-tabs-v2`. Island routes use `mystic-island-v1` / `mystic-island-shore-garden-v18`; learning captures also record `mystic-island-learning-v2`. Reduced motion was enabled, there was no service-worker controller, and all profile fixtures were confined to disposable browser contexts.

A visual spot-check of Welcome, Island home, learning input, records, settings, photos, parent review, and Battle setup found no additional confirmed high-impact layout defect in those frames. The player panels were scrollable, but the preview pass did not establish physical-device discoverability; the later `live-70` geometry check and `live-72` follow-up below independently confirmed that the next required grade group was clipped and added a visual direction cue. Physical-device use, screen-reader behavior, participant evaluation, release/update, and full route/state coverage remain open.

## Current Island Battle setup prerequisite guidance (`live-67`, 2026-09-21)

### F-41 — Disabled Battle start did not explain the missing player grades (medium, fixed and runtime checked)

At 390×844, the second player's grade picker continues below the initial viewport inside a scrollable setup panel. The fixed `スタート！` action correctly stayed disabled until both grades were selected, but the screen did not say why. This made a valid prerequisite look like an unresponsive button, especially when Player 2's unfinished section was below the fold.

The setup now places a concise dynamic `role="status"` message immediately above the fixed action. It names the missing grade selection for Player 1, Player 2, or both, and changes to a ready message when both are selected. The E2E journey asserts all three states. Battle eligibility, focus behavior, persistence, and game rules are unchanged. A before/after comparison is available for the compact phone state.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844, before vs. after | Before: disabled start had no visible reason. After: the missing two-player grade prerequisite is explained above the fixed action. | [Before, revision `4c023d6f`](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/before-390-battle-setup-4c023d6f.png), [after](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/390-battle-setup.png) |
| 844×390, 1024×390, and 1280×720 | Status stays clear at short landscape and desktop sizes; the route verifies the missing-grade → one-player-missing → ready transitions. | [Contact sheet](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/report.json) |
| Four-viewport current-Island route sweep | 106 route/action checks and 74 captures pass; zero page errors. | [Contact sheet](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-67-current-island-battle-setup-guidance-2026-09-21/report.json) |

Runtime identity for every after capture: `http://127.0.0.1:5200`, revision `ef6ec876`, version `ef6ec876:f9c4808d-b99b-4528-9503-c8e70c3385d0`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`. Battle setup is a shared utility surface, so there is no route-specific visual candidate (`not-applicable-shared-utility`). Reduced motion was enabled, there was no service-worker controller, and disposable profile fixtures were used. The before image is from revision `4c023d6f` with Island=true / NatureTown=false.

This verifies local-preview status visibility and state changes, not actual screen-reader announcement order, physical-device scroll discoverability, child comprehension/enjoyment, release/update behavior, or whole-app completion. Those remain separate human and release gates.

## Current Island compact-phone route sweep (`live-68`, 2026-09-21)

The canonical Island route survey was extended to 320×568 and 360×640 so the first-run shell, learning, records, photos, settings, parent views, Other Games, and Battle entry could be compared at the narrow-phone boundary. This supplements the 390px phone runs; it is not a claim that every route state is covered.

| Viewport | Coverage | Result | Evidence |
|---|---|---|---|
| 320×568 | First-run Welcome, Island routes, photos/detail, parent views, Other Games, and Battle's intentional small-screen guidance/return | 27 route/action checks; 18 captures; zero errors | [Contact sheet](evidence/screens/live-68-current-island-edge-phone-2026-09-21/contact-sheet.html), [report](evidence/screens/live-68-current-island-edge-phone-2026-09-21/report.json) |
| 360×640 | Same narrow-phone route sweep; Battle reaches two-player setup at the supported minimum height | 27 route/action checks; 18 captures; zero errors | [Contact sheet](evidence/screens/live-68-current-island-edge-phone-2026-09-21/contact-sheet.html), [report](evidence/screens/live-68-current-island-edge-phone-2026-09-21/report.json) |

Each capture identifies `http://127.0.0.1:5264`, revision `14b5fcc1`, version `14b5fcc1:7c43d8a1-57a4-41eb-908a-c2cb4d528106`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, and route-appropriate delivery/candidate metadata. Navigation captures use `island-navigation-five-tabs-v2`; shared Battle/Utility surfaces correctly report no route-specific visual candidate. Reduced motion was on and no service worker controlled the preview. Screens were visually spot-checked across Welcome, home, learning, play, records, settings, photos, parent review, and Battle entry. The disposable browser fixture did not alter the user's profile. The route sweep did not include the eventual Battle timeout-result state; that required the focused flow below.

## F-42: Current Island Battle result label clipped at 360×640 (`live-69`, 2026-09-21)

The focused current-Island Battle flow exposed a narrow-phone issue missed by the route sweep: in the cooperative timeout result at 360×640, the `チームダメージ` label needed 76px but its metric column exposed only 71px. The forced single-line label therefore overflowed by 5px. The pre-fix screenshot and failing report retain the measured state: [result before fix](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/before-360x640-battle-timeout-result.png), [before report](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/before-report.json).

The phone-portrait cooperative result now uses 20px horizontal screen padding and an 8px metric gap. The original 11px label size and three equal metric columns are preserved; at 360×640 each label now has 76px, with `clientWidth === scrollWidth`. The selector excludes Tug of War. Replay and exit remain visible at 48px high. The fix is committed as `554f9ba9`.

| Viewport | Runtime result | Evidence |
|---|---|---|
| 360×640 | 12 focused Battle checks pass; all three labels fit at 76px; replay and exit are 48px high and within the screen | [Result after fix](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/360x640-battle-timeout-result.png) |
| 390×844 | 12 focused Battle checks pass; all three labels fit at 86px; replay and exit remain within the result card | [Result](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/390x844-battle-timeout-result.png) |
| Both viewports | 24 route/action checks, 30 captures, zero page errors. The journey covers Other Games → cooperative setup/play/timeout/result/replay/end and Tug of War setup/play/win/result/replay. The five Battle question visuals use disposable fixtures; learning records are compared before/after and remain unchanged. | [Contact sheet](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/contact-sheet.html), [after report](evidence/screens/live-69-current-island-battle-edge-phone-2026-09-21/report.json) |

Every after-capture reports revision `554f9ba9`, version `554f9ba9:6a6b273b-a8c0-4ec2-ac59-60fd4f53db08`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, shared Utility candidate not applicable, reduced motion enabled, and no service-worker controller.

`npm run verify:core` passed: docs and current-entry checks, lint, typecheck, 457 Vitest files / 4,153 tests, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing/non-blocking diagnostics were one `IslandMilestone.tsx` Fast Refresh lint warning; the overdue Review By warning for the unrelated `2026-09-13-mysterious-island-v3.md`; stale Browserslist data, the empty `pdf` chunk, and a bundle chunk over 500 kB. `git diff --check` also passed. This is local-preview evidence only: physical-device touch discovery, actual screen-reader order, participant comprehension/enjoyment, release/update behavior, Nature Town, and full route/state coverage remain open.

## F-43: Current Island Battle setup hid the next required grade below its scrollport (`live-70` / `live-72`, 2026-09-21)

The tablet/desktop route sweep at `live-70` exposed a discoverability gap left by F-41: the fixed start action explained that grades were required, but did not show where the next required grade group was. At 1024×768 the setup scrollport ended at y=676 while Player 1's grade group ended at y=807; at 1280×720 the scrollport ended at y=628 and only the top edge of the group (y=619) was visible. The [1024×768 before capture](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/before-1024-battle-setup.png) and [1280×720 before capture](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/before-1280-battle-setup.png) show the unchanged prerequisite status beside a disabled start action. This is deterministic clipping evidence, not an inference about physical-device behavior.

Battle setup now tracks the first player whose grade is still required and compares that group with the actual scrollport. When any part is clipped, a compact `↑`, `↓`, or `↕` scroll-direction cue appears beside the existing prerequisite status and disappears once the required group is fully visible or all grades are selected. It follows Player 2 after Player 1 is ready. The cue is supplementary and does not change start gating, status copy, or gameplay. A first follow-up (`live-71`) caught a stale cue after the final grade was selected; rendering now checks that the cue belongs to the currently required player, and the final `live-72` run verifies that it clears.

| Viewport / state | Result | Evidence |
|---|---|---|
| 390×844 phone and 844×390 short landscape | The cue tracks the required grade group as setup is scrolled and as the required player changes; existing Battle return and navigation checks pass. | [Phone setup](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/390-battle-setup.png), [short-landscape setup](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/844-battle-setup.png) |
| 1024×768 and 1280×720 | The initially clipped required grade shows a downward cue; selecting both grades clears it and enables start. | [1024×768 after](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/1024-battle-setup.png), [1280×720 after](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/1280-battle-setup.png) |
| Four viewports | `npm run e2e:island-navigation` passes 105 route/action checks across 73 captures, with zero page errors. The route sweep also validates the scroll-cue geometry and the Player 1 → Player 2 → ready state changes. | [Contact sheet](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/contact-sheet.html), [run report](evidence/screens/live-72-current-island-battle-scroll-cue-2026-09-21/report.json) |

Runtime identity for every capture: `http://127.0.0.1:5264`, revision `554f9ba9`, version `554f9ba9:6a6b273b-a8c0-4ec2-ac59-60fd4f53db08`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`, and navigation candidate `island-navigation-five-tabs-v2`. Battle is a shared Utility surface with no route-specific visual candidate. Reduced motion was enabled and no service worker controlled the preview. The route fixtures were disposable and did not write learning history.

This confirms local-preview layout and runtime integrity for the tested sizes; it does not score visual appeal or establish physical-device scroll discovery, real screen-reader behavior, silent comprehension/safety, or child-participant experience. Those remain separate open gates.

## F-44: Current Island five-tab labels wrapped at 320px (`live-74` / `live-76`, 2026-09-21)

The 320×568 route sweep's screenshot showed that the fixed five-tab Island navigation split `まなぶ` and `きろく` onto two lines. A focused DOM assertion reproduced two line rectangles for those labels while `しま`, `いえ`, and `設定` remained on one line. The cause was the 52px grid column losing 16px to each tab's horizontal padding, leaving only 36px for a three-character label. The [before capture](evidence/screens/live-76-current-island-nav-320-2026-09-21/before-320-home.png) and [raw failing report](evidence/screens/live-76-current-island-nav-320-2026-09-21/before-report.json) preserve the reproduction.

At widths up to 360px, tab horizontal padding is now 2px and labels do not wrap. The labels remain inside their buttons, each at least 44×44px; their order, icon, active state, and meaning are unchanged. Updated [display-layout spec 44](../../../product/44_display_layout_spec.md) to make one-line labels part of the 320px minimum-width contract. The [before/after contact sheet](evidence/screens/live-76-current-island-nav-320-2026-09-21/contact-sheet.html) includes the current Island home and Records views.

| Viewport | Result |
|---|---|
| 320×568 | Five labels are one line and fit their ≥44px-wide buttons. Full Island route/action journey passes, including Welcome, home, play, guide, inventory, customization, experience, help, house, settings, learning, records, photos/detail, parent, Other Games, and Battle small-screen guidance. |
| 390×844, 768×1024, 844×390, 1280×720 | The same navigation and return-flow journey passes; the narrow-only CSS does not alter these layouts. |
| All five viewports | `npm run e2e:island-navigation` passes 133 route/action checks across 91 captures with zero page errors. The 320px test measures each label's line rectangles, containment within its button, and button width/height. QA profile, answer, and photo fixtures were confined to disposable browser contexts. |

Runtime for the `live-76` run: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, navigation candidate `island-navigation-five-tabs-v2`, reduced motion enabled, and no service-worker controller. This local route/layout evidence does not substitute for physical-device touch, screen-reader, or child-participant evaluation.

`npm run verify:core` passed after this change: docs check, current-UI entry guard, lint, typecheck, 457 Vitest files / 4,154 tests, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing diagnostics: one `IslandMilestone.tsx` Fast Refresh lint warning; the unrelated overdue Review By warning in `2026-09-13-mysterious-island-v3.md`; stale Browserslist data, the empty `pdf` chunk, and a chunk above 500 kB. `git diff --check` passed. This does not close the task's screen-reader, physical-device, participant, or exhaustive route/state gates.

## F-45: Other Games desktop heading detached from its centered choices (`live-77` / `live-78` / `live-81`, 2026-09-21)

On the 1280×720 current-Island `/#/battle` chooser, the global header kept `ほかの あそび` at the left while the narrow choice stack was centered. The browser measurement gave heading center x=269.93 and first-choice center x=640, a 370.07px difference. The [before screenshot](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/before-1280-other-games.png) and [failing geometry report](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/before-alignment-report.json) preserve it.

At widths ≥700px, only this screen's title and eyebrow are now centered over the choice column; the back button stays at the left. Phone widths keep their original left-aligned header. A regression check requires title/card center delta ≤2px. The [after screenshot](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/after-1280-other-games.png) and [contact sheet](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/contact-sheet.html) show the result.

## F-46: Third Other Games choice sat under the fixed navigation at 844×390 (`live-79` / `live-80` / `live-81`, 2026-09-21)

The short-landscape screenshot showed the third primary game card partially behind the bottom tabs. Before the fix, navigation began at y=324.41 while the third card ended at y=344.19, hiding about 19.78px. The [before screenshot](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/before-844-other-games.png) and [failing layout report](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/before-landscape-report.json) retain that state.

For widths ≥600px and heights ≤430px, only the chooser's vertical gaps and card padding are compacted. All three primary game choices remain completely above the navigation, and each stays at least 44×44px. The [after 844×390 screenshot](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/after-844-other-games.png) confirms the visible result. The secondary “2人あそびを くわしく えらぶ” link remains below the fold at this height; its scroll discovery on a physical device is still unverified.

`npm run e2e:island-navigation` passed all five viewports: 320×568, 390×844, 768×1024, 844×390, and 1280×720. The run covered 137 route/action checks across 91 captures with zero page errors. It now checks wide-header alignment, short-landscape choice visibility, and minimum target sizes. Runtime identity for every capture: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, app-root Island=true / NatureTown=false, configured delivery `snap-root-v1`, visual lineage `pokko-field-v1`, navigation candidate `island-navigation-five-tabs-v2`, reduced motion enabled, and no service-worker controller. Battle/Other Games are shared Utility screens with no route-specific visual candidate. QA profile and fixtures were isolated and discarded. Full report: [live-81 report](evidence/screens/live-81-current-island-other-games-layout-2026-09-21/after-report.json).

`npm run verify:core` passed after F-45/F-46: docs and current-entry guard, lint, typecheck, Vitest 457 files / 4,154 tests, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing/non-blocking diagnostics remain: the unrelated overdue Review By on `2026-09-13-mysterious-island-v3.md`, one `IslandMilestone.tsx` Fast Refresh lint warning, stale Browserslist data, an empty `pdf` chunk, and a bundle chunk above 500 kB.

This is local-preview geometry and navigation evidence, not physical-device touch evidence. It does not establish screen-reader behavior, child comprehension/enjoyment, visual appeal scores, or whole-app completion; these remain separate gates.

## F-47: Photo capture action hidden under fixed navigation below 600px in short landscape (`live-82` / `live-83` / `live-84`, 2026-09-21)

The existing short-landscape photo layout only activated at widths ≥600px. At 599×430, and again at 480×430, the populated album kept its normal tall vertical flow: the 44px `しゃしんを とる` action occupied y=388.77–432.77 while the fixed navigation began at y=364.41. The action was entirely behind the navigation and could not be tapped. The [599px before screenshot](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/before-599-photos.png), [480px before screenshot](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/before-480-photos.png), and their failing reports preserve both reproductions.

The short-height layout now applies at every width: the album alone scrolls in the main column and the camera action stays in a side rail above fixed navigation. The side rail scales down on narrow screens while preserving a ≥44px target; photo detail returns to a single full-width reading surface. See [after 320×430](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/after-320-photos.png), [after 599×430](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/after-599-photos.png), and [after 1024×390](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/after-1024-photos.png).

The `live-84` boundary sweep covered 320×430, 480×430, 599×430, 600×430, 699×720, 700×720, and 1024×390: all seven journeys passed, with 193 route/action checks, 131 captures, and zero page errors. The photo action remained fully above the navigation. Runtime identity on all captures: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, app-root Island=true / NatureTown=false, root delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, reduced motion enabled, no service-worker controller. The default navigation E2E now includes 599×430 so future short-landscape runs cover the just-below-600 boundary. `live-85` passed the default 390×844 / 768×1024 / 599×430 set: 81 route/action checks, 55 captures, zero page errors; at every short-height viewport, the E2E also requires all three primary Other Games choices above fixed navigation. See [live-85 regression report](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/default-regression-report.json). Full `live-84` report: [boundary sweep](evidence/screens/live-84-current-island-photo-short-landscape-2026-09-21/after-report.json).

`npm run verify:core` passed after F-47: docs and current-entry guard, lint, typecheck, Vitest 457 files / 4,154 tests, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing/non-blocking diagnostics remain: the unrelated overdue Review By on `2026-09-13-mysterious-island-v3.md`, one `IslandMilestone.tsx` Fast Refresh lint warning, stale Browserslist data, an empty `pdf` chunk, and a bundle chunk above 500 kB.

These are local browser layout checks, not physical-device touch, assistive-technology, participant, or whole-app completion evidence.

## F-48: Short-landscape learning keypad clipped below the 700px breakpoint (`live-86` / `live-87` / `live-88`, 2026-09-22)

On the current Island learning screen at 599×430, the stacked phone layout gave the answer input y=169.19–219.19 and the keypad y=227.19–451.19. All 13 keys were 47.5px high, but the final row extended 13.19px below the 430px viewport. The hint/skip row occupied y=369–414, overlapping the final keypad row by about 18px. The [before screenshot](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/before-599-learning.png) and [failing geometry report](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/before-geometry-report.json) preserve the issue.

The landscape workbench now switches from stacked to two-column at widths ≥480px (height ≤600px): only the prompt/support column scrolls, while the answer, complete keypad, and assistance actions remain in view. The [480×430 after screen](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/after-480-learning.png), [599×430 after screen](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/after-599-learning.png), and [1024×390 after screen](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/after-1024-learning.png) show the responsive result. The permanent navigation E2E measures every keypad key, answer input, and help action, and requires each key to remain at least 44×44px and fully visible.

The `live-87` boundary sweep covered 480×430, 599×430, 699×430, 700×430, and 1024×390: five journeys passed with 147 route/action checks, 95 captures, and zero page errors. A separate `live-88` 599×430 run passed 29 checks / 19 captures / zero errors and retained exact after geometry: input y=102.19–150.19; keypad y=158.19–384; all 13 targets 61.5×52.95px; assistance actions y=384–428 at 44px, with no overlap. Runtime identity: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, app-root Island=true / NatureTown=false, root delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, reduced motion enabled, no service-worker controller. Full reports: [live-87 boundary report](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/after-boundary-report.json), [live-88 exact geometry](evidence/screens/live-87-current-island-short-landscape-learning-layout-2026-09-22/after-599-geometry-report.json).

This is local browser geometry and screenshot evidence, not physical-device touch, real screen-reader, participant, or whole-app completion evidence.

`npm run verify:core` passed after F-48: docs/current-entry guard, lint, typecheck, full Vitest suite, production build, and asset budget (PWA precache 11.60 MiB / 12.00 MiB). Existing/non-blocking diagnostics remain: an unrelated overdue Review By, one `IslandMilestone.tsx` Fast Refresh lint warning, stale Browserslist data, the empty `pdf` chunk, and a chunk above 500 kB. `git diff --check` and `node --check tools/e2e-island-navigation.mjs` also passed. This closeout does not establish physical-device touch, assistive-technology, participant, or whole-app completion evidence.

## F-49: Compact-landscape Welcome CTA pushed below the fold (`live-89`, 2026-09-22)

At 480×431, the old vertical Welcome flow used a full-size island scene and accumulated 619px of page content; the `まなぶ` CTA sat at y=469–595 and was wholly below the initial viewport. A separate `max-height:430px` compact rule made the layout change abruptly at 430px, leaving the 431–600px short-landscape band uncovered. The 480×431 baseline was measured during the earlier failing run; its screenshot/report was not retained, so this entry does not present a reconstructed “before” image as captured evidence.

For landscape widths ≥480px and heights ≤600px, Welcome now places the island scene on the left and the two 44px choices plus 48px `まなぶ` CTA in a right-hand action rail. Final E2E geometry confirms zero document overflow and all three targets in the first viewport at 480×320 and the original 480×431 boundary. See the [480×431 after screen and geometry report](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/responsive-regression/contact-sheet.html), [480×320 after contact sheet](evidence/screens/live-89-current-island-short-landscape-2026-09-22/run-480x320-touch-targets/contact-sheet.html), [480×401 boundary sheet](evidence/screens/live-89-current-island-short-landscape-2026-09-22/run-480x401-boundary/contact-sheet.html), and [480×400 / 568×320 sheet](evidence/screens/live-89-current-island-short-landscape-2026-09-22/run-480x400-568x320-touch-targets/contact-sheet.html).

## F-50: Very-short landscape learning hid keypad rows and help (`live-89`, 2026-09-22)

At 480×320 before the fix, the answer field occupied y=102.19–150.19 and the keypad y=158.19–274. The last 44px key row extended to y=344.19 (24.19px outside the viewport) and overlapped the assistance row at y=274–318 by about 18px. The [before screenshot](evidence/screens/live-89-current-island-short-landscape-2026-09-22/before/pre-f50-480x320-learning.png) and [failing geometry report](evidence/screens/live-89-current-island-short-landscape-2026-09-22/before/pre-f50-480x320-report.json) preserve this expected pre-fix failure.

At landscape heights ≤400px, progress and help/skip controls now live in the left prompt column, while the answer field and complete 13-key keypad use the right column. At 480×320 after the fix, all keys are 49.5px high and fully visible; the actual hint state renders its support panel at y=221.91–266, above the help action at y=270–318, without page scrolling or overlap. The E2E opens a hint only in the disposable profile and checks that it creates no answer log, answer-count increment, or plan advance (the normal support/relearning safeguard remains). The [after hint screenshot](evidence/screens/live-89-current-island-short-landscape-2026-09-22/run-480x320-touch-targets/480x320-learning-hint.png) and final [480×320 geometry report](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/after-geometry/report.json) preserve the measurements.

## F-51: Third Other Games choice obscured by fixed navigation (`live-89`, 2026-09-22)

At 480×320 before the fix, each game card was about 84px high. The third card ended at y=344.19 while fixed navigation began at y=254.41, leaving that primary choice hidden behind the navigation. The [before screenshot](evidence/screens/live-89-current-island-short-landscape-2026-09-22/before/pre-f51-480x320-other-games.png) and [failing report](evidence/screens/live-89-current-island-short-landscape-2026-09-22/before/pre-f51-480x320-report.json) preserve the reproduction.

Short-landscape cards now scale to 64px, and to 48px at heights ≤400px; the three primary game choices remain at least 44px high and above the fixed navigation. The additional details selector remains reachable by its own internal scroll. At 480×320, all three cards measure 48px high and end at y=116 / 170 / 224, above navigation at y=254.41. The regression journey exercises all three choices and the details route; final viewport evidence is in the `live-89` contact sheets and the [current geometry report](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/after-geometry/report.json).

The final `live-89` current-source viewport set (480×320, 480×400, 480×401, and 568×320) passed all 4 journeys: 123 route/action checks, 79 captures, and zero page errors. Runtime identity on captured Island screens: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, app-root Island=true / NatureTown=false, root delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, reduced motion enabled, and no service-worker controller. The Other Games utility itself has no route visual candidate. Before-fix failures are retained separately from passing after-fix runs.

These are local Playwright geometry, interaction, and screenshot checks, not physical-device touch, real screen-reader, participant comprehension/enjoyment, release/update, or whole-app completion evidence. The overall audit remains partial and active.

## F-52: Photo capture action hidden at 431px (`live-90`, 2026-09-22)

The previous photo short-height treatment stopped at `max-height:430px`. At 480×431, the ordinary vertical gallery returned: the 44px `しゃしんを とる` action occupied y=389.77–433.77 while fixed navigation began at y=365.41, leaving the action entirely inaccessible. The [before screenshot](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/before/pre-f52-480x431-failure.png) and [failing report](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/before/pre-f52-480x431-report.json) retain the exact breakpoint failure.

The existing all-width treatment remains for heights ≤430px; an additional width ≥480px, landscape, height ≤600px query now uses the same independent album scroller and camera side rail. At 480×431 after the fix, the camera action is y=212.75–256.75, 44px high, with navigation at y=365.41; at 599×430 it is y=212.25–256.25 above navigation at y=364.41. The regression run covers 480×431, 599×430, 390×844, and 768×1024 (111 route/action checks, 73 captures, zero page errors). A geometry-focused run at 480×320 / 480×431 / 599×430 passes 89 checks / 57 captures / zero errors and stores the camera-action and all three Other Games card rectangles in the report. See [geometry/contact sheet](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/after-geometry/contact-sheet.html), [responsive regression report](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/responsive-regression/report.json), and [responsive contact sheet](evidence/screens/live-90-current-island-photo-breakpoint-2026-09-22/responsive-regression/contact-sheet.html).

The permanent default Island navigation E2E now includes 480×431, so this one-pixel-over-boundary case remains covered. Runtime screenshots retain the same Island/Nature Town flags, version, delivery, candidate, reduced-motion, and service-worker identity described above. The overall audit is still partial: no physical-device, assistive-technology, participant, release/update, or complete all-state evidence is claimed.

`npm run verify:core` passes after F-52 and the geometry-report enhancement: docs/current-entry guard, lint, typecheck, Vitest (457 files / 4,154 tests), production build, and asset budget (11.61 MiB / 12.00 MiB). Existing advisories remain: an unrelated overdue Review By in `docs/tasks/active/2026-09-13-mysterious-island-v3.md`, one `IslandMilestone.tsx` Fast Refresh lint warning, 8-month-old Browserslist data, the empty `pdf` chunk, and a chunk above 500 kB. `node --check tools/e2e-island-navigation.mjs` and `git diff --check` pass.

## Records scroll reachability (`live-91`, 2026-09-22)

Checked the shared Records/Stats utility surface in the canonical Island app at 844×390 (`http://127.0.0.1:5198/#/stats`): Island enabled, Nature Town disabled, root delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, reduced motion on, service worker off. The utility itself is shared UI, not a separate art candidate. The disposable-profile navigation run passes 35 checks / 22 captures / zero page errors.

The actual browser-wheel journey reaches the end of the internal `.utility-layout-scroll`: 1,222px content in a 256px scrollport, `scrollTop=966` (the maximum), while the document remains 390px high. The scrollport and fixed-nav top both land at y=324.41; the final report section ends at y=299.78. Scrolling up 200px brings its final heading fully into view at y=182.78–206.78, above the fixed nav. The [initial Records view](evidence/screens/live-91-current-island-records-scroll-2026-09-22/844x390-records.png), [bottom of the scroll](evidence/screens/live-91-current-island-records-scroll-2026-09-22/844x390-records-scrolled-bottom.png), [heading back in view](evidence/screens/live-91-current-island-records-scroll-2026-09-22/844x390-records-scrolled.png), [all-screen contact sheet](evidence/screens/live-91-current-island-records-scroll-2026-09-22/contact-sheet.html), and [machine-readable report](evidence/screens/live-91-current-island-records-scroll-2026-09-22/report.json) preserve the run.

This confirms reachability with a desktop browser wheel only; it does not establish touch discoverability on a real device, screen-reader behavior, or child comprehension/enjoyment. No Records defect is confirmed and no cue or production UI change was made. The broader route/state audit remains partial.

## Records scroll responsive matrix (`live-92`, 2026-09-22)

Repeated the internal-scroll and fixed-navigation assertions across the canonical default viewport set: 390×844 phone, 768×1024 tablet, 480×431 and 599×430 short landscape, and 568×320 compact landscape. All five use the same Island-enabled runtime and shared Records surface; all 157 route/action checks pass, with 103 captures and zero page errors. Each Records capture identifies `/#/stats`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, root delivery `snap-root-v1`, Island enabled, Nature Town disabled, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, reduced motion on, and service worker off. Records is shared Utility UI, not a separate art candidate. In every viewport, `document.documentElement.scrollHeight` equals the viewport height, the Records content reaches its internal scroll end, the final report section ends 24px above the fixed navigation, and its heading can be reached above navigation (already visible at the lower end on the two tall viewports; brought into view by scrolling up on the shorter ones).

| Viewport | Internal content / viewport | Max scroll | Report bottom / nav top | Final heading y |
| --- | ---: | ---: | ---: | ---: |
| 390×844 | 1968 / 710px | 1258px | 753.78 / 778.41px | 372.78–396.78px |
| 768×1024 | 1222 / 890px | 332px | 933.78 / 958.41px | 616.78–640.78px |
| 480×431 | 1884 / 297px | 1587px | 340.78 / 365.41px | 183.78–207.78px |
| 599×430 | 1820 / 296px | 1524px | 339.78 / 364.41px | 246.78–270.78px |
| 568×320 | 1820 / 186px | 1634px | 229.78 / 254.41px | 136.78–160.78px |

The [five-viewport contact sheet](evidence/screens/live-92-current-island-records-scroll-2026-09-22/contact-sheet.html) and [full machine-readable report](evidence/screens/live-92-current-island-records-scroll-2026-09-22/report.json) retain route captures and runtime identity for every viewport. This remains desktop-wheel/browser evidence, not physical-touch discovery, assistive-technology, or child-participant evidence; no production UI change is warranted by this check alone.

## F-53: Inventory continuation was undiscoverable on short landscape (`live-97`, 2026-09-22)

Impact: P2. The inventory choices remain in the app's own vertical scroller, but their labels and the secondary furniture destination were placed below the fixed navigation with no indication to continue. At 568×320, the two item cards occupy y=163.53–342.66 while navigation begins at y=254.41; both names start at y=275.53 and descriptions at y=304.22. The internal page is 458px tall in a 254px viewport (`maxScrollTop=204`). At 480×431 and 599×430, both item cards fit, but the 44px `くらしの どうぐを みる` action at y=360.66–404.66 extends under navigation at y=365.41 / 364.41. In each case, the page is scrollable; the failure was the absence of a discoverability cue, not an unreachable destination.

`IslandInventory` now observes its `.island-page` scroller and appends the existing calm `↓ つづき` cue to the instruction only while more content is below. It reuses `hasMoreContentBelow`, reacts to scroll/resize/content-size changes, hides at the end, and reappears at the top. This keeps the screen's item-selection purpose and colors unchanged. In the 568×320 journey, a 100px wheel gesture brings both full cards into view at y=63.53–242.66 (names/descriptions also above navigation); at the end (`scrollTop=204`), the furniture action is y=156.66–200.66 and the cue is gone. At 480×431 / 599×430, scrolling to the end places that action at y=267.66–311.66 / 266.66–310.66. The cue is absent on fitting 390×844 and 768×1024 captures.

The canonical five-viewport run passes 160 route/action checks / 107 captures / zero page errors. Runtime identity is consistent: `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, Island enabled / Nature Town disabled, root `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, reduced motion on, service worker off. The [five-viewport contact sheet](evidence/screens/live-97-current-island-inventory-scroll-cue-2026-09-22/contact-sheet.html) and [full runtime report](evidence/screens/live-97-current-island-inventory-scroll-cue-2026-09-22/report.json) preserve before/scroll/end states. `npm run verify:core` passes: lint (one pre-existing Fast Refresh warning), typecheck, 457 Vitest files / 4,154 tests, production build and 11.61/12.00 MiB asset budget. Existing Browserslist, empty `pdf` chunk and >500 kB chunk advisories remain. Browser-wheel validation is not physical-device or child-participant evidence; those gates and the broader route/state audit remain open.

The `live-98` minimum-portrait check found no extra scrolling need at 320×568: `scrollHeight=clientHeight=502`, `maxScrollTop=0`, so the cue stays hidden; both item cards and their labels fit, and the 44px furniture action ends at y=404.66 before navigation at y=502.41. Its dedicated [contact sheet and runtime report](evidence/screens/live-98-current-island-inventory-320-portrait-2026-09-22/contact-sheet.html) record 32 checks / 20 captures / zero errors, with the same Island=true / Nature Town=false identity and candidate. The navigation E2E now includes 320×568 by default and asserts the no-overflow/no-cue branch as well as the three overflowing landscape branches. The resulting six-viewport run passes 192 checks / 127 captures / zero page errors; its [aggregate report](evidence/screens/live-98-current-island-inventory-320-portrait-2026-09-22/report-six-viewports.json) records 320×568, 390×844, 768×1024, 480×431, 599×430, and 568×320 under the same runtime version. The other five rendered screens and their scroll captures remain in `live-97`. This is still browser-wheel evidence, not physical-touch discovery or participant evidence.

## Utility-route capture settling (`live-99`, 2026-09-22)

The `live-98` 320×568 `/#/battle` screenshot made the bottom navigation look empty. Reproduction showed five route-shell buttons in the DOM with visible styles and 44px-or-larger hit areas, but the screenshot taken 300–500ms after navigation did not paint the tab icons/labels; later frames did. This was a capture-timing ambiguity, not sufficient evidence of a persistent UI defect. No app presentation code changed.

`captureUtility` in `tools/e2e-island-navigation.mjs` was changed to wait 800ms after a shared Utility route transition. The `live-99` run passed 32 checks / 20 captures with zero page errors, and the default six-viewport run passed 192 checks / 127 captures / zero errors. At the time, the 320×568 screenshot was recorded as corrected; a later byte-level review found it still matched the blank-footer image from `live-98`. See the `live-101` correction below. Root identity remains Island=true / Nature Town=false, delivery `snap-root-v1`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`; this shared route has no separate Island visual candidate.

## Correction: 320px Utility full-screen capture still omits the footer (`live-101`, 2026-09-22)

A fresh 320×568 current-Island navigation run passed 33 checks with zero page errors at `http://127.0.0.1:5198/#/battle`. Runtime identity: revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, Island=true, Nature Town=false, root delivery `snap-root-v1`; the shared Utility route has no feature visual candidate.

The full-viewport PNG remained byte-identical to the earlier blank-footer capture (`e911a1af…`) even after a 1.6s settle, and an `.app-container` element capture produced the same image. In the same run, the navigation's computed layout was visible (`display:grid`, `opacity:1`, `z-index:50`) at x=0, y=502.41, 320×65.59px. All five buttons had ≥44px height and 53.59px width; `elementsFromPoint` hit the central SVG → button → navigation, with no covering element. A separate navigation-element screenshot shows all five icons and labels. The 360×640 full-screen capture also shows them.

This is a screenshot-composition inconsistency, not proof that the app omits the tabs at runtime. However, the 320px full-screen image is not reliable visual evidence, and a headful/physical-device confirmation remains open. No app presentation code changed. The [comparison page](evidence/screens/live-101-utility-screenshot-capture-2026-09-22/capture-comparison.html) and [runtime/E2E report](evidence/screens/live-101-utility-screenshot-capture-2026-09-22/report.json) preserve both captures and the DOM/hit-test measurements. The `live-99` wait increase should not be treated as having fixed or visually verified this capture.

The same 320px review found the learning brand heading visually ellipsized (`scrollWidth=112`, `clientWidth=82`), while the accessibility snapshot retains the full “ふしぎな しま” heading and the sound/close controls remain 44px or larger. The initial decision was to defer a change pending a layout comparison. That comparison and the later measured fix are recorded in F-54 below.

## F-54: Compact-phone learning header truncated the Island name (`live-100`, 2026-09-22)

Impact: P3. At 320×568, the learning header had a single-line “ふしぎな しま” heading but constrained its 112px text to an 82px box. The visible ellipsis made the current product identity look inconsistent with the full title on Home; the accessible heading still contained the full name, so the next action was not ambiguous.

For widths up to 360px, the learning-only header now uses 8px safe-area-aware horizontal padding, an 8px gap, and a 15px heading. This lets the full title fit without wrapping or increasing the header height. At 320×568, heading scroll/client width is 99/99px, header bottom and workbench top remain 64.19px, the sound control stays 100.88×44px, the close control 56.97×55.19px, and document width remains 320px. At 360×640 the heading also fits; at 390×844 the breakpoint does not apply.

The regression now asserts full text, no horizontal overflow, unchanged vertical workspace, and ≥44px sound/close targets at narrow portrait widths; 360×640 was added to the default viewport set as the breakpoint boundary. The [three-viewport contact sheet and report](evidence/screens/live-100-learning-header-title-fit-2026-09-22/contact-sheet.html) record 95 checks / 60 captures / zero errors across 320×568, 360×640, and 390×844. The complete seven-viewport default run passes 226 checks / 147 captures / zero errors across phone, tablet, and short landscape. Runtime identity is Island=true / Nature Town=false, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`. Browser verification does not substitute for child comprehension, physical-device, or assistive-technology evaluation.

## F-55: Island read-error recovery used stale primary actions (`live-102`, 2026-09-22)

Impact: P3. The Island-session load failure already had retry copy, but its out-of-page `.island-loading` surface escaped the current Island blue-button override and inherited the older yellow primary. The profile-resolution fallback was more inconsistent: it used the browser-default button and the shorter `もういちど` label. Either failure could therefore show a recovery action that looked unrelated to the current PokoMoko primary action.

Both failure branches now render the shared `IslandLoadingError` component, with `しまを ひらけなかったよ。` and the clearly named `もういちど ひらく` action. `PokomokoWorld.css` applies the existing blue/deep-blue primary tokens to that loading/error surface, including the pressed shadow, even though it is outside `.island-page`.

`live-102` used fresh Playwright browser contexts, each with one disposable native profile and no learning activity. Two initial Island transactions were rejected to cover the two React development StrictMode effect mounts; a separate profile-read failure was filtered to `getActiveProfile` called from `Island.tsx`, avoiding the earlier `PrivateRoute` guard. Across 320×568 and 568×320, both error branches expose `role=alert`, the same 176.47×54px retry button, white-on-blue (`rgb(49, 95, 181)`) colors, and no horizontal overflow. Each reload returns to Island home with zero page errors. Before retry, the six inspected stores (islands, plans, events, logs, math memory, vocabulary memory) are empty; after retry, only the new Island record exists, with plans/events/logs/memories still at zero. The development app-root identity is Island=true / Nature Town=false, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, configured delivery `snap-root-v1`. Since the error fallback renders before the Island root, it has no route-specific candidate marker; after recovery, the same context reports delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and learning candidate `mystic-island-learning-v2`.

The [eight-screen contact sheet](evidence/screens/live-102-island-retry-error-state-2026-09-22/contact-sheet.html) and [runtime/geometry/store report](evidence/screens/live-102-island-retry-error-state-2026-09-22/report.json) preserve both failure branches and both viewport sizes. The default seven-viewport Island navigation E2E also passes after this change: 226 checks / 147 captures / zero page errors. `npm run verify:core` passes (457 files / 4,154 tests, production build, 11.61/12.00 MiB asset budget). Existing advisories remain: an unrelated overdue Review By in the Island v3 task, one `IslandMilestone.tsx` Fast Refresh warning, 8-month-old Browserslist data, an empty PDF chunk, and a chunk over 500 kB. This is injected local-browser recovery evidence, not a real storage outage, physical-device, screen-reader, or participant evaluation; the overall audit remains partial.

## Current Island wide-screen route sweep (`live-103`, 2026-09-22)

The canonical Island runtime at `http://127.0.0.1:5198` passed the navigation journey at 1024×768 and 1440×900: 30 route/action checks per viewport, 40 screenshots total, zero page errors. Each capture records revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, Island=true / Nature Town=false, root delivery `snap-root-v1`, and lineage `pokko-field-v1`. Island-root captures use delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, and learning candidate `mystic-island-learning-v2`; shared Utility surfaces record no route-specific Island candidate.

The route sheet includes first-run Welcome, Island home and panels, house, learning, photos, Settings, Records, empty/populated Parent review, Other Games, and two-player Battle setup. Spot-checked screenshots showed no new high-priority layout or cross-surface consistency defect. The Parent return CTA sits near the fixed navigation in the initial frame; the browser journey scrolls it into view before activating it. That establishes automated reachability only, not natural touch discoverability, so no extra cue or layout change was made without device/participant evidence. QA profiles, the temporary answer/photo, and display-only Parent fixture were confined to disposable browser contexts and discarded at context close.

The [40-screen contact sheet](evidence/screens/live-103-current-island-wide/contact-sheet.html) and [runtime/action report](evidence/screens/live-103-current-island-wide/report.json) retain the evidence. This expands wide-screen coverage but does not complete every route/state, real-touch, screen-reader, participant, or release/update gate; the whole-app audit remains partial.

## F-56: Battle question diagrams hid their continuation (`live-104` / `live-105`, 2026-09-22)

Impact: P2. The baseline wide Battle sweep (`live-104`) showed that at 1024×768 a common two-group question occupied 232px of scroll content inside a 174px client viewport, with no visible continuation cue. Its panel still had 57px answer targets, so the short-height rule was needlessly limiting the question area. Denser diagrams also needed scrolling at other sizes, but the scroll surface did not explain that more of the problem was below.

For roomy 960px+ screens between 740px and 800px tall, the question frame can now use the 15rem cap; the 1024×768 addition sample grows to 234px and fits while answer targets remain at least 44px. When a diagram still overflows, a reserved in-frame line shows `↓ つづき`, changes to `↕ うごかしてね` while between ends, and becomes `↑ もどる` at the end. The question content itself is a labelled, keyboard-focusable region only when it actually scrolls. A new question resets its scroll position to the first row.

The post-change [59-screen contact sheet](evidence/screens/live-105-current-island-battle-scroll-cue-2026-09-22/contact-sheet.html) and [runtime report](evidence/screens/live-105-current-island-battle-scroll-cue-2026-09-22/report.json) cover 844×390, 1024×768, 1280×720, and 1440×900 with 12 journey checks per viewport, the five Battle visual families, zero page errors, and no learning-record changes. Regression assertions cover the no-cue/fitting case, down/up direction, labelled focus target, keyboard `End` scrolling, answer target sizes, and reset-on-new-question. Captures identify `http://127.0.0.1:5198`, revision `development-local`, version `development-local:60d95334-58f8-4594-b548-c113c90cad1c`, Island=true / Nature Town=false, root delivery `snap-root-v1`, reduced motion, and no service-worker controller.

This is local browser evidence, not a physical-touch, real screen-reader, child-participant, or release/update evaluation; the broader audit remains partial.

## Current Island production PWA checkpoint (`live-106`, 2026-09-22)

The first attempt to run the PWA journey against a plain `npm run build` preview stopped before evidence because that build reports `island.enabled=false` in `version.json`; it was not accepted as a current-Island result. Rebuilding with the required `VITE_ISLAND_ENABLED=true` contract and serving the result at the separate production-preview port `5298` produced an Island-enabled manifest and passed `npm run e2e:island-pwa` with eight protected-flow checks.

The run covered current Island welcome and seeded home, deferred update during active learning, a persistence hold across navigation, answer/checkpoint restoration, automatic growth and maturity checkpoints, explicit legacy Explore resume preservation, one legacy Island migration with its unclaimed gift and ten stars intact, and a hook-free real Service Worker offline reload. The offline checkpoint was controlled by the real worker, loaded cached JavaScript/CSS, kept reduced motion and sound off, persisted the answer and automatic growth, and resumed the same plan. No Nature Town screen was used: the report records `Island=true`, `NatureTown=false`, root delivery `snap-root-v1`, Island delivery `mystic-island-v1`, visual candidate `mystic-island-shore-garden-v18`, learning candidate `mystic-island-learning-v2`, revision `development-local`, and version `development-local:aa15cdca-fce5-4459-b532-643f80341c65`.

The [three-screen contact sheet](evidence/screens/live-106-current-island-pwa-2026-09-22/contact-sheet.html) and [full PWA report](evidence/screens/live-106-current-island-pwa-2026-09-22/pwa-report.json) preserve the result. This is production-preview/browser evidence; it does not cover a real two-build update, physical-device behavior, real screen-reader output, participant understanding/enjoyment, or normal-speed human timing.

## Release-mode separation and two-build checkpoint (`live-108`, 2026-09-22)

The explicit classic release gate was rerun with `VITE_ISLAND_ENABLED=false VITE_BUILD_PLAY_ENABLED=false`. The current-entry guard, lint, typecheck, full Vitest suite, production build/assets check, and all 31 classic smoke scenarios passed. The run exposed a stale E2E selector in the classic PWA update harness: the onboarding input is named by its visible label and exposes `あだ名でOK` as a placeholder, so `getByRole('textbox', { name: 'あだ名でOK' })` never targeted it. The update and two-build harnesses now use `getByPlaceholder('あだ名でOK')` for both filling and retention assertions.

After that test-only correction, the classic PWA update harness passed its four checks (onboarding safe-router handoff, protected router recovery, same-route Battle checkpoint, and real Service Worker version-drift recovery). The two-build harness also passed real old→new Service Worker update, protected-form/persistence retention with one reload, stale-worker network recovery, offline/cache/data retention. This validates the intentional classic regression path and its separation from the current Island evidence; it does not relabel classic Explore as the current app. Existing non-blocking advisories remain the overdue unrelated Review By, one Fast Refresh warning, stale Browserslist data, an empty PDF chunk, and large chunks.
