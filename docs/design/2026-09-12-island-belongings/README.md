# 実物で選ぶ、もちもの・いろ・ひろげる

2026-09-12。仕様48の任意操作UIを実装したローカル変更。公開・最終アート承認は行っていない。

- もちもの：購入見本と同じモデルを、現在の育ち・色で表示。詳細にも実物を残し、移設・収納・色変更へつなぐ。家具に花用の成長完了文を表示しない。
- いろ：対象の実モデルを3色で比較。ぽこもこはスカーフの色が変わることを表示し、使用中・使える色・不足を分ける。見本だけで消費しない。色が変わらないランタンには色交換を提示しない。
- ひろげる：実配置と家の上面図から追加3列を選び、明示確定。予告時の土地・残高は不変。
- しまの ようす：編集4ボタンを外し、住人の好み・活動・いぶきを読む。320×568で既存CSSが隠していた見出しと閉じる操作を復元し、本文を12pxにした。

## 継承と境界

基準は既存v8の実物商品見本。ミントの見本面、既存モデル・素材・色・生育段階と生成りの操作面を継承する。文字だけのもちもの一覧、小さい色丸、図のない拡張ボタン、情報面の編集メニュー重複は継承しない。新しい家具・報酬・画像生成・学習入力・経済・DB形式・writerは追加変更していない。新しい見本は一度描いて画像をキャッシュし、WebGLコンテキストを保持しない。

同じ作業ディレクトリには別作業のv9住人モーションと商品スワイプがある。これらを含む固定production artifactで検査し、このタスクの変更としては扱わない。詳細なビルド識別とartifact hashは `artifact.json`、実画面は `contact-sheet.html`、各viewportの結果は `report.json` を参照。

## 検証

- `npm run docs:check`: PASS。
- `npm run lint`: PASS、既存IslandMilestone.tsxのFast Refresh警告1件。最終変更ファイルのESLintもPASS。
- `npm run typecheck`: PASS。
- `npm run test:run`: 350 files / 3,660 tests PASS。見本の画角と閉じる操作の修正後は型検査・lint・buildと実ブラウザの対象経路を再確認した。
- `npm run build`: PASS。最終CSS修正後は同じflagで外部ディレクトリへproduction build。`assets:check` PASS、98 precache files / 10.71 MiB。
- `npm run e2e:smoke`: 31 scenarios PASS。これは共通導線のDEV回帰であり、下記固定productionの結果と区別する。
- `tools/e2e-island-life-belongings.mjs`: 390×844、768×1024 reduced motion、320×568。初回設定から空状態、情報面と退出、もちもの詳細、実物の色比較、色変更、収納・再配置、拡張予告/確定、再読込、同じ学習への復帰を確認。7つの学習関連storeは編集前後で不変。音なしでも操作可能。

必要残高と成熟状態は分離したブラウザ内の明示的な過去credit・配置fixtureで作る。実学習からの獲得・子どもの理解や動機の証拠ではない。実参加者N=0、実機iOS/インストール済みPWA、故障注入、通常連問の再計測は今回の対象外。既存E2E helperの編集入口を「つくる」に更新したが、旧専用ハーネス全体の再実行を意味しない。

## 判定

- 視覚：実物を選ぶ一覧、対象の色の比較、拡張方向の予告を実画面で確認。最終アート承認は未実施。
- 無説明理解・安全：読み取れる操作と取り除く確認を維持。独立参加者による無説明理解は未検証（HOLD）。
- Runtime：記載した固定ビルドの対象経路はPASS。アプリ全体の公開判定を兼ねない。

初回の320px診断では閉じるボタンが既存の短画面CSSで非表示だった。`initial-small-failure.png`を保持し、見出しを常時表示する修正後の同じ操作経路で解消を確認した。
