# Done Log - 2026-03

Use this file for March 2026 completions.

## 2026-03-13 - Workspace docs operating system foundation

### Summary

- Added the repo-level docs operating system for constitution, ownership, verification, runbooks, archive policy, and durable risk tracking.

### Scope

- Files/areas: `CONSTITUTION.md`, `docs/README.md`, `docs/memory.md`, `docs/verification_matrix.md`, `docs/runbooks/*`, `docs/ownership_map.md`, `docs/risk_register.md`, `docs/archive_policy.md`, `docs/archive/*`, `.agent/skills/*`

### Docs / Decisions

- Updated: documentation routing, verification shortcuts, design review flow, archive policy, ownership map, risk register, schema migration and backlog triage runbooks
- Intentionally unchanged: app behavior specs under `docs/01_app_spec.md` and child specs

### Verification

- Commands: `npm run typecheck`, `npm run verify:core`, `git diff --check`
- Manual checks: read-through for role boundaries, links, and SSOT alignment

### Risks / Follow-ups

- Status/backlog docs still need periodic freshness reviews so they do not become shadow specs.

## 2026-03-13 - Added docs automation baseline

### Summary

- Added a lightweight docs checker so link drift and missing doc metadata are caught by command instead of only read-through.

### Scope

- Files/areas: `tools/check-docs.mjs`, `package.json`, `docs/verification_matrix.md`, `docs/memory.md`, `docs/README.md`, `docs/runbooks/backlog-triage.md`, `docs/12_ui_fix_tasklist.md`

### Docs / Decisions

- Updated: docs verification baseline, backlog triage runbook, UI tasklist freshness metadata
- Intentionally unchanged: app specs and code behavior

### Verification

- Commands: `npm run docs:check`
- Manual checks: read-through of doc role boundaries and stale-doc expectations

### Risks / Follow-ups

- The checker intentionally stays lightweight and does not replace human review for tone or product accuracy.

## 2026-03-13 - Added CI parity for docs and core verification

### Summary

- Added GitHub Actions workflows so docs and core verification run outside local machines too.

### Scope

- Files/areas: `.github/workflows/docs-check.yml`, `.github/workflows/verify-core.yml`, `docs/verification_matrix.md`, `docs/runbooks/release-checklist.md`, `docs/risk_register.md`

### Docs / Decisions

- Updated: CI baseline now mirrors `npm run docs:check` and `npm run verify:core`
- Intentionally unchanged: release smoke still remains a local/manual gate rather than always-on CI

### Verification

- Commands: `npm run docs:check`, `git diff --check`
- Manual checks: workflow trigger scope and command parity review

### Risks / Follow-ups

- CI only catches what the local commands cover; device-specific UX checks still stay manual.

## 2026-03-17 - State UI token alignment

### Summary

- Refreshed shared state UI primitives and the most visible settings-related modals so modal, badge, progress, and loading surfaces now use the current glass/token language.

### Scope

- Files/areas: `src/index.css`, `src/components/ui/Modal.tsx`, `src/components/ui/Badge.tsx`, `src/components/ui/ProgressBar.tsx`, `src/components/ui/Spinner.tsx`, `src/components/gate/ArithmeticGateModal.tsx`, `src/components/gate/ParentGateModal.tsx`, `src/components/domain/PaperTestScoreModal.tsx`, `docs/10_design_refresh_status.md`, `docs/tasks/active/2026-03-17-v1-core-stabilization-plan.md`

### Docs / Decisions

- Updated: shared surface tokens/utilities, design refresh status, parent stabilization plan
- Intentionally unchanged: feature behavior specs in `docs/01_app_spec.md` and the remaining screen-level P0 task files

### Verification

- Commands: `npm run lint`, `npm run typecheck`, `npm run test:run`, `npm run build`, `node tools/check-docs.mjs`
- Manual checks: not run in this pass; follow-up should confirm modal contrast, safe-area spacing, and mobile readability in the app

### Risks / Follow-ups

- Screen-level restyling is still needed in `Study` / `Battle` / `Stats` / `Settings` / `Onboarding` for full visual consistency.

## 2026-03-17 - Study and battle tone alignment

### Summary

- Refreshed the main learning surfaces so study overlays, timers, input panels, and battle screens now share the current glass/token-based tone.

### Scope

- Files/areas: `src/pages/StudyLayout.tsx`, `src/pages/Battle.tsx`, `src/components/domain/TenKey.tsx`, `src/components/domain/ChoiceGroup.tsx`, `src/components/domain/MultiNumberInput.tsx`, `src/components/domain/HissanCell.tsx`, `src/components/battle/BattleArena.tsx`, `src/components/battle/BattleSetup.tsx`, `src/components/battle/PlayerPanel.tsx`, `src/components/battle/TugOfWarBar.tsx`, `src/components/battle/BossCoopBar.tsx`, `src/components/battle/BattleCountdown.tsx`, `src/components/battle/OrientationGate.tsx`, `src/components/battle/BattleResult.tsx`, `docs/10_design_refresh_status.md`, `docs/tasks/active/2026-03-17-v1-core-stabilization-plan.md`

### Docs / Decisions

- Updated: design refresh status, parent stabilization plan
- Intentionally unchanged: `src/pages/Study.tsx` and learning-rule specs under `docs/01_app_spec.md`

### Verification

- Commands: `npm run lint`, `npm run typecheck`, `npm run test:run`, `npm run build`
- Manual checks: not run in this pass
- Extra note: `npm run e2e:smoke` was attempted and failed on stale Home selectors that predate this slice

### Risks / Follow-ups

- `Stats` / `Settings` / `Onboarding` still need the same tone pass to complete P0.
- Smoke scenarios should be updated to the current Home footer/start flow before relying on them as a regression gate.

## 2026-03-17 - Secondary screen tone alignment

### Summary

- Refreshed stats, settings, and onboarding so the support surfaces now match the shared glass/token system used by Home, Study, and Battle.

### Scope

- Files/areas: `src/pages/Stats.tsx`, `src/pages/Settings.tsx`, `src/pages/Onboarding.tsx`, `src/components/Header.tsx`, `docs/10_design_refresh_status.md`, `docs/tasks/active/2026-03-17-v1-core-stabilization-plan.md`

### Docs / Decisions

- Updated: design refresh status, parent stabilization plan
- Intentionally unchanged: `docs/12_ui_fix_tasklist.md` and feature behavior specs under `docs/01_app_spec.md`

### Verification

- Commands: `node tools/check-docs.mjs`, `npm run lint`, `npm run typecheck`, `npm run test:run`, `npm run build`
- Manual checks: not run in this pass; follow-up should confirm onboarding on a narrow viewport plus stats/settings contrast on real devices

### Risks / Follow-ups

- P0 visual alignment is complete, but smoke selectors still need to catch up with the current Home start flow before `npm run e2e:smoke` is reliable again.
- Device and accessibility checks remain the next gate before treating the refreshed UI as fully stabilized.

## 2026-03-17 - Smoke gate realignment

### Summary

- Updated the smoke runner so it follows the current Home footer CTA and only attaches to a matching Sansu dev server, restoring `npm run e2e:smoke` as a usable regression gate.

### Scope

- Files/areas: `tools/e2e-smoke.mjs`, `docs/10_design_refresh_status.md`, `docs/done/2026-03.md`, `docs/tasks/active/2026-03-17-v1-core-stabilization-plan.md`

### Docs / Decisions

- Updated: design refresh status, parent stabilization plan
- Intentionally unchanged: app feature specs and screen-level SSOT docs

### Verification

- Commands: `npm run lint`, `npm run e2e:smoke`
- Manual checks: not run in this pass

### Risks / Follow-ups

- Smoke is back, but it still covers only the minimum path set; deeper regression coverage for the core learning flow remains a P1 follow-up.
- Device and accessibility checks are still manual gates.
